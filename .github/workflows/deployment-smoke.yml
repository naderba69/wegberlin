name: Deployment Smoke

on:
  workflow_dispatch:
    inputs:
      deployment_url:
        description: "Exact Vercel Preview or Production HTTPS origin"
        required: true
        type: string
  deployment_status:

permissions:
  contents: read

env:
  DEPLOYMENT_URL: ${{ inputs.deployment_url || github.event.deployment_status.environment_url || github.event.deployment_status.target_url }}

jobs:
  smoke:
    if: github.event_name == 'workflow_dispatch' || github.event.deployment_status.state == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Verify deployed critical routes and headers
        run: node scripts/smoke-deployment.mjs
