# تدقيق شامل للإخلالات والنقائص — Der Weg nach Berlin

**تاريخ التدقيق:** 2026-09-12 — Africa/Tunis  
**نوع التدقيق:** فحص شيفرة، عقود اختبارات، محتوى، وثائق، Offline، صوت، نشر وحوكمة.  
**الحكم المختصر بعد دفعة الإصلاح:** أُغلقت الأعطال البرمجية الحرجة المكتشفة في دورة المفردات وQuality Gate وWorkflows والحوكمة المركزية، وأضيف تحقق Responsive نوعي. المنصة اجتازت البوابة المحلية، لكنها لا توصف بإصدار نهائي معتمد قبل المراجعات البشرية والبعيدة المعلّقة.

> هذا التقرير لا يدعي مراجعة لغوية ألمانية مستقلة، ولا اختبار أجهزة/قارئات شاشة حقيقية، ولا مراجعة حقوق. النتائج البشرية المؤجلة مفصولة عن نتائج الشيفرة.

## تحديث تنفيذي لاحق للتدقيق — الحالة الحالية

هذه الفقرة هي الحالة المرجعية الأحدث؛ تفاصيل الأعطال أدناه توثق خط الأساس الذي اكتُشف قبل الإصلاح:

- `npm run check`: **PASS** بعد إصلاح الاختبار وتشغيل Lint وTypeScript و978/978 Unit عبر 143 ملفًا وSecret/Source/Handoff/Build وPostbuild budgets.
- دورة المهارات أصبحت Terminal بلا Modulo، تحفظ إكمال كل عبارة في IndexedDB، وتنتقل من Vocabulary إلى Discover عند اكتمال المجموعة.
- خطوة الكلام أصبحت محاولة ميكروفون لحظية: Signal check ثم Whisper محلي ثم مطابقة كل كلمات العبارة بترتيبها؛ لا حفظ للصوت ولا ادعاء فونيمي.
- هدف الكتابة أصبح متكيفًا مع المستوى وشكل العبارة، مع Token-boundary وحد سياق ورسالة إصلاح.
- الاستماع لا يُحتسب إلا بعد بدء TTS بنجاح.
- Workflows الثلاثة مثبتة تحت `.github/workflows`؛ RC يطلب Label ويرفع Artifact فعليًا، وTagged Release يمرر Tag/Previous/Output.
- Dictation وBranching وCollocations دخلت 16 عائلة Zod و3,020 سجل Lifecycle/Similarity/Review عبر 4,622,320 زوجًا و0 Issues.
- مصفوفة Browser الحالية: **82/82** (41 Desktop + 41 Mobile)، وتشمل 320×568 و360×800 و768×1024 عبر `compact/default/large` مع كشف القص والتراكب والنص الخارج عن الشاشة.
- أضيف `local-review-reminder-v1`: يعتمد بطاقات SRS المستحقة فعليًا، يحترم المنطقة الزمنية وساعات الهدوء، ويحفظ الإخفاء اليومي في IndexedDB؛ إشعار الجهاز اختياري أثناء فتح المنصة فقط بلا Push أو ادعاء تشغيل خلفي.
- بقيت حدود Whisper صريحة: النتيجة تؤكد الكلمات المتوقعة ولا تثبت فونيمًا أو لهجة أو طلاقة.

### متابعة التدقيق الإنتاجي الثانية

استخرج الفحص اللاحق خمس فجوات إضافية وأُغلقت برمجيًا:

- كان العداد القديم `dueReviews` يستطيع توجيه جلسة الخمس دقائق إلى Review رغم عدم وجود بطاقة مؤهلة؛ أصبح الهدف يعتمد طابور SRS الحقيقي، والقيمة الابتدائية صارت صفرًا.
- كانت بعض الواجهات تعرض `targetExam` وContent/Route IDs تقنية؛ أصبحت تعرض أسماء Goethe/telc وعناوين بشرية مع إبقاء المعرف داخل حمولة البلاغ فقط.
- أربعة مختبرات محدودة كانت تستعمل Modulo وتعود صامتًا إلى أول عنصر: الإملاء، المحادثات المتفرعة، التلازمات، وملاءمة السياق. أصبحت تنتهي أو ترتقي للمستوى التالي، وإعادة الجولة أصبحت قرارًا صريحًا.
- كانت نسخة المنهج مؤلفة يدويًا في TypeScript وService Worker؛ أصبح JSON واحد مصدر التأليف و`runtime:materialize` يولد وحدة TypeScript وقيمة Worker في `prepare` و`prebuild` مع اختبار Drift.
- كانت الحزمة القديمة المكتملة بلا `curriculumVersion` تصنف Build update؛ أصبحت `unknown-installed-curriculum` وتُعامل كتغيير منهج غير مؤكد.

---

## 1. منهجية الفحص

تم تنفيذ أو مراجعة ما يلي:

- `npm ci`: نجح، و`npm audit` سجل **0 ثغرات**.
- `npm run check`: فشل في خط الأساس الأول، ثم أصبح **PASS** بعد الإصلاح.
- Vitest الحالي: **978/978 ناجح** عبر **149/149 ملفًا**.
- `secret:audit`: نجح؛ 731 ملفًا نصيًا، 0 نتائج.
- `source:audit --strict`: نجح؛ 18/18 مصدرًا fresh حتى تواريخ أكتوبر 2026.
- تدقيق المحتوى/التشابه/اللغة/الدعم التونسي: نجح ضمن نطاقه الآلي.
- Security headers: نجح؛ 15 توجيه CSP و7 رؤوس.
- Media budget: نجح؛ 520 ملف MP3+Opus.
- `js:budget`: نجح بعد Build؛ 105 Chunks و1,490,937 بايت gzip إجماليًا، وأكبر Chunk 476,929 بايت.
- فحص ZIP الإنتاجي المحدّث: سليم بنيويًا، ويطابق ملف SHA-256 الجانبي؛ يشمل تقرير التدقيق وADR-071.
- مراجعة مباشرة للمكوّنات الحرجة، خاصة دورة المفردات، الصوت، التنقل، الخطوط، Workflows، الحوكمة، وملفات الحقيقة.

مصفوفة Browser الحالية بعد الإصلاح هي 82/82 عبر Desktop وMobile؛ الاختبار الآلي لا يساوي جهازًا أو قارئ شاشة بشريًا فعليًا.

---

# 2. النتائج الحرجة — يجب إصلاحها قبل أي ادعاء Release

## AUD-C01 — Quality Gate أحمر في خط الأساس — **مغلق برمجيًا**

**الشدة:** حرجة  
**الدليل:** `npm run check` فشل في `tests/unit/deployment-smoke.test.ts`.

الاختبار ما زال يطلب النص القديم:

```text
cp deployment/deployment-smoke.yml .github/workflows/deployment-smoke.yml
```

لكن `TERMUX_REPLACE_REPO.sh` تغير إلى Loop ينسخ ثلاثة Workflows. الاختبار لم يُحدّث مع التنفيذ.

**الأثر:**

- الحالة الحالية لا تحقق معيار المشروع الإلزامي `npm run check`.
- كانت أرقام “868/868” و“Build PASS” غير صحيحة لحالة خط الأساس الأحمر.
- Build وpostbuild لم يصلا إلى التنفيذ في جولة `check` الحالية.

**الإصلاح المطلوب:** تحديث الاختبار ليفحص Loop والملفات الثلاثة، ثم إعادة `npm run check` كاملًا قبل أي ZIP أو Push.

---

## AUD-C02 — دورة `Lesen · Hören · Schreiben · Sprechen` كانت تعود إلى الصفر — **مغلق برمجيًا**

**الشدة:** حرجة وظيفيًا  
**الملف:** `src/components/four-skill-vocabulary-cycle.tsx`

التنقل الحالي:

```ts
setIndex((index + 1) % lesson.phrases.length)
```

عند آخر عبارة، مثل 14/14، تصبح النتيجة 1/14. لا توجد حالة `completed` ولا `onComplete` ولا انتقال إلى المرحلة التالية من الدرس.

**مشكلات إضافية:**

- تقدم الدورة محفوظ في `useState` فقط؛ الخروج أو إعادة التحميل يعيده إلى العبارة الأولى.
- لا يُحفظ إكمال كل عبارة في IndexedDB.
- لا يستطيع `LessonRunner` معرفة أن دورة المفردات انتهت.

**الإصلاح المطلوب:** آلة حالات `active → completed` بلا Modulo، مع `onComplete` ينقل إلى المرحلة التعليمية التالية، وسجل محلي per-lesson/per-phrase يمنع فقدان 14/14 عند إعادة التحميل.

---

## AUD-C03 — زر «قل جملة جديدة» كان لا يستمع ولا يطابق — **مغلق لمطابقة الكلمات المحلية**

**الشدة:** حرجة تربويًا  
**الملف:** `src/components/four-skill-vocabulary-cycle.tsx`

السلوك الحالي هو:

```ts
onClick={() => setSpoken(true)}
```

أي نقرة واحدة تجعل الكلام منجزًا دون:

- طلب ميكروفون.
- تسجيل صوت.
- تشغيل المحاولة للمتعلم.
- فحص الإشارة.
- Whisper محلي.
- مطابقة كلمات العبارة.
- أي دليل على أن المتعلم نطق أصلًا.

النص أسفل المكوّن يقر بأنه «تقرير ذاتي»، لكن تسمية الزر توحي بمهمة تحدث فعلية.

**الإصلاح الصحيح ضمن حدود المشروع:** تسجيل مؤقت → استماع ذاتي → فحص إشارة → Whisper محلي اختياري → مطابقة كلمات العبارة الهدف فقط. يمكن القول «ظهرت كلمات الهدف/لم تتأكد»، ولا يجوز تحويل ذلك إلى phoneme/accent/fluency score. تقييم الجملة الحرة نحويًا أو نطقيًا غير متوفر محليًا حاليًا.

---

## AUD-C04 — Workflows الـRelease كانت غير قابلة للتشغيل — **مغلق محليًا؛ التشغيل البعيد معلق**

**الشدة:** حرجة للتسليم  
**الملفات:**

- `deployment/release-candidate.yml`
- `deployment/release.yml`
- `scripts/create-release-candidate-attestation.mjs`
- `scripts/generate-release-changelog.mjs`

### Release Candidate

- `workflow_dispatch` لا يعرّف `candidate_label`.
- لا يمرر `RC_LABEL` إلى السكربت.
- سكربت Attestation يرفض Label فارغًا؛ لذلك خطوة الإنشاء ستفشل.
- `actions/upload-artifact@v4` بلا `with.path` أو `with.name`؛ الرفع غير مكتمل.

### Tagged Release

- Workflow لا يمرر `RELEASE_TAG`.
- لا يمرر `CHANGELOG_OUTPUT` أو `PREVIOUS_TAG`.
- مولد Changelog يستقبل Tag فارغًا ثم يرفضه بعبارة `Invalid semantic release tag`.

### تفعيل GitHub

المجلد `.github/workflows/` الحالي يحتوي فقط `ci.yml` و`source-freshness.yml`. ملفات Release موجودة كقوالب في `deployment/` وليست Workflows نشطة إلا إذا نسخها سكربت Termux أثناء رفع محدد.

**الأثر:** P2-312 وP2-369 مصنفان `implemented` بأكثر مما تثبته الشيفرة الحالية.

**الإصلاح:** إصلاح Inputs/Env/Artifact path، إضافة اختبارات تنفيذية للسكربتين لا String checks فقط، ثم تثبيت Workflows فعليًا تحت `.github/workflows` والتحقق من Run بعيد.

---

# 3. نتائج عالية الأولوية في UI/UX وResponsive

## AUD-H01 — نظام الخط يكبر الأرقام دون إعادة تركيب دلالية للواجهة

**الشدة:** عالية  
**الملف:** `src/app/globals.css`

يوجد 43 تقريبًا من متغيرات الحجم من `--fs-6` إلى `--fs-48`. وضع Large يصل إلى 67px، بينما كثير من التخطيطات تبقى Grid ثابتة أو `auto` أو sticky. التكبير يغير كل Label وBadge وNavigation وCard بنفس المنطق العددي بدل Semantic typography.

**لماذا الاختبارات الحالية لا تكفي:**

- اختبار Horizontal overflow لا يثبت غياب التراكب أو القص أو فقدان التسلسل البصري.
- لا توجد مصفوفة كاملة `route × 320/360/768 × compact/default/large`.
- لا توجد اختبارات Intersection/overlap أو Screenshot baselines.
- 44 سطر CSS على الأقل يستعمل `nowrap` أو `ellipsis` أو `overflow:hidden`؛ مع Large قد تختفي معلومات بدل إعادة تدفقها.

**الإصلاح:** تقليل النظام إلى Tokens دلالية (`caption/body/label/title/display`) باستخدام `rem` و`clamp`، وإضافة Container Queries وتحويل Grids حسب المساحة الفعلية بعد التكبير لا حسب عرض الشاشة وحده.

---

## AUD-H02 — دورة المهارات الأربع لم يكن لها Responsive خاص كافٍ — **مغلق في المكوّن الحالي**

**الشدة:** عالية

المكوّن يستعمل:

```css
grid-template-columns: 1fr auto
```

مع Footer مرن وزر يدفع نفسه بـ`margin-inline-start:auto`. لا توجد قواعد خاصة للمكوّن عند Large text/320px لتحويل كل Action إلى صف كامل، ما يفسر تجربة الازدحام رغم عدم وجود Document overflow.

**الإصلاح:** عند Container ضيق: عمود واحد، أزرار بعرض 100%، تعليمات الكتابة في Block مستقل، Progress steps عمودية، ومنع أي Ellipsis في المحتوى التعليمي.

---

## AUD-H03 — سياق الصفحة كان يختفي على الهاتف — **مغلق برمجيًا**

**الشدة:** عالية  
**الملف:** `src/app/globals.css`

عند الهاتف:

```css
.topbar-copy { display:none; }
```

وبذلك يختفي عنوان السياق الذي أضيف أصلًا لتعريف مكان المتعلم. كذلك `/practice` ليس ضمن `mobilePrimary`، وزر «المزيد» يُعد Active فقط لمسارات `resourceNav`؛ لذلك صفحات التدريب قد لا تملك Destination نشطة في الشريط السفلي.

**الإصلاح:** إبقاء Context مختصر قابل للالتفاف، وجعل «المزيد» Active لكل وجهة غير موجودة ضمن الأربع الأساسية، ومنها Practice وتفرعاتها.

---

## AUD-H04 — IDs تقنية كانت ظاهرة للمتعلم — **مغلقة للأمثلة المستخرجة**

**الشدة:** متوسطة إلى عالية

أمثلة:

- `writing-lab.tsx`: يعرض `lesson.id`.
- `writing-portfolio.tsx`: يعرض `taskId` خامًا.
- `writing-repair-practice.tsx`: يعرض `patternId`.
- `unified-concept-map.tsx`: روابط باسم `A1-01`.
- Profile switcher يعرض قيم Provider الداخلية مثل `goethe-b2`.

**الإصلاح:** Registry لتسميات بشرية، مع إبقاء IDs في `data-*` وLogs فقط.

---

# 4. نتائج تربوية ووظيفية

## AUD-H05 — تحقق الكتابة في دورة المفردات كان ضعيفًا — **مغلق بغاية متكيفة وحدود كلمات**

الحالة الحالية:

```ts
written.toLowerCase().includes(phrase.de.toLowerCase())
```

المتعلم يستطيع كتابة العبارة وحدها، أو إدخال نص يحتويها داخل كلمة أطول، ويحصل على Check. كما أن Placeholder يكشف العبارة نفسها.

**الإصلاح:** Token/phrase-boundary matching، حد أدنى لكلمات إضافية، منع مطابقة Placeholder وحده، وإظهار سبب الرفض.

---

## AUD-H06 — الاستماع كان يُحتسب عند الضغط — **مغلق بحدث بدء التشغيل**

يتم `setHeard(true)` فور استدعاء `onSpeak`. إذا لم يدعم المتصفح TTS أو فشل الصوت، تبقى المهمة منجزة.

**الإصلاح:** Callback `onStart/onEnd/onError` أو مشغل Audio موثوق؛ لا يثبت الاستماع إلا بعد `play` ناجح أو اختيار صريح «الصوت غير متاح» دون اعتباره دليلًا.

---

## AUD-H07 — المحتوى الجديد كان خارج الحوكمة المركزية — **مغلق برمجيًا**

في خط الأساس، لم تكن البنوك التالية تدخل تقارير Academic/Similarity/Review المركزية؛ أصبحت الآن جزءًا من 3,020 سجلًا محكومًا:

- 16 مهمة Dictation.
- 8 سيناريوهات Branching conversation.
- 16 شبكة Collocation و48 وصلة.

البحث في Scripts/Content validation لم يجد إدراجها؛ الموجود هو اختبارات وحدات وHandoff strings فقط.

**الأثر:** لا توجد عليها حالات German/Arabic/CEFR/Copyright المنفصلة نفسها، ولا مقارنة تشابه مركزية.

**الإصلاح:** إضافة Schema families وسجلات lifecycle لهذه البنوك، وإدراج Prompts/Examples في similarity وlanguage/content governance.

---

## AUD-H08 — الكلام العملي الآخر يعتمد أحيانًا تقريرًا ذاتيًا ثابتًا

`information-gap-lab.tsx` ينشئ محاولة بـ:

```text
selfScore: 3
reflection: partner confirmed
```

بعد تأكيد من الواجهة، دون تقييم لغة. هذا مقبول كتأكيد عملية، لكنه لا يجب أن يظهر في أي Metric كجودة تحدث.

---

# 5. انجراف وثائق الحقيقة

## AUD-H09 — تناقضات رقمية كانت تمر رغم Handoff — **مغلقة للأعداد المكتشفة**

أمثلة مؤكدة:

- `P1_AUDIT.md`: 132 صفًا، لكن `PROJECT_STATUS.md` وPrompt يقولان 136/136.
- Prompt عند السطر التقريبي 762 يقول P2 = 117/2/21، بينما `P2_AUDIT.md` = 118/2/20.
- `PROJECT_STATUS.md` ما زال يذكر 303 Offline routes في وصف Handoff، بينما Manifest = 306.
- `offline-pack-control.tsx` يقول للمستخدم «303 مسارات»، بينما Manifest = 306.
- `P0_AUDIT.md` يسمي 819/819 و128/128 و70/70 «خط الأساس الحالي»، بينما الوثائق الأحدث تقول 868/136/76.
- مقدمة P0/P1 ما زالت تشير إلى Sprint P2 قديم `104/2/34/0`.
- نهاية `P1_AUDIT.md` تحذر من 15 بندًا جزئيًا، بينما الجدول يملك 4 فقط.
- P0 يذكر أعداد Offline تاريخية 51/199/298 وعمليات Browser 66/66 بصياغة يمكن فهمها كحالية.

**النتيجة:** `handoff:check` ينجح رغم تناقضات صريحة؛ تغطيته للوثائق انتقائية.

**الإصلاح:** مصدر JSON واحد للأعداد، توليد README/Status/Handoff sections، ومنع الأرقام اليدوية خارج قسم Historical واضح.

---

# 6. النشر، Offline، والأمان

## AUD-M01 — `curriculumVersion` كان مكررًا يدويًا في Service Worker — **مغلق عبر Materialization**

يوجد ثابت مستقل في `public/sw.js` بالإضافة إلى `src/config/curriculum-version.ts`. لا توجد عملية توليد تضمن تطابقهما دائمًا.

**الإصلاح:** Materialize الثابت أثناء Build أو تضمينه في Manifest يقرأه Worker.

## AUD-M02 — الحزمة القديمة بلا نسخة منهج كانت تصنف Build update — **مغلق Fail-safe**

إذا كانت Metadata المثبتة قديمة ولا تحتوي `curriculumVersion`، فإن `versionChanged` يصبح false. الواجهة تعرض «غير مسجل → الحالي» لكن قد تصنف الحالة Build update فقط.

**الإصلاح:** حالة مستقلة `unknown-installed-curriculum` تمنع طمأنة غير مبررة.

## AUD-M03 — اختبارات كثيرة تتحقق من Strings لا من السلوك

- 52/136 ملف Unit يستعمل `readFileSync` لقراءة المصدر.
- 99/136 يستعمل `toContain` في موضع ما.
- 14 فقط تستعمل Render مباشر، وملف واحد فقط يستورد `fake-indexeddb` صراحة.

هذا ليس خطأ بذاته، لكنه سمح بتصنيف Workflows مكسورة كمنجزة لأن الاختبارات تحققت من وجود كلمات لا من تمرير Inputs/Env أو إنتاج Artifact.

**الإصلاح:** YAML schema validation، تشغيل scripts في Temp Git repo، واختبارات UI behavior بدل Source-string كلما أمكن.

## AUD-M04 — صيانة الشيفرة صعبة بسبب الأسطر العملاقة

116 ملفًا في `src` ما زال يحتوي سطرًا بطول 1,000 حرف أو أكثر بعد تنسيق بعض المكونات. أمثلة:

- `globals.css`: سطر 12,219 حرفًا.
- `merge.ts`: سطر 4,858 حرفًا.
- `live-capability-probe.ts`: 4,561 حرفًا في سطر واحد.
- `collocation-network-lab.tsx`: سطر يصل 3,857 حرفًا.

**الأثر:** مراجعة ضعيفة، Merge conflicts، صعوبة تحديد أخطاء JSX/CSS، وتحفيز اختبارات String هشة.

**الإصلاح:** Prettier/format gate، تقسيم المكونات، وتقسيم `mergeLearningStates` إلى Domain mergers مستقلة.

## نقاط قوة أمنية مؤكدة

- `npm audit`: 0 ثغرات حاليًا.
- Secret scan الحالي: 0 نتائج.
- CSP بلا wildcard عام وفق التدقيق.
- مفتاح AI في `sessionStorage`، وOllama Loopback، وموافقة منفصلة للشبكة.
- ZIP لا يحتوي أوزان Whisper.

هذه نقاط جيدة، لكنها لا تلغي فشل Quality Gate أو نقص الاختبار البعيد.

---

# 7. نقص المحتوى والمراجعة البشرية

## الصوت

كل الأصول الحالية:

```text
261 MP3 + 260 Opus
Human recorded: 0
Exam grade: 0
Speaker count: 1
Rights: generated-for-project-review-required
```

لا توجد محادثات متعددة المتحدثين حقيقية، ولا مراجعة بشرية للنطق/الأرقام/الأسماء/السرعات.

## الألمانية وCEFR والحقوق

- 3,020 عنصرًا داخل الحوكمة المركزية: كلها automated-pass-human-pending، و0 independently reviewed.
- لا Corpus حقوقي مخول لمقارنة Menschen/Hueber/المصادر الرسمية.
- اقتباسات دليل القراءة آلية وتحتاج مراجعة بشرية دلالية.
- 1,044 سجل اسم و104 Verb frames وستة Exclusions تحتاج مراجعة ألمانية مستقلة.

## العربية والتونسية

- 17 ملاحظة تونسية كلها `authored-review-pending`.
- لا مراجعة مستقلة للفصحى/التونسية وتمثيل بقية العالم العربي.

## النطق

الموجود: Signal check + Whisper German STT + Expected-word matching.  
غير الموجود: Phoneme alignment، اكتشاف `ch/ü/u` علميًا من التسجيل، Accent score، Fluency score، أو تقييم رسمي.

## الكتابة والوساطة

المصحح المحلي محدود بأنماط حتمية ضيقة؛ لا يكتشف كل خطأ نحوي أو طبيعي. Gemini مراجعة اختيارية عند الشك، وليست مصححًا كاملًا أو تقييمًا بشريًا.

## الامتحانات

Goethe/telc منفصلان بنيويًا، لكن الصوت اصطناعي والنتائج داخلية، ولا توجد محاكاة معتمدة أو تقييم بشري للإنتاج.

---

# 8. البنود الرسمية غير المكتملة

## P0 الجزئي — 9

```text
98, 99, 124, 135, 255, 301, 302, 373, 376
```

تشمل المراجعة الألمانية والدلالية والصوتية، أجهزة الوصول، PR Preview، CI البعيد، والمراجعة العربية/التونسية.

## P1 الجزئي — 4

```text
92, 331, 377, 380
```

حقوق المحتوى، Assistive Technology فعلي، مراجعة قانونية/قنصلية، وفونيتك عربية مستقلة.

## P2 الجزئي — 2

```text
264, 384
```

WCAG يدوي حقيقي ومراجعة عربية مستقلة.

## P2 غير المنجز — 20

```text
22, 24, 93, 96, 119, 156, 165, 168, 215, 216,
225, 276, 349, 350, 352, 355, 357, 359, 360, 371
```

الأهم للمتعلم الآن: P2-156 قابلية الفهم بمهمة حقيقية، P2-168 نقل مؤجل، P2-352 مقارنة موجة صادقة، وP2-355 مولد اختبارات محلي موثوق.

---

# 9. ترتيب الإصلاح المقترح

## الدفعة 0 — استعادة الصدق الأخضر

1. إصلاح `deployment-smoke.test.ts` ثم جعل `npm run check` أخضر.
2. إصلاح Workflows RC/Release أو إعادة حالتي P2-312/369 إلى غير منجز حتى تعمل فعليًا.
3. مزامنة 132 P1 و118 P2 و306 Offline وكل أعداد الاختبار.
4. توسيع `handoff:check` لرفض كل الأرقام المتناقضة.

## الدفعة 1 — شكوى المتعلم الحالية

1. إعادة بناء FourSkill كآلة حالات نهائية؛ 14/14 ينتقل للمرحلة التالية ولا يعود 1/14.
2. حفظ تقدم العبارات محليًا.
3. استبدال زر الكلام الوهمي بتسجيل فعلي واستماع وفحص إشارة ومطابقة كلمات محلية.
4. جعل الكتابة تتطلب جملة جديدة فعلًا، لا Substring.
5. عدم احتساب الاستماع عند فشل التشغيل.

## الدفعة 2 — Responsive/Typography

1. Semantic type scale بدل 43 رقمًا متحركًا.
2. Container queries للمكونات الكثيفة.
3. اختبار كل الصفحات الأساسية × الأحجام الثلاثة × 320/360/768.
4. كشف overlap/clipping/ellipsis لا Horizontal overflow فقط.
5. إبقاء سياق الصفحة وActive destination ظاهرين على الهاتف.

## الدفعة 3 — حوكمة المحتوى الجديد

إدخال Dictation/Branching/Collocations في Zod academic roots وSimilarity وGerman/Arabic/CEFR/Copyright lifecycle.

## الدفعة 4 — المراجعة البشرية النهائية

ألمانية، عربية/تونسية، فونيتك، حقوق، صوت، أجهزة وقارئات شاشة، ثم GitHub CI/PR Preview/Vercel.

---

# 10. قرار الجاهزية

| المجال | الحكم |
|---|---|
| اتساع المنهج | قوي كميًا |
| اختبارات البنية | واسعة لكن جزءًا معتبرًا منها String-based |
| Quality Gate المحلي الحالي | **PASS** |
| دورة المفردات الأربع | Terminal + حفظ IndexedDB + انتقال تلقائي |
| الكلام داخل الدورة | ميكروفون لحظي + Signal + مطابقة كلمات محلية مرتبة؛ بلا ادعاء فونيمي |
| Responsive مع تكبير الخط | Reflow وContainer Query ومصفوفة نوعية آلية؛ المراجعة البشرية النهائية معلقة |
| المحتوى الجديد | داخل Zod وLifecycle وSimilarity وReview المركزي |
| الصوت | اصطناعي أحادي وغير امتحاني |
| المراجعة البشرية | غير منجزة |
| النشر البعيد | قديم/غير أخضر حسب السجل |
| الجاهزية كنسخة دراسة تجريبية محلية | ممكنة بعد إصلاح الأعطال الحرجة |
| الجاهزية كإصدار نهائي موثوق | **لا** |

**الخلاصة الحالية:** أُغلق الفرق البرمجي الحرج بين واجهة دورة المهارات وسلوكها، وأصبحت البنوك الجديدة داخل الحوكمة ونجحت البوابة المحلية. لا تزال المراجعات البشرية والفونيمية والحقوقية وCI/Preview البعيدة حدودًا معلقة يجب ألا تُعرض كمنجزة.
