# برومبت متابعة مهني — WegBerlin (`der-weg-nach-berlin`)

**تاريخ التحرير:** 2026-09-07 · **الفرع:** `arena/01a069c4-wegberlin` · **الطلب:** PR #1 على `naderba69/wegberlin`

> أعطِ هذا الملف لأي وكيل/مساعد ليُكمل العمل **بنفس القيود ونفس المميزات**.
> كل رقم فيه **مقيس** في جلسات 2026-09-05 → 2026-09-07، لا مقدَّر ولا مفترض.
> عند الشك، المصدر هو المستودع نفسه: `P0_AUDIT.md` و`PROJECT_STATUS.md` و`PROFESSIONAL_CONTINUATION_PROMPT_AR.md` و`docs/adr/*`.

---

## 1. هوية المشروع (ثوابت لا تتغيّر بالدفعات)

- تطبيق ويب لتعلّم الألمانية **A1 → B2** فقط، **محلي أولًا**: IndexedDB مصدر الحقيقة، و`.dwnb` للتصدير/الاستيراد، وCache Storage للحزمة غير المتصلة.
- **ميزانية 0 USD صلبة**: لا خدمة مدفوعة ولا نموذج مدفوع. الذكاء اختياري عبر OpenRouter بنماذج `openrouter/free` أو `:free` فقط، والمفتاح في `sessionStorage["dwnb-ai-key"]` وحده بموافقة صريحة لكل إرسال، ولا يُحفظ في التصدير.
- **لا محتوى منقول** من Menschen / Hueber / Goethe / telc، ولا ادّعاء انتماء أو اعتماد رسمي.
- **فصل صارم** بين `goethe-b2` و`telc-deutsch-b2`؛ والبوابات: `A1 → assessment/a1 → A2 → … → B2 → assessment/b2`.
- **الصوت**: ملفات MP3 مولّدة للمشروع، معلنة صراحة بأنها **غير امتحانية**، مع SHA-256 وملف حقوق في الواجهة.
- **التوجيه قبل القياس** في `/today`؛ ولا تقييم آلي للنطق أو الطلاقة أو الكتابة.

### ادّعاءات ممنوعة (لا تكتبها في كود ولا وثيقة ولا تقرير)
ضمان الوصول إلى B2 · صوت بجودة امتحان · شريك محادثة حي · قياس آلي للنطق أو الطلاقة · تصحيح كتابة بشري · قفل متصفح (browser lockdown) · نشر فعلي على GitHub/Vercel بصفة رسمية · موافقة جهة رسمية.

---

## 2. الحالة الراهنة (مقيسة)

| المقياس | القيمة | كيف تتحقق |
|---|---|---|
| اختبارات الوحدة/التكامل | **365/365** (52 ملفًا) | `npx vitest run` |
| متصفح Production (Chromium 149) | **37/37** سطح مكتب + **37/37** Pixel 7 | مصفوفة Playwright (بند 7) |
| البوابة الكاملة | `npm run check` أخضر | انظر تركيبها في بند 6 |
| الحزمة غير المتصلة | `v64` — `dwnb-full-pack-v64` و`dwnb-level-pack-{a1,a2,b1,b2}-v64` | `public/sw.js` + العقود |
| المسارات غير المتصلة | 298 مسارًا (full)، و51/51/51/199 لكل مستوى | `public/offline-size-manifest.json` |
| P0 في `P0_AUDIT.md` | **110 مُنفَّذ · 11 جزئي · 2 غير مُنفَّذ · 1 محجوب** | `node scripts/verify-continuation-handoff.mjs` |
| CI على GitHub | `check` و`e2e` وVercel كلها خضراء | `gh pr checks 1` |

---

## 3. ⚠️ فخ مكلّف قبل أي شيء: المستودع المحلي يُعاد نسخه

أُعيد نسخ المستودع وفقد **26 التزامًا** مرتين. القاعدة الملزمة:

```bash
git fetch origin arena/01a069c4-wegberlin
echo "محلي: $(git rev-list --count HEAD) · بعيد: $(git rev-list --count FETCH_HEAD)"
```

- إن كان المحلي **أقل**: البعيد هو المصدر. الاسترجاع الآمن (بلا force-push أبدًا):
  ```bash
  cp -a <ملفات عملك الجديد> /tmp/wt-backup/      # احتفظ بعملك أولًا
  git reset --hard FETCH_HEAD                     # استعد كل المحتوى المرفوع
  git clean -fd                                   # احذف البقايا غير المتتبَّعة (بدون -x)
  # ثم أعد تطبيق عملك فوق الشجرة المستعادة، وتأكد أن git diff --stat صغير وإضافي
  ```
- **لا `git push --force` ولا إعادة كتابة تاريخ** في أي حال.
- قبل أول التزام: قارن العددين أعلاه، وإلا قد تلتزم فوق قاعدة قديمة وتمحو عملًا مرفوعًا.

---

## 4. ما أُنجز (سجل الدفعات)

| البند | ما نُفِّذ فعلًا | الدليل المقيس | الحالة |
|---|---|---|---|
| **P0-26** | عينة إنتاج (كتابية + شفهية) بعد التشخيص، الحفظ **بالاكتمال لا بالجودة**، ومخطط Zod يرفض أي درجة | درجة التشخيص لا تتغيّر قبلها وبعدها (اختبار متصفح) | مُنفَّذ |
| **P0-38** | إحماء استرجاع 2–4 دقائق بدل حذف كتلة المراجعة حين لا توجد بطاقة SM-2، مع استبعاد أي عنصر يطابق بطاقة مجدولة | اختبارات وحدة + متصفح | مُنفَّذ |
| **P0-266** | يوم سماح أسبوعي (`GRACE_DAYS_PER_WEEK = 1`)، أرقام مفصولة، **والسلسلة لا تُجمَّد** | اختبارات وحدة + متصفح | مُنفَّذ |
| **P0-256** | إعلان ذري واحد قصير لكل نتيجة مختبر/امتحان | اختبارات وحدة | مُنفَّذ |
| **P0-254** | محرك عزل اتجاهي + **بوابة تدقيق DOM** على 22 مسارًا | `tests/e2e/bidi-audit.ts`، 35/35 | **مُغلق** |
| **P0-267** | قاموس مدح سلوكي (7 أسطح) يسمي الفعل المقيس، ومنع المدح العام باختبار يمسح الشجرة | `GENERIC_PRAISE_RE` (19 عبارة) | **مُغلق** |
| **P0-373 + 376** | 12 شرحًا تونسيًا منهجيًا (فصحى/تونسي/لماذا يختلف الفهم) تُعرض داخل بطاقة القاعدة عند اختيار الدعم التونسي | تغطية **72/84** درسًا بـ**125** موضعًا؛ 3 اختبارات وحدة + 1 متصفح | جزئي (مراجعة لغوية) |
| **P0-304** | فحص أسرار آلي: 9 قواعد، أنماط `--staged`/`--tree`/`--history`، خطاف `pre-commit` بلا تبعية، و`npm run check` يُشغّله | شجرة 347 ملفًا + تاريخ 9,086 ملفًا/29 التزامًا؛ 7 اختبارات وحدة؛ **مفتاح مزروع رُفض عند الالتزام** | جزئي (مراجعة الاستثناءات + إثبات التاريخ في CI) |
| **P0-135** | سرعة استماع واحدة مشتركة (0.75×/1×/1.15×) بين كل المشغلات، تُطبَّق على بديل المتصفح أيضًا | 5 اختبارات وحدة + اختبار متصفح يثبت الثبات بعد إعادة التحميل | جزئي (تحقق بشري بالأذن) |

**سجل القرارات:** `ADR-017` (العزل الاتجاهي) · `ADR-018` (المدح السلوكي) · `ADR-019` (الدعم التونسي) · `ADR-020` (فحص الأسرار) · `ADR-021` (سرعة الاستماع) — كلها في `docs/adr/`.

---

## 5. ما بقي (11 بندًا جزئيًا) — بالناقص المحدّد

| البند | الناقص بالضبط | لماذا لا يُغلق آليًا هنا |
|---|---|---|
| **98** | مراجعة ألمانية بشرية لصيغ Genitiv والمجرور والأجناس والجموع + تدقيق المفردات الظاهرة في الجمل فقط | لا معجم ألماني دون اتصال؛ الجرد بلا مُعلّم صرفي |
| **99** | مراجعة ألمانية لكل صيغة وإطار + توسيع قاموس التكافؤ لأفعال جرّية أخرى | لا معجم ألماني دون اتصال؛ الجرد بلا مُعلّم صرفي (نفس سبب 98) |
| **112** | تدقيق منهجي يثبت أن **كل** تدريبات الحالات تبدأ بالدور الدلالي قبل النهايات | قابل للتنفيذ جزئيًا: تدقيق + إصلاح، والحكم النهائي تربوي |
| **124** | مراجعة بشرية لـ**504 مواضع دليل** مؤلفة (قراءة + استماع) وتعليلاتها العربية، وفحص المواضع المستنتَجة (23+27) | مراجعة مدرّس لا استنتاج آلي |
| **135** | **تحقق بشري بالأذن** من عدم التشويه عند 0.75× و1.15× على كل الملفات (84 درسًا + المكتبة + مقاطع الامتحان) | لا قياس تشويه آلي في المشروع |
| **159** | معلومات ناقصة ثنائية الطرف تحتاج شريكًا حيًا أو وكيلًا اختياريًا | لا يجوز ادّعاء التفاعل الحالي |
| **255** | جولة يدوية على كل عنصر تحكم مع VoiceOver/NVDA/Switch Control ومتصفحات غير Chromium | أجهزة وقارئات شاشة خارج مساحة العمل |
| **302** | تشغيل مصفوفة الهاتف **داخل CI** | **يحتاج المالك**: رفض GitHub تعديل Workflow بصلاحية `workflows`. التغيير جاهز في `docs/pending/ci-mobile-matrix.patch` |
| **304** | مراجعة ملف الاستثناءات + إثبات فحص التاريخ الكامل في CI (نسخة ضحلة `fetch-depth: 1`) | لا Workflow جديد ممكن (صلاحية) + قرار بشري للاستثناءات |
| **373 / 376** | مراجعة لغوية تونسية لكل شرح + تغطية الـ12 درسًا الباقية | لغوي تونسي بشري |

> قاعدة: **لا تُبدّل تصنيف أي بند** إلا بعد (1) تنفيذ فعلي، (2) اختبار مقيس، (3) تحديث `P0_AUDIT.md` **وقائمة المعرّفات المُنفَّذة** **وعدّادات `scripts/verify-continuation-handoff.mjs`** معًا. سابقة الإغفال معروفة (P0-26).

---

## 6. قواعد العمل الملزمة (عقود)

1. **رفع الحزمة غير المتصلة:** بعد أي تغيير في أصول التطبيق (مكوّن، CSS، أصل عام) ارفع `PACK_VERSION` و`PACK_STAGING_CACHE` **وكل أسماء الحزم الخمسة** معًا، وحدّث كل العقود: `tests/e2e/critical-flows.spec.ts`، `tests/unit/offline-pack-controls.test.ts`، `scripts/verify-continuation-handoff.mjs`، `PROJECT_STATUS.md`، `PROFESSIONAL_CONTINUATION_PROMPT_AR.md`. **احذر:** استبدال `v64` نصيًا يصيب `package-lock.json` (تجزئات base64) — استثنِه دائمًا.
2. **`npm run check` قبل أي ادّعاء** = lint + typecheck + `vitest run` + `secret:scan` (شجرة + تاريخ) + `source:audit --strict` + `handoff:check` + `build`.
3. **مصفوفة المتصفح فعلية** قبل أي ادّعاء يمسّ E2E أو IndexedDB أو الأوفلاين أو عقد المتصفح — لا تُختصر بالسطح المكتبي وحده.
4. لا تُدخل `playwright.sandbox.config.mjs` في المستودع (يُحذف بعد كل تشغيل).
5. **تعليقات `//` في الأسطر المفردة المضغوطة تُبلع ما بعدها** (أفسدت ملفًا فعلًا) — استخدم `/* … */` داخلها.
6. أي اختبار جديد يغيّر الأرقام الموثّقة: حدّث `PROJECT_STATUS.md` و`README.md` و`scripts/verify-continuation-handoff.mjs` في **نفس الدفعة**، ولا ترفع رقم المتصفح وحده.
7. **أرقام مقيسة فقط** في الوثائق والتقارير؛ واذكر دائمًا «ما لا يُدعى».

---

## 7. بيئة مساحة العمل (تُهيَّأ من الصفر كل جلسة)

```bash
# 1) التبعيات (node_modules تُمسح بين الجلسات)
npm ci

# 2) Chromium 149 (لا CDN لـPlaywright: registry.npmjs.org وحده يعمل)
mkdir -p /tmp/chromium-dl && cd /tmp/chromium-dl
npm pack @sparticuz/chromium@149.0.0 && tar xzf sparticuz-chromium-149.0.0.tgz
mkdir -p /tmp/chromium /tmp/al2023
node -e "const fs=require('fs'),z=require('zlib');fs.writeFileSync('/tmp/chromium/chromium',z.brotliDecompressSync(fs.readFileSync('/tmp/chromium-dl/package/bin/chromium.br')));fs.writeFileSync('/tmp/al2023.tar',z.brotliDecompressSync(fs.readFileSync('/tmp/chromium-dl/package/bin/al2023.tar.br')));"
chmod +x /tmp/chromium/chromium && cd /tmp/al2023 && tar xf /tmp/al2023.tar
# ⚠️ مسار المكتبات الصحيح: LD_LIBRARY_PATH=/tmp/al2023/lib   (وليس /tmp/al2023/lib/lib)
export LD_LIBRARY_PATH=/tmp/al2023/lib && /tmp/chromium/chromium --version   # Chromium 149.0.7827.0

# 3) ملف تشغيل مؤقت (يُحذف بعد التشغيل)
cat > playwright.sandbox.config.mjs <<'EOF'
import base from "./playwright.config.ts";
export default { ...base, use: { ...base.use, launchOptions: { executablePath: "/tmp/chromium/chromium",
  args: ["--no-sandbox","--disable-dev-shm-usage","--disable-gpu","--use-fake-ui-for-media-stream","--use-fake-device-for-media-stream"] } } };
EOF
npm run build
npx playwright test --config=playwright.sandbox.config.mjs --project=chromium          # 37/37
npx playwright test --config=playwright.sandbox.config.mjs --project=mobile-chromium   # 37/37
rm -f playwright.sandbox.config.mjs
```

**الشبكة:** `registry.npmjs.org` يعمل؛ `cdn.playwright.dev` و`storage.googleapis.com` و`deb.debian.org` ونتائج GitHub (blob) **لا تعمل** → لا تستطيع قراءة سجلات CI من مساحة العمل، فلا تدّعِ ما لم تره.

**أوامر أخرى:** `npm run dev` يستخدم webpack (`next dev --webpack`)؛ أوقف المعاينة قبل بناء ثقيل؛ المعاينة تُربط على `0.0.0.0`؛ وروابط نسبية فقط من كود المتصفح.

---

## 8. أولى الخطوات التالية (مرتّبة)

1. **P0-302** — طبّق `docs/pending/ci-mobile-matrix.patch` (سطران: السقف 30 دقيقة + `npm run test:e2e:mobile`) — **يحتاج المالك** (صلاحية `workflows`).
2. **P0-373/376** — مراجعة لغوية تونسية للشروح الـ12، ثم توسيع التغطية إلى الـ12 درسًا الباقية.
3. **P0-135** — تحقق بشري بالأذن من عدم التشويه على السرعتين غير الأصليتين.
4. ثم **112** (تدقيق الحالات دلاليًا قبل النهايات) و**124** (مراجعة مواضع الأدلة) — أول بنتين قابلتين للتنفيذ البرمجي بعد الثلاث الأولى.

---

## 9. النسخة الاحتياطية (خارج المستودع)

- `/home/user/wegberlin-full.zip` + بصمته `/home/user/wegberlin-full.zip.sha256`.
- إعادة البناء بعد أي دفعة:
  ```bash
  cd /home/user/wegberlin
  rm -f /home/user/wegberlin-full.zip
  zip -r -q /home/user/wegberlin-full.zip . -x ".git/*" "node_modules/*" ".next/*" "test-results/*" "playwright-report/*" "coverage/*" "*.zip" "*.dwnb" "debug-*.png"
  cd /home/user && sha256sum wegberlin-full.zip > wegberlin-full.zip.sha256 && sha256sum -c wegberlin-full.zip.sha256
  ```
- استعادة: فكّ الضغط ثم اتبع بند 3 (تحقّق من التاريخ قبل أي التزام).
