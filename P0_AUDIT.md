# P0 Implementation Audit

Last audited: 2026-09-11

هذا التدقيق يطابق عناصر P0 الـ124 الواردة في `IDEA_BACKLOG.md` مع ما يوجد فعليًا في الشيفرة والاختبارات. كلمة «منجز» تعني أن مسارًا قابلًا للاستخدام ومعيار قبول آليًا موجودان؛ لا تعني مراجعة بشرية أكاديمية أو صوتية أو اعتمادًا رسميًا. تمت مزامنته بعد Sprint P2 (104/2/34/0) دون تغيير حدود P0 البشرية أو البعيدة.

## الملخص الدقيق

| الحالة | العدد | المعنى |
|---|---:|---|
| منجز في المنتج | 115 | يعمل فعليًا وله دليل شيفرة/اختبار مناسب لحجمه |
| منجز جزئيًا | 9 | توجد بنية مفيدة، لكن جزءًا من معيار الفكرة ما زال ناقصًا |
| غير منجز | 0 | اكتملت كل دورات P0 القابلة للبناء آليًا؛ بقيت البنود الجزئية البشرية/البعيدة |
| متوقف على صلاحية المستخدم | 0 | لا يوجد بند متوقف بالكامل؛ اختبار PR Preview ما زال يحتاج إجراءً على حساب المستخدم |
| **المجموع** | **124** | جميع عناصر P0 مصنفة، بلا إسقاط |

خط الأساس الآلي المشترك بعد دفعة 2026-09-11 هو 819/819 اختبار وحدة/تكامل عبر 128/128 ملفًا و70/70 اختبار متصفح Desktop/Mobile؛ هذا لا يغيّر البنود البشرية التسعة ولا يساوي اختبار جهاز أو قارئ شاشة فعليًا.

## P0 المنجز جزئيًا — ما ينقص تحديدًا

| ID | الموجود الآن | الجزء المتبقي قبل الإغلاق |
|---:|---|---|
| 98 | اكتمل الجرد البنيوي: 1,044 مرساة اسم، و3,219 إشارة = 1,106 covered / 0 pending-human / 2,113 context-only عبر A1–B2. تستعمل الصيغ المصرفة Lemmas أساس ولا تُنشئ جمعًا مزيفًا | مراجعة ألمانية مستقلة للسجلات الـ1,044 وقرارات الاستبعاد السياقية قبل تحويل البند إلى منجز؛ الصفر الآلي ليس اعتمادًا لغويًا |
| 99 | توجد 104 إطارات فعل/حرف جر، وصار طابور المرشحين غير المصنفين صفرًا. ستة False positives لها استبعادات إصدارية معللة: موضع مكاني، `vorliegen/ausreichen` كأفعال منفصلة، `bei Bedarf` ظرف شرط، و`um … zu` غاية | مراجعة ألمانية مستقلة للاستبعادات الستة وتحويل `authored-review-pending` إلى دليل مراجعة مسمى؛ ثم مراجعة جودة الإطارات الـ104 قبل الإغلاق النهائي |
| 124 | كل جواب قراءة يعرض الآن بعد الالتزام جملة حرفية من النص تختارها مطابقة كلمات السؤال والخيار الصحيح | تحويل الاقتباسات الآلية إلى مواضع دليل مؤلفة/مراجعة بشريًا لكل سؤال حتى لا يختار التطابق اللفظي جملة صحيحة شكليًا وضعيفة دلاليًا |
| 135 | `learning-playback-speed-v1` يوفّر 0.75×/1×/1.15× في جميع أسطح الاستماع المؤلف: التهيئة/التشخيص، الدرس، المكتبة، التدريب الامتحاني الموجّه، وShadowing. يطلب `preservesPitch=true`، ويطابق TTS، ويثبت البروفة الزمنية المتصلة على 1× | جولة استماع بشرية على أجهزة ومتصفحات وأصوات TTS ممثلة للتحقق من التشويه والوضوح والأرقام والأسماء؛ طلب الحفاظ على النبرة لا يساوي دليلًا صوتيًا |
| 255 | Skip link، Focus visible، مراحل الدرس، والحوارات مختبرة بلوحة المفاتيح؛ ومسار WebGPU يملك Capability/Mock آليًا | جولة يدوية كاملة لكل عنصر تحكم مع VoiceOver/NVDA/Switch Control ومتصفحات غير Chromium، وتثبيت نموذجي MiniLM 118 MB وWhisper المحلي 70–90 MB فعليًا على أجهزة GPU ممثلة وقياس الذاكرة/الحرارة/السرعة والانهيار |
| 301 | `main` العام محدث عند `be56463e`، Vercel أعاد Deployment success، و`https://wegberlin.vercel.app/today` يعمل | إنشاء Pull Request فعلي والتحقق من Preview URL ثم إغلاقه |
| 302 | Workflow المحلي المحدّث يشغّل مشروعَي Playwright كاملين Desktop+Mobile مع Retry واحد فقط في CI، وActions v5؛ المجموعة المحلية 66/66 | دفع التعديل والحصول على GitHub Actions run أخضر؛ التشغيل السابق `be56463e` فشل في e2e المكتبي رغم نجاح Build و`npm run check` |
| 373 | صار خيار `tunisian-supported` يغيّر مرحلة القاعدة فعليًا: 17 ملاحظة اختيارية في 17 درسًا، موزعة A1/A2/B1/B2 = 6/5/4/2. كل ملاحظة تفصل جسر الفصحى عن التقريب التونسي والمرساة الألمانية، ولا تظهر في وضعي الفصحى أو العربي المحدود | مراجعة تونسية/فصحى مستقلة لكل الملاحظات الـ17 وتسجيل اسم المراجع وتاريخه؛ كلها الآن `authored-review-pending` بوضوح، لذلك لا يُغلق البند آليًا |
| 376 | يغطي `tunisian-support-v1` خمسة عشر نوع فرق مؤثر، منها ترتيب السؤال وV2 والحالة وPerfekt والروابط والنفي المودالي والموصول والمبني للمجهول ونسبة الخبر والنسب المئوية. كل سجل يشرح أثر الفرق على الفهم بدل ترجمة كلمة بكلمة | التحقق اللغوي المستقل من دقة الفروق ومن تمثيل التنوع داخل تونس، وتعديل أو حذف أي تعميم قبل تحويل الحالة إلى `independently-reviewed` |

## P0 غير المنجز

لا يوجد بند P0 بلا دورة استخدام الآن. بقيت تسعة بنود جزئية لا تُغلق إلا بدليل بشري مستقل أو تشغيل GitHub/Vercel بعيد فعلي.

أُغلق البندان `194` و`364` في 2026-09-03 عبر سجل مركزي توسع الآن إلى 18 مصدرًا رسميًا بمهلة 30 يومًا، تنبيه Runtime لصيغة الامتحان، حارس 0 USD يمنع AI البعيد قبل `fetch` عند تقادم المصدر أو غموض الموديل، وفحص Build صارم. يشغّل Workflow شهري فحص التاريخ والوصول ويفتح/يحدّث Issue للمراجعة البشرية. نجاح HTTP لا يُعد تحققًا دلاليًا.

أُغلقت البنود `289` و`292` و`387` في 2026-09-03 عبر 13 عائلة Zod صارمة تتحقق من 3,710 كائنًا أكاديميًا علويًا وكل بنيتها المتداخلة، وسجل آلي يربط 2,584 عنصر إجابة مغلقة بالجواب والدليل و348 مهمة إنتاجية بسياسة عدم وجود جواب وحيد، وتقرير 336/336 هدفًا يربط التدريس بالتدريب والتقييم. تفشل `prebuild` إذا تغيّر المحتوى دون إعادة توليد التقارير. بقيت ثلاثة إعفاءات تسريب نوعية موثقة للتحية المتبادلة ومهمة حذف رأي؛ الفشل غير المعتمد صفر. تقرير الهدف بنيوي على نطاق الدرس ولا يدّعي مراجعة دلالية بشرية لكل عنصر.

أُغلق البند `254` في 2026-09-05 عبر `language-boundary-v1`. يفحص `prebuild` حاليًا كل 112 ملفات TSX و4,043 Opening tag: يفرض اقتران اللغة والاتجاه للألمانية والعربية بما فيها `ar-TN`، ويطلب Scope تقنيًا لكل LTR غير لغوي، ويمنع Unicode bidi controls الخام. تصنّف `fragmentLanguageAttributes` بنوك الأجوبة العامة إلى عربي/ألماني/mixed-auto/technical في 10 Renderers. يسجل التقرير 188 Fragment ألمانيًا و13 Scope و137 عقدة عربية+لاتينية تحت plaintext/isolate CSS مع صفر مشكلة. تحقق Playwright من DOM وcomputed `unicode-bidi` عبر المكتبة والتشخيص والتقييم والدرس والامتحان؛ يبقى النطق الفعلي لقارئ الشاشة ضمن P0-255 المؤجل للمراجعة النهائية.

أُغلق البند `112` في 2026-09-05 عبر `meaning-first-case-v1`. تعرض 17 وحدة تدريس حالة تسلسلًا ثابتًا قبل النظرية: `Bedeutung → Rolle → Form`، مع سؤال معنى لا يسمي الحالة، وخيارات دور دلالي، ثم قاعدة الشكل. يربط السجل 21 كتلة نظرية و54 تمرينًا موجّهًا و40 عنصر Mini-Test عبر A1=6 وA2=4 وB1=4 وB2=3. يشغّل `prebuild` تدقيقًا مستقلا يملك كل إشارات الحالة الصريحة (18 نظرية/27 تدريب/20 تقييم) ويمنع المراجع المكسورة أو بدء السؤال الدلالي بالنهايات. المراجعة اللغوية البشرية لجودة الصياغة مؤجلة بقرار المستخدم إلى مراجعة المشروع الكامل ولا تُدّعى هنا.

أُغلق البند `26` في 2026-09-05 عبر `diagnostic-productive-sample-v1`. بعد حد الأسئلة المغلقة يظهر لأصحاب الخبرة فقط Prompt ألماني أولًا مع دعم عربي، ويقبل كتابة من ثلاث كلمات أو تسجيلًا محليًا 3–45 ثانية أو كليهما أو `not-yet` بلا عقوبة. تُحفظ العينة وتقييم الاستقلال الذاتي مع الحد `self-evidence-no-automated-language-score`، ولا تحتوي Score ولا تغير مستوى الفهم المقترح. تدخل وسائطها DWNB والحذف الفوري وNamespace الملف الجديد، بينما يبقى مبتدئ الصفر خارج التشخيص والكتابة المبكرة.

أُغلق البند `159` في 2026-09-06 عبر `two-party-information-gap-v1`. يعرض مسار Speaking بطاقتي A/B بمعلومات خاصة غير متطابقة، ويخفي كل بطاقة قبل تمرير الجهاز، ثم يطلب أربع نقلات وقرارًا مشتركًا. لا يُحفظ الدليل إلا بعد تأكيد وجود شخص ثانٍ؛ التدريب الفردي لا يحتسب دليلًا ثنائيًا. لا يدعي التطبيق شريكًا حيًا من المنصة أو تقييمًا لغويًا.

أُغلق البند `160` في 2026-09-06 عبر `content-grounded-follow-up-v1`. بعد تسجيل المحاولة والاستماع إليها يكتب المتعلم اختياريًا خلاصة ألمانية؛ يولد المحرك المحلي سؤالًا يستعمل إشارة فعلية من المكان أو العمل/الدراسة أو التفضيل أو الخطة أو السبب/الرأي، ويرفض النص الفارغ أو القصير أو العربي فقط. Gemini وOpenRouter Free-only وOllama اختيارية خلف موافقة صريحة جديدة تعرض النص والوجهة قبل `fetch`، ويُرفض JSON الموسع أو `groundingCue` غير الموجود في النص. يُحفظ SHA-256 والمقتطف المحدود والسؤال والمزود/الموديل/الموافقة داخل محاولة المحادثة وDWNB، ويمكن حذف المقتطف فورًا مع التسجيل. لا يُرسل Blob الصوت ولا يُدعى STT أو فهم التسجيل أو تقييم النطق.

أُغلق البند `219` في 2026-09-07 عبر `browser-webgpu-model-v1`. تضيف الإعدادات فحص HTTPS و`navigator.gpu` ومحول فعلي وذاكرة معلنة 4 GB وحد Buffer محافظ ومساحة Browser، ثم تعرض تنزيلًا Opt-in بنحو 130–150 MB لنموذج `Xenova/paraphrase-multilingual-MiniLM-L12-v2` الكمي المثبت على Revision محدد. يعمل `feature-extraction` داخل Web Worker مع `device:webgpu` و`q8` لترتيب أربعة أسئلة متابعة مؤلفة؛ لا يوجد WASM fallback صامت، وعند الفشل يعود السؤال الحتمي. Cache مخصص بعلامة اكتمال قابلة للحذف، والأوزان خارج Git/ZIP/Offline pack. ملفات Runtime المتصفحية فقط موردة مع SHA-256 ونصوص Apache/MIT، وسجل المصادر ارتفع إلى 16. تختبر CI العامل الثقيل بـMock ولا تدعي تنزيل 118 MB أو اختبار GPU فعلي؛ تلك الجولة مؤجلة للأجهزة النهائية.

أُغلق البند `256` في 2026-09-05 عبر `status-announcement-v1`. يفصل المكوّن النص المرئي عن عقدة Live مخفية واحدة `role=status` مع `aria-live=polite` و`aria-atomic=true`، ويطبّع النص ويمنع إعادة إعلان الرسالة نفسها عند Rerender. عُمم على التشخيص والبوابات الأربع ومراجعة الوحدات وعلاج الأخطاء والكتابة والمحادثة والوساطة وكل أنواع نتيجة الامتحان المستهدف والتسليم المتصل، مع إبقاء عقد الأسئلة الذرية السابقة. يبقى P0-255 منفصلًا لأن الاختبار الآلي لا يعادل تجربة قارئ شاشة حقيقي.

أُغلق البندان `242` و`243` في 2026-09-05. يعرّف Manifest Offline v2 خمس حزم مستقلة: A1/A2/B1 لكل منها 51 مسارًا، B2 مع الامتحانات 199، والكاملة 298؛ ويقيد الصوت الاختياري إلى 40/48/48/124/260 ملفًا. يشغّل `postbuild` بيان `offline-size-manifest` من HTML المبني وملفات Next الفريدة ويعرض Gzip قبل التنزيل مع بصمة Build، مع إبقاء قياس Cache Storage الفعلي بعد التثبيت. القيم الدقيقة متولدة لكل Build ولا تُثبت في وثيقة التسليم؛ رؤوس HTTP أو Brotli قد تختلف ولا تُخفى هذه الحدود.

أُغلق البند `304` في 2026-09-05 عبر `secret-audit-v1`: يفحص `npm run check` النصوص الحالية، ويطلب CI تاريخ Git كاملًا بـ`fetch-depth: 0` ثم يفحص الأسطر المضافة والمحذوفة في كل الفروع، ويُفعّل Hook إصداريا في `.githooks/pre-commit` ومسار Termux الآمن. نُسخ المستودع العام عند `be56463e` كاملًا وفُحصت 15 Commit و43,401 سطر Patch نصي بلا أي نتيجة. كل نتيجة محتملة تُعرض ببصمة محجوبة، ويبقى تدوير المفتاح عند تعرض حقيقي إجراءً إلزاميًا خارج قدرة الفحص.

تقدّم البند `135` في 2026-09-05 دون ادعاء مراجعة صوتية. يوحّد `learning-playback-speed-v1` ثلاث سرعات تعليمية في خمسة أسطح استماع، ويطبق السرعة نفسها على MP3 وBrowser TTS، ويحملها عبر Segments الامتحان المرتبة. تبقى البروفة الزمنية المتصلة على 1×، ولا تمس السرعات تسجيلات المتعلم. اختبرت المتصفحات `playbackRate` و`preservesPitch`، لكن الحكم البشري على التشويه مؤجل للجولة النهائية ولذلك يبقى البند جزئيًا.

تقدّم البندان `373` و`376` في 2026-09-05 دون إغلاق زائف. يربط `tunisian-support-v1` سبع عشرة ملاحظة اختيارية بسبعة عشر Theory block منشورًا، ويعرضها فقط لمن اختار `tunisian-supported`. لكل سجل جسر فصيح وتقريب تونسي منفصل وخطر فهم ومرساة ألمانية؛ التوزيع 6/5/4/2 من A1 إلى B2 عبر 15 فئة فرق. يفشل `tunisian:audit` عند مرجع مكسور أو ظهور خارج الوضع المختار أو ادعاء مراجعة بلا اسم/تاريخ. كل السجلات ما زالت `authored-review-pending`، والمراجعة اللغوية المستقلة مؤجلة للجولة النهائية حسب قرار المستخدم.

أُغلقت البنود `38` و`266` و`267` في 2026-09-05. يحافظ `pre-srs-retrieval-warmup-v1` على ثلاث محاولات استرجاع German-first مع كشف مؤجل عندما لا توجد بطاقة SRS، دون صنع بطاقة أو دليل إتقان؛ وتبقى جلسة الصفر بلا اختبار حتى توجد مادة سبق عرضها. تعرض `weekly-grace-v1` يوم سماح واحدًا في الخطة، ولا تحسبه دراسة أو دين تعافٍ، وتسمح لاستمرارية الأدلة بجسر يوم واحد فقط بين يومي دراسة فعليين. يوحّد `behavioral-praise-v1` مدح السلوك في Today والمراجعة والكتابة والمحادثة والامتحان، وتمنع الاختبارات عبارات التصفيق العامة.

أُغلق البندان `63` و`325` في 2026-09-03. يستخدم `novelty-weighting-v1` وزن 1.5 لأول سؤال نقل جديد، و1 لأول تدريب جديد، و0.25 فقط لأحدث إعادة للسؤال نفسه مهما كثر النقر؛ إكمال الدرس يحسب التغطية والدقة الموزونة بدل عدّ كل إجابة صحيحة متكررة. وأصبح SM-2 `sm2-v2-calendar` يحدد يوم المراجعة في منطقة IANA محقونة، ويحفظ `review-calendar-v1` والمنطقة مع الموعد والحدث، مع مصفوفة UTC وAfrica/Tunis وEurope/Berlin وAmerica/New_York وAustralia/Sydney عبر انتقالات DST. السجلات القديمة `sm2-v1` ما زالت صالحة للاستيراد.

توسعت الطبقة إلى 1,044 سجل اسم و104 إطارات. أصبحت جميع قوائم الاسم A1/A2/B1/B2 بصفر pending-human آليًا، وطابور الإطار غير المصنف صفر مع ستة استبعادات إصداريا معللة. يبقى P0-98/99 جزئيًا فقط إلى حين المراجعة الألمانية المستقلة المسماة.

في 2026-09-04 تحقق Push عام فعلي إلى `be56463e27ae676e82289c6b348b97c5161d7051`، وظهرت الملفات الجديدة عبر Raw GitHub، وأعاد Vercel حالة `success` مع Production حي على `https://wegberlin.vercel.app`. نُقل P0-301 من «متوقف» إلى «جزئي»؛ المتبقي هو PR Preview فعلي.

تشغيل Quality Gate رقم 3 أكمل بحالة `failure`: Job `check` وBuild نجحا، بينما فشل Job e2e المكتبي. لأن Production suite الأحدث نجحت محليًا 66/66، عُدّل Workflow ليشغّل Desktop+Mobile مع Retry واحد في CI فقط، ورُفعت GitHub actions من v4 إلى v5. يبقى P0-302 جزئيًا حتى ينجح التشغيل البعيد بعد Push.

## P0 المتوقف على صلاحية المستخدم

لا توجد الآن عناصر P0 متوقفة بالكامل. البند 301 انتقل إلى «جزئي» بعد تحقق GitHub `main` وVercel Production؛ يبقى اختبار PR Preview فعليًا عبر حساب المستخدم.

## عناصر P0 المنجزة

الأرقام التالية مغلقة حاليًا بدليل منتج/اختبار:

```text
1, 2, 3, 4,
13, 14, 15, 16,
25, 26, 27, 28,
37, 38, 39, 40,
49, 50, 51, 52,
61, 62, 63, 64,
73, 74, 75, 76,
85, 86, 87, 88,
97, 100,
109, 110, 111, 112,
121, 122, 123,
133, 134, 136,
145, 146, 147, 148,
157, 158, 159, 160,
169, 170, 171, 172,
181, 182, 183, 184,
193, 194, 195, 196,
205, 206, 207, 208,
217, 218, 219, 220,
229, 230, 231, 232,
241, 242, 243, 244,
253, 254, 256,
265, 266, 267, 268,
277, 278, 279, 280,
289, 290, 291, 292, 303, 304,
313, 314, 315, 316,
325, 326, 327, 328,
361, 362, 363, 364,
374, 375,
385, 386, 387, 388
```

## أعمال مهمة خارج P0 ما زالت غير منجزة

هذه ليست فشلًا مخفيًا في عداد P0، لكنها تمنع وصف الإصدار بأنه مراجَع بشريًا أو رسميًا:

1. مراجعة أكاديمية مستقلة لكل A1–B2 والعربية.
2. تسجيلات بشرية متعددة المتحدثين؛ الحالي اصطناعي أحادي المتحدث.
3. مراجعة حقوق مزود التوليد قبل توزيع تجاري.
4. تدقيق تشابه/حقوق مستقل مع Menschen والمواد الرسمية.
5. اختبار أجهزة فعلية: iOS Safari وFirefox وSamsung Internet وVoiceOver وTalkBack وNVDA.
6. شريك محادثة حي أو تقييم نطق/طلاقة صوتي صالح؛ غير موجود ولا يُدعى.
7. Browser lockdown أو مراقبة امتحان رسمية؛ غير موجودة ولا يمكن ادعاؤها.
8. GitHub `main` وVercel Production يعملان علنًا؛ PR Preview لم يُختبر بعد، ولا يزال النشر يحتاج مراجعة بشرية للمحتوى والحقوق قبل وصفه نهائيًا.

## ترتيب الإغلاق المقترح المتبقي

قرار المالك هو إكمال البناء والاختبارات الآلية أولًا، ثم جمع الاختبارات اليدوية والمراجعات البشرية في جولة نهائية واحدة. لذلك الترتيب الحالي:

1. `302`: بعد Push المستخدم، لا يُغلق إلا بتشغيل GitHub Actions جديد أخضر.
2. `301`: أنشئ PR فعليًا، تحقق من Vercel Preview URL، ثم أغلق PR؛ Production وحده لا يكفي.
3. أنشئ `P1_AUDIT.md` بندًا بندًا قبل ادعاء أي نسبة P1، ثم نفّذ أعلى فجوة آلية القيمة.
4. الجولة البشرية النهائية: `98 + 99` للمراجعة الألمانية المستقلة، `124` لدلالة اقتباسات القراءة، `135` لتشويه السرعات، `255` للأجهزة والتقنيات المساعدة، و`373 + 376` للمراجعة التونسية/الفصحى. لا يُغلق أي منها آليًا.
