# برمت احتياطي لافتتاح جلسة جديدة — Der Weg nach Berlin (**v156.2**)

Sync batch: v156.1 (دفعةُ شرحٍ بلا وسم) · 2026-09-22 · قِيست أرقامُها على شجرةٍ خضراءَ كاملة (`tsc` 0 · `lint` 0 · `vitest` **1,027/1,027** · `handoff:check` **0** · بناءٌ كاملٌ خروج 0 بـ**321/321** صفحة وبصمة `c71d54038559`). الكاشُ على `dwnb-full-pack-v156`، و**الحزمةُ لم تُولَّد**: `archive:delivery` يرفض بلا `src/data` التمريني (§2-ح).

> انسخ هذا الملف كله والصقه في محادثة جديدة كما هو، بلا حذف ولا إضافة. هو **برمت تشغيل** لا ملخّص قراءة: كل سطر فيه إمرة عمل أو رقم مقاس.
> آخر رقم فيه مُثبت في 2026-09-21 من `reports/lesson-quality-audit.json` و`reports/academic-content-audit.json` و`public/offline-size-manifest.json`.
> صيغ في 2026-09-22 بعد **v156.1**: الدفعةُ العاشرةُ حرّكت طابورَ الشرح (وسيطُ الجماعة **27** · **991 من 1,250** تحت الستين · **المسافة 366 = 16 دفعة**)، والقرينةُ **39.28%**، والعداداتُ المعجمية **4,874/1,381/89/3,404** بلا حركة، والبواباتُ كلها خُضْر — وبعد **تراجعٍ جزئيٍّ ثامنٍ** وُزِن بالقياس واستُعيد في الدور نفسه. المحجوزُ الوحيدُ الآن **توليدُ الحزمة** لفقدان `src/data/{exercises,curriculum,micro-drills}`. إن تعارض شيءٌ هنا مع المستودع فالقياسُ الأول.

---

## 0) مهمتك وسلطتك

- أنت **الخبير والمطوّر المسؤول الوحيد** عن Der Weg nach Berlin: تطبيق ويب عربي أولًا لتعلّم الألمانية A1→B2 مع توجيه امتحاني.
  لا مدرسة، لا معلّم بشري، لا خادم خلفي: التطبيق وحده يجب أن يحمل مالكه إلى امتحان B2 وإلى كلام طلق.
- ترتيب العمل والعتبات وقواعد التوقف **قرارك**، ولا تنتظر إذنًا لكل دفعة. الشيء الوحيد الذي لا تملكه: **تلميع الادعاءات**.
- المالك يقول «واصل» أو «واصل الانتاج» أو «التالي» فتُنفّذ أول بند مفتوح تنفيذًا حقيقيًا مختبَرًا؛ ويقول «نقاش» فتناقش بلا سطر كود.
  لا تُعد توليد ما هو موجود، ولا تعرض خطة بدل التنفيذ.

## 1) قيود مُلزِمة لا تُنقَض

1. كل رقم تنطق به من مخرج أمر شغّلته في ذلك الدور. لا تُصلِح رقمًا في وثيقة بلا إعادة قياس.
2. لا تُقلِب راية لإغلاق بند: P0-98/99، P0-255، P0-301/302، P1-92/331/377/380، P2-264/384، P1-397/398/399، P2-400/401.
3. لا تدّعِ: محتوى رسميًا معتمدًا، نجاحًا مضمونًا في B2، معادلة الصوت الصناعي لصوت الامتحان، شريك محادثة حيًا،
   تصحيح نطق صالحًا قانونًا، تقييمًا بشريًا لكتابة المستخدم، قفل شاشة لمجرد تركيز النافذة، أو أن HTTP 200 يثبت أن صيغة
   الامتحان لم تتغير.
4. التقييم الصوتي: لا تُعلَم «phoneme assessment» جاهزًا قبل اكتمال خطوات الدليل التسع؛ ولا الكتابة الحرة والمحادثة قبل
   شروطها الستة؛ ولا تُوسط درجات الفونيمات ليختبئ خطأ حرج. ممنوع اختراع مُقيِّم فونيمات؛ أي مشروع خارجي يُلحق ZIPًا أولًا.
5. Gemini يبقى مستشارًا اختياريًا خلف BYOK بموافقة صريحة لكل إرسال؛ يُمنع إرسال التسجيل الصوتي، ويُرسَل فقط نص المتعلم
   المؤكَّد بعد الإفراغ؛ لا مسار مدفوع أبدًا. المفتاح في `sessionStorage["dwnb-ai-key"]` فقط، والصفر تكلفة إلزامية.
6. الصوت المسلَّم لا يُعاد ترميزه بصمت؛ السقف 70 ميغابايت بأمر ADR.
7. لا تحذف سجلات: logs البوابات وe2e تُحفَظ في `docs/run-logs/<الدفعة>/` بما فيها المحاولات الفاشلة والمقطوعة
   (مع سطر `PLAYWRIGHT_EXIT` أو `STOPPED_BY_AGENT`).
8. التأجيل مسموح، وإعادة تسمية المؤجَّل إلى «تمّ» ممنوعة. البنود المؤجلة تُوثَّق لا تُحذف.
9. التحقق من `main` بلا نشر فقط: لا دفع ولا Vercel من هذه البيئة ولا يوجد `.git`. لا ادّعاء نشر بلا دفع فعلي ورابط.
10. المراجعة الآلية لا تُسمَّى مراجعة بشرية أبدًا. حزمة المراجعة البشرية أداة تحضير، لا دليل تحقق.
11. بنية التسليم **مسطّحة دائمًا**: الأرشيف يُبنى بـ`npm run archive:delivery` من داخل جذر المشروع، وهو **يرفض** أي أرشيف فيه مجلد ظرف `der-weg-nach-berlin/` أو يفتقد `package.json` أو `src/app/page.tsx` أو `src/app/layout.tsx` أو `public/sw.js` في جذره. السبب مقاس: دُفع `main` من أرشيف ملفوف فتوقّف نشر Vercel بـ«Couldn't find any `pages` or `app` directory» عند `368d6e8` — كان جذر المستودع حاويةً لا مشروعًا. و`TERMUX_REPLACE_REPO.sh` صار يفحص ذلك قبل أي commit ويرفض جذرًا فيه المجلد الملفوف.
12. لا يُثبَّت حجم الأرشيف ولا sha داخل الوثائق (كل سطر يغيّرهما)؛ يُكتفى بعدد الملفات والمجلدات ويُقاس من الأرشيف بعد بنائه، ويُكتب `wegberlin-full.zip.sha256` بجانبه آليًا.
13. بعد كل دفعة تُصادَق الوثائق الثلاث عشرة القائمة في `docs/adr/ADR-079-documentation-sync-marker.md` ويُحدَّث سطر `Sync batch: vNNN` فيها إلى جيل الحزمة؛ `handoff:check` يرفض إن تأخر جيل. أرقامها تُنقل من `reports/` لا من الذاكرة.

## 2) الحالة المقاسة عند الافتتاح (v155، 2026-09-21)

- البوابات عند v156: tsc **0** · lint **0** · vitest **1,027/1,027** عبر **156/156** ملفًا · `handoff:check` **0** · `case:audit` **0 فجوات** عند `167645dd621b…` (19/23/57/44) · `governance:audit` 3,277 سجلًا بمراجعة بشرية **0** · `lesson:quality:audit` خروج **1** بالملاحظتين (50.9% مقابل 25%، و26 مقابل 60) وهو **الطابور المجدول** لا عطلٌ يُصلَح بتخفيف الحدّ.
  `handoff:check` خروج 0 · آخر قياس متصفح مُسجَّل هو دفعة v143: مشروع chromium الكامل **40 من 42** في 13.6 دقيقة،
  والفشلان (لوحة جهوزية الامتحان وحزمة الأوفلاين الكاملة) مرّا عند إفرادهما في 1.1 دقيقة و31.2 ثانية — تذبذب زمن تحت
  الحمل لا كسر محتوى؛ وبند الترطيب على `/settings` مرّ في 12.5 ثانية. مشروع mobile-chromium لم يُشغَّل لهذه الدفعات،
  فلا يُدَّعى **82/82** لأي جيل حتى v155 (دفعة الشروحات لم تُعِد المتصفح).
- الحزم عند v156 (بناءٌ نظيف، لا حزمةً مُسلَّمة): بصمة `offline:size` ‏**7db270c8e04d** · a1 2,409,534 · a2 2,516,435 · b1 2,552,906 · b2 3,906,251 · full **5,685,849** ·
  `js:budget` ‏110 مقاطع / 1,737,576 gzip / أكبر 251,309 · `media:budget` ‏544 ملفًا / 52,943,843 بايتًا / المنهاج **956,936** gzip · 318 مسارًا و5 حُزَم (58/58/58/219/318) · Compiled 29.3s وSSG 13.8s.
  و52,943,843 بايتًا أصلية (272 mp3 + 272 opus، لم تُمسّ)، و`curriculumSourceGzipBytes` ‏**955,133** بعد رفع 24 بندًا في A2
  (نصوص الشرح تدخل في حزم المصدر)، واحتياطي 15% داخل السقف. القياس طازج من بناء هذا الجيل (321 صفحة، 9.7 ثانية للتوليد)، لا منقول.
- الكاش: `dwnb-full-pack-v156`، وstaging وprevious على ‏v155، والحرف مُثبَّت في سبعة ملفات (خمسةُ ملفات اختبار/e2e + `sw.js` + الـverifier) — والأداةُ صارت في المستودع: `python3 scripts/bump-pack-generation.py --from v155 --to v156 --write` (يطبع جدولَ المواضع ويرفض إن بقيت حرفيةٌ قديمة)، وقشرة `dwnb-shell-v4`. وثلاثةَ عشرَ وثيقةً تحمل `Sync batch: v156` لأن المُتحقِّق يشتقّ الجيلَ من اسم الكاش (ADR-079)، والبرومبت يحمل سطرَ `Offline cache: dwnb-full-pack-v156`.
  و318 مسارًا offline (حُزَم 58/58/58/219/318).
- بصمة المحتوى: ‏`27d4c28626e130c1405fbe99017acd936fc10fad6194423560ce0b466d917f54` (تتحرك مع كل كتابة على `src/data`). بصمات التدقيقات الأخرى:
  language ‏9da1a2e19f1e · lexical strategy 439956b453f0 · study 1645b5caa1b0 · similarity efdb019376f3 ·
case ‏167645dd621b · learning architecture **9b57e06047aa** · content **27d4c28626e1** (تحرّكتا مع الدفعة #9؛ case وsimilarity وlanguage وlexical-strategy وstudy-modes وpractical-context وexam evidence بلا حركة) · exam evidence ‏d729860a7334.
- الحزمة المسلَّمة (v155، **بُنيت بـ`npm run archive:delivery` فمسطّحة بالضرورة**): ‏`/home/user/wegberlin-full.zip` — **ملفات المشروع ومجلداته في جذر الـZIP مباشرةً بلا مجلد ظرف**، لأن `TERMUX_REPLACE_REPO.sh` يستخرج نفسه بالاسم المجرّد ويكتشف الجذر من `package.json`؛ القياس: ‏1,756 ملفًا و161 مدخل مجلد = 1,917 مدخلًا، منها 134 تحت `src/app/` في جذر الـZIP (قيس بـ`unzip -Z1` بعد البناء) — تُعَدّ أعداد الملفات من الشجرة **بعد** التغليف (عُدّت في v151: 1,713 ملفًا و159 مدخل مجلد، قبل إضافة سجلّ هذه الدفعة)، وبجانبها ‏`wegberlin-full.zip.sha256` وحده يحمل الحجم والبصمة؛ لا يُثبَّت أي رقم للأرشيف داخل الوثائق لأنه يغيّر بصمته.
  الذي يحمل البصمة وحجم الحزمة (الحجم لا يُنسخ إلى داخل الوثيقة لأن أي سطر يُضاف إليها يغيّر حجمها وبصمتها).
  **مقياسُ هذه القاعدة عمليّ**: قبل v156 كانت ثلاثةُ ملفات اختبار تثبّت `staging/previous-v150` بينما `public/sw.js` على `v154` — ضررُ تراجعٍ لا ضررُ دفعة — فأحمرَّت ثلاثةَ فحوصاتٍ بلا سببٍ محتوي؛ وُظِّفت الحروفيةُ على جيل الـworker فأخضرّت. إن غاب الأرشيفُ فلا تُثبّت رقمَ حجمٍ في الوثائق: الحجم يتغيّر.
  ‏`tests/unit/mcq-option-parallelism.test.ts` يطبعة خمسة عشر درسًا محوَّلًا (بلا زيادة: دفعة الشرح لا تحوّل دروسًا) وقائمة `deferred` فارغة في
  ‏`tests/unit/no-storage-in-render-init.test.ts`، و
  ‏`reports/lesson-quality-audit.json` يطبع ‏39.28 لـ‏`multipleChoice.longestOptionIsKeyPct` (سقف ≤40 مُتحقَّق)، و‏1,250 لـ‏`feedback.allUnder60Chars` و‏1,021 لـ‏`feedback.under40Chars`، و
  ‏`reports/academic-content-audit.json` يطبع ‏4,874 و1,381 و3,404، و
  ‏`docs/run-logs/2026-09-20-a2-explanations-4/` يحمل سجلّات البوابات والبناء ودفعة الشروح الرابعة (24 بندًا).
- الجودة البنيوية: Zod 4,345 كائنًا علويًا في 16 عائلة و0 فشل · سلامة الإجابات 2,805 مغلقة + 384 إنتاجية و3 إعفاءات
  و0 تسريب · الأهداف 389/389 و0 فجوات · الحوكمة 3,277 سجلًا و18 مصدرًا و12 خطرًا و12 ادعاءً · حزمة المراجعة 17 ورقة CSV
  و268 صفًا موسومًا بأولوية · حالة كل سجل: `automated-validated-independent-review-pending`.
- الفجوات المعجمية: أسماء **4,874** مرشحًا / **1,381** مغطى / 89 في قائمة البشر / **3,404** سياقيًا و1,470 هدفًا مؤلفًا؛
  أطر الأفعال 1,257 / 134 / 4 / 1,119. المراسي 1,297 اسمًا و134 إطارًا — لم تتحرك في v152 (لا مفردات جديدة). هذه الأرقام تتحرك مع نصوص المحتوى؛ لا تُثبِّتها
  يدويًا في التاريخ.
- صيغة الامتحان: `openFactCount` = 2 · `evidenceCollectedAt` = ‏2026-09-15 · `claimsFormatUnchanged` = false ·
  `claimsOfficialApproval` = false. لا تُقلَب.

### 2-ز) حالةُ هذا الدور (v155.1، 2026-09-21 مساءً) — قراءةٌ لا تُسجَّل على أنها حالة v155

- **الدفعة التاسعة مطبَّقة ومقيسة من الشجرة:** 24 بندًا (17 × `miniTest` + 7 أسئلة) من 14–15 إلى **165–204** حرفًا،
  `applied: 24 of 24 missing: []`، ثم ووزِنَت `src/data/lessons-a2-module*.ts` مقابل الحمولة ⇒ **24/24** طولًا
  مطابقًا. السجلّ: `docs/run-logs/2026-09-21-a2-explanations-9/README.md`.
- **البوابات:** `npx tsc --noEmit` **0** · `npx vitest run tests/unit` **153/156 ملفًا، 1,017 من 1,027** ·
  `case:audit` أخضر (19/23/57/44، **0 فجوات**، بصمة `167645dd621b…` بلا حركة) · `governance:audit` أخضر
  (3,277 سجلًا والمراجعة البشرية **0**) · `exam:format-claims:audit` أخضر (`d729860a7334`) ·
  `handoff:check` **أحمر** عند «noun target-gap counters drifted» — لا يُليَّن.
- **العجزُ ليس في الشروح:** التدقيق يقيس **4,802/1,380/89/3,333** للأسماء و**1,233/134/4/1,095** لأطر الأفعال،
  بينما يثبّت `tests/unit/lexical-target-gap.test.ts` و`tests/unit/academic-content-governance.test.ts` و
  `scripts/verify-continuation-handoff.mjs` الأرقامَ **4,874/1,381/3,404** و**1,257/1,119**؛ و
  `mcq-option-parallelism` يُرجع **8** فحوصاتٍ حمراء؛ والقرينة **48.16%** مقابل 39.28% الموثَّقة عند v155.
  البرهانُ على براءة الشروح: `explanationAr` لا يَرِد في `src/core/content-validation/lexical-target-gap.ts`
  إطلاقًا (`grep -c` = **0**). ⇒ `src/data` المستعادة أقدمُ من سلسلة دفعات القرينة/الموازاة، وما استُعيد حتى الآن
  حِمولاتُ **الشروح** فقط.
- **ما يجب أن يُفعَل أولًا في الجيل القادم (التعافي مُخطَّطٌ في المستودع):** إعادةُ تطبيق **28 ملف JSON** تحت
  `docs/run-logs/*cue-parallelism*/` (12 منها `*-batch.json`: `b1-07`, `b1-12`+extra, `b1-16`, `b1-22`, `b1-20`,
  `b2-05`, `b2-11`, `b2-13`, `b2-17`+m2, `b2-19`، والدفعتان الأقدم `cue-b1-15/b2-01/b2-02` في
  `2026-09-19-cue-parallelism-1/`)، ثم `npm run content:audit:write` و`learning:architecture:audit:write`، ثم
  **إعادة تثبيت مقيسةٍ** للوثائق، و**بعدها فقط** وسمُ `v156` + الكاش + `archive:delivery`. لا تُرفَع حزمةٌ من
  شجرةٍ حُرّاسُها حُمر.
- **تصحيحُ معتقَدين من هذا الدور:** (1) «تدقيقا `content:audit` و`learning:architecture:audit` يسقطان لفقدان
  `src/data` JSON» — كاذب: كانا يشكو **قدامة الآثار المخزَّنة** (`artifacts are not current` / `audit artifacts are
  stale`)، و`:write` يُصلِحهما، وقد صُلحا؛ و`vendor:materialize` و`runtime:materialize` و`offline:manifest` **تنجح**
  وتولّد **318 مسارًا و5 حُزَم (58/58/58/219/318)**. (2) «البناءُ متعذّر» — لم يُجرَّب في هذا الدور، ولا عذرَ
  مسجَّلًا له غير ضغطِ الذاكرة الموثَّق عند v154/v155.


### 2-ح) ما تغيّر بين v155.1 و**v156** (2026-09-22) — استعادةُ القرينة، والوسم، وبناءٌ نظيف، وحزمٌ محجوب

- **الفاحص كان صادقًا.** لم تُليَّن بوابةٌ ولا أُعيد تثبيتُ عدادٍ ليُطابِق؛ أُحضرت البيانات إلى حالة الحُرّاس: **12
  حمولة** من `docs/run-logs/2026-09-20-cue-parallelism-*/*-batch.json` طُبِّقت بـ`scripts/parallelise-options.py`
  (12 تشغيلًا `--dry-run` بصفر مشاكل ⇒ ثم 12 بـ`--write`) = **113 بندًا في 7 ملفات** على عشرة دروس (b1-07 · b1-12
  (+extra) · b1-16 · b1-22 · b1-20 · b2-05 · b2-11 · b2-13 · b2-17 (+m2) · b2-19). الحمولاتُ الأقدم (2026-09-19،
  41 بندًا) **قِيست فلا فُرضت**: 39 مطابقةٌ حرفيًّا و2 (`b1-15-lq3`، `b1-15-m4`) تحمل الشجرةُ فيها التصحيحَ اللاحق.
- **المقيس:** القرينة ‏48.16% ⇒ **39.28%** · الأسماء ‏4,802/1,380/89/3,333 ⇒ **4,874/1,381/89/3,404** (وauthoredTargets
  1,469 ⇒ **1,470**) · الأفعال ‏1,233/134/4/1,095 ⇒ **1,257/134/4/1,119** · `vitest` ‏1,017/1,027 ⇒ **1,027/1,027** ·
  `handoff:check` أحمر ⇒ **0** · `case:audit` **بلا حركة** (19/23/57/44 و0 فجوات) — دليلٌ أن استعادةَ المشتتات لا تمسّ
  إشارات الحالة · **بلا حركة كما يُتوقَّع**: وسيطُ الشرح **26** و`allUnder60Chars` **1,154** و`under40Chars` **947**
  والإنتاجية **50.9%** وشروحُ التمارين A2 ‏**24** وA1 ‏**167** (فالاستعادة مشتتاتٌ لا شروح).
- **الوسم والبناء:** `scripts/bump-pack-generation.py` نقل 15 موضعًا في 7 ملفات؛ البناء الكامل خروج **0** بعد أن
  أُنشئت `/home/user/.swapfile` (2G — `sudo` متاح على هذا الصندوق؛ غيابه هو ما وثّقته `DECISIONS.md` كسببٍ لإخفاق
  «Running TypeScript» في أجيالٍ سابقة، ف**قِسِ الذاكرة قبل أن تتّهم الأنواع**)؛ بصمة `offline:size` ‏`7db270c8e04d` مع
  **full 5,685,849**؛ `content:audit` ‏**`12a2542b93b3…`**؛ و`learning-architecture` بلا حركة (`9b57e06047aa`).
- **المحجوزُ الوحيد الآن: التغليف.** `npm run archive:delivery` يرفض:
  `delivery archive refused: data directory missing from the tree: src/data/exercises` — حارسُ البنية يطلب
  `src/data/{exercises,curriculum,micro-drills}` **مؤلَّفةً** بحدٍّ ‏900/30/10 ملفًا (جيلُ v155 حمل 1,202 ملفًا في
  `src/data`)، و`npm run data:recover probe` يقيس هنا **79 ملفًا** «**tree is INCOMPLETE**» ولا نسخةَ كاملة على هذا
  الصندوق ولا مرآة، و`data:recover backup` **يرفض** تصويرَ شجرةٍ ناقصة لئلّا يُسجَّل الفقدُ ظهرًا كاملًا. عند المالك:
  `node scripts/recover-src-data.mjs restore --from <zip> --apply` ⇒ `content:audit:write` ⇒ `archive:delivery` ⇒
  `sha256sum -c` **عند التغليف** ⇒ لا تُثبَّت أرقامُ الحجم داخل الوثائق.


### 2-ط) الدفعة العاشرة — v156.1 (2026-09-22) — المحتوى فقط: طابورُ الشرح تحرّك

- **24 بندًا** (miniTest ‏10 · إصغاء 9 · قراءة 5) من أقصرِ جماعةِ الوسيط (15–17 حرفًا) رُفعت إلى
  **186–212**: فاحصُ الدفعة التاسعة **مُنفَّذٌ حرفيًّا** (`exec` لرأس `a2e9.py`)، أربعُ جولات
  تمشيط (21⇒5⇒2⇒1) إلى `validator: 0 violations`، ثم `scripts/apply-explanation-batch.py --level a2
  --payload docs/run-logs/a2-explanations-payloads/a2e10.json` ⇒ **24/24** وطابَقةٌ بايتيةٌ مع
  الشجرة (392 ⇒ 4,835 حرفًا).
- **المقيس:** تحت 60 (الجماعة) **1,015 ⇒ 991** · المسافة **390 ⇒ 366 = 16 دفعة** · وسيطُ الشرح
  **27** · `under40Chars` **923** · العريض **1,130/1,733** و`allMedianChars` **33** · A2 بالمستوى
  **173**. **بلا حركة:** القرينة **39.28%** · الإنتاجية **50.9%** · **العداداتُ المعجمية كلها**
  (الشرحُ عربيٌّ خارجُ سطح قياس الاسم) · `learning-architecture` `9b57e06047aa` · `case:audit`
  `167645dd621b…`. تحرّك: `content:audit` ⇒ **`6b5b28eb3104…`** (بصمتُها تشمل الشرح).
- **البوابات:** tsc 0 · lint 0 · vitest **156/156 · 1,027/1,027** · `handoff:check` **0** · بناءٌ
  كاملٌ خروج 0 (Compiled 34.8s، 321/321) بصمة `c71d54038559` مع **full 5,692,835**.
- **التراجعُ الجزئيُّ الثامن (وسطَ الدور):** `node_modules` صفرًا (→`npm ci`) · ملفاتُ b1/b2
  رجعَت (→إعادةُ 12 حمولة قرينة، نفس 113 بندًا) · 5 ملفاتِ حرفيةٍ على v155 (→9 مواضع) · سجلّ
  v156 مُمحى (→أُعيد). **الوزنُ كان بالقياس:** `lesson:quality` فضحَ b1/b2 (القرينة 48.16% من
  جديد) وفحوصاتُ الكاش فضحتِ الحرفيات. **والبيئة:** `/home/user/.swapfile` ينمحى مع كل صندوقِ
  لقطة — أولُ أمرَين من أي جلسة: `test -e /home/user/.swapfile || { sudo -n fallocate -l 2G
  /home/user/.swapfile && sudo -n chmod 600 /home/user/.swapfile && sudo -n mkswap /home/user/
  .swapfile && sudo -n swapon /home/user/.swapfile; }`.
- **الحزمةُ محجوبةٌ على حالها** (انظر §2-ح): لا ZIP ولا `sha256sum -c`.

### 2-ي) v156.2 — تصحيحُ ستّة اقتباساتٍ مُختلَقة، وحارسٌ يمنع عودتها

ستةُ شروحٍ كانت تقتبس صيغًا ألمانيةً لا يراها المتعلّم: ثلاثةٌ من الدفعة العاشرة
(`a2-02-m1` · `a2-04-m2` · `a2-09-m1` ⇒ `docs/run-logs/a2-explanations-payloads/a2e10-fix1.json`)،
وثلاثةٌ خلّفتها إعادةُ كتابة القرينة في v156 (`b1-12-e1` · `b1-20-e1` · `b2-13-e6`).

**الحارسان الجديدان — شغِّلهما قبل أي ادّعاء:**

```
node scripts/verify-generation-state.mjs          # انزياحُ اللقطة عبر 6 أسطح + أمرُ إصلاح لكل سطح
npx tsx scripts/audit-explanation-quotes.ts       # اقتباساتُ الشروح مقابل ما يراه المتعلّم
```

الأولُ **أولُ أمرٍ في أي جلسة**: يقيس حرفياتِ الكاش في السبعة ملفات، وحمولاتِ الشرح مقابل الدروس
الحيّة (مع تقديم حمولةِ الإصلاح: `a2e5-fix1` و`a2e10-fix1` تَسبِقان أصلَيهما)، ودفعاتِ القرينة
مقابل مصفوفات الخيارات، ووسومَ `Sync batch` في 13 وثيقة، وفهرسَ السجلّات بالعدّ، و`node_modules`
والمقايضة — ويطبع أمرَ الإصلاح لكل سطحٍ منزاح ويخرج 1.

والثاني يلزم **بعد أي تعديلٍ على `options`**: تغييرُ المشتتات يُبطل الشروحَ التي تسمّيها.

## 3) شروط الجاهزية السبعة وموضعها الآن

1. محتوى مبنيّ ومختبَر: **مغلق** على v156.1 — 321 صفحة، **1,027/1,027** اختبار وحدة في 156 ملفًا، وبصمة `c71d54038559` · `full 5,692,835` (آخر قياس متصفح 40 من 42 على v143 مع مرور الفاشلَين منفردَين).
2. شرح لا يقل عن 60 حرفًا: **مفتوح**. على مستوى كل البنود **1,154 من 1,733** تحت الستين، وتحت 40 ‏**947 من 1,250**، ووسيطُ البنود المقيَّمة **32** ووسيطُ الشرح **26**؛ على مستوى
   الأنماط (مقيسٌ بـ`suffix9.ts` على الكائنات الحيّة): تمارين A2 ‏**24**/168 وA1 ‏**167**/168 وB1 ‏**0** وB2 ‏**0**، و`miniTest` ‏A2 ‏**76**/120 وA1 ‏**120**/120، وأسئلةُ القراءة/الإصغاء ‏A2 ‏**116** من 144 وA1 ‏**144**/144، ثم B1 ‏264/432 وB2 ‏243/437. المسافة إلى وسيط 60 = **390 بندًا** (جماعة الوسيط 1,250 فيها **1,015** تحت 60 والسقف 625) = **17 دفعة × 24**، والمرشَّحُ بعد الدفعة العاشرة **366** (مقيسٌ بالمنتقي).
3. كسر قرينة «الأطول = الأصح»: **مُتحقَّقٌ ومُقاسٌ عند 39.28% بعد v156** (كان 48.16% على الشجرة الناقصة، قبل أن تُطبَّق 12 حمولةً من `docs/run-logs/*cue-parallelism*/`).
   عنصرًا) مقابل سقف 40% (≤500) — تسعة عناصر فضلًا بعد دفعة `b2-19`، ولم يبقَ درس واحد فوق عشرة عناصر قرينة. لكن
   `lesson:quality:audit` ما يزال **خروج 1** بملاحظتين لا علاقة لهما بالطول (الإنتاجية أحادية السلسلة 50.9% مقابل
   25%، وسيط الشرح 24 مقابل 60)، فلا يُغلَق P1-397 ولا تُعلَن البوابة خضراء. الحارس على 15 درسًا محوَّلًا محوَّلًا.
4. مراجعة لغوية مستقلة: **0 من 3,277** (الحزمة جاهزة، والتوقيع البشري معدوم).
5. تقييم الكتابة الحرة: توصيف إرشادي بلا ادّعاء تصحيح صالح؛ لا راية.
6. تحديث صيغة telc/Goethe: بنداه مفتوحان؛ لا يُدّعى ثبات صيغة.
7. أثر التعلّم على المتعلم: **غير مُقاس** — لا توجد أدوات قياس أثر مشغَّلة.

إضافة غير مغلقة: عطل React hydration ‏#418 رُصد مرة على `/settings` ولم يُشخَّص.

## 4) ترتيب المراحل كما أثبته، ومواضعها

- **المرحلة 1 — حمل الكلمات المجهولة في القراءة: مُغلقة على المستويات الأربعة** بمعايير وسيط ≤25% ولا نص فوق 45%.
  الأرقام: A1 ‏21.6 · A2 ‏19.0 · B1 ‏23.9 · B2 ‏23.3، الوسيط العام 22.7، أعلى نص 39.1 (b2-07)، ونصوص فوق 45% صفر.
  الدفعات: 1 عند v133 (4 دروس)، 2 عند v136 (53 ترجمة)، 3 عند v137 (76 ترجمة على 16 درسًا).
  يبقى اختياريًا **الدفعة الرابعة**: الذيل الستة b2-07 ‏39.1 وb2-01 ‏38.2 وb1-23 ‏36.5 وb1-12 ‏35.4 وa1-04 ‏35.2 وb2-12 ‏35.0،
 خفضها يحتاج مراسي أسماء بقرار موثق، لا حشوًا. لا تُسمَّ مُغلقة ولا مُنجزة.
- **المرحلة 2 — صرامة الإنتاج الحر: مُغلقة في نطاق ADR-078**. 197 من 387 تمرينًا ببديل واحد مقبول = 50.9% مقابل سقف 25%،
  والبوابة حمراء **عمدًا**: `npm run lessons:variant-worklist` (مقيس عند v151 وأُعيد قياسه عند v152) يرجع **0 قابلة للإضافة بثقة** من
  الـ197 — بالنوع 159 فراغًا بكلمة واحدة و36 ترتيب كتل و2 تصحيح؛ ولو رُفعت الـ38 كلها لبقي **41.1%** مقابل سقف 25%.
  فالبند قرار تعريف/تصميم عند المالك (P1-398 لا يُغلق تلقائيًا ولا تُرخى سقفه).
- **المرحلة 3 — بلغَت سقفها**: تحويل مشتتات الخيارات أنزل القرينة إلى **39.28%** (‏491 من 1,250) عند v149؛
  البند يبقى مفتوحًا على الملاحظتين الأخريين للبوابة المشتركة، لا على الطول. انظر §5.
- **المرحلة 4 — الشروح، وهي العمل الجاري**: على مستوى التمارين أولًا. بعد **تسع** دفعات (آخرها v155.1 بهذا الدور) بقي على مستوى التمارين ‏**A2 ‏24 ثم A1 ‏167**
  (الباقي في `miniTest` وأسئلة المراحل، والمجموعُ على القائمة الأوسع **1,154** من 1,733)، ويُعلن الإغلاق لاحقًا
  فقط** مع ذكر أن 987 سطرًا في أسئلة المراحل ما تزال مفتوحة؛ ولا يُنقل P1-399 إلى implemented قبل أن يبلغ
  **وسيط الشرح 60** (الآن 26).
- **المرحلة 5 — خارطة طريق دراسية للمالك** تُعرض عليه بصفته دارسًا منفردًا (جدول أسبوعي، تسلسل الوحدات، معايير الانتقال،
  محاكاة الامتحان، ما الذي لن يفعله التطبيق).

## 5) أول بند مفتوح: نفّذه فورًا إن قال المالك «واصل»

**الترتيب الجاري بعد v155**: أول بند مفتوح هو **الشرح تحت 60 حرفًا** (P1-399؛ **1,178** بندًا تحت الستين من 1,733،
منها **24** تمرينًا في A2 و**167** في A1). الدفعات تُختار **من جماعة الوسيط** (`multiple-choice` + أسئلة القراءة +
الاستماع + `miniTest` = 1,250 بندًا، منها **1,039** تحت 60) لا من قائمة التدقيق الأوسع: دفعةُ الجماعة حرّكت المسافة
**24** مرّتين (‏462 ⇒ 438 ⇒ **414 = 18 دفعة**) ورفعت وسيطَ الجماعة 25 ⇒ **26**، بينما دفعةٌ من القائمة الأوسع (v153)
حرّكتها **2 فقط**. أقصرُ ما بقي **14 حرفًا** (‏`a2-01-m3`، `a2-03-lq3`، `a2-05-m1`)، والدفعة التالية تُنزل المسافة إلى
**390**. تُدفع دفعات ‏24 بندًا بأدوات
`scratch/` (§5-أدوات الشرح أدناه)، ثم يأتي **تحرير المشتتات** (P1-397) عملًا
اختياريًا لأن السقف مُتحقَّق عند 39.28%. ما يُروى في هذا الفصل عن القرينة تاريخٌ مُثبَّت، لا قائمة عمل.

قرينة الطول كانت 55.2% ثم 52.08% عند v138/v139، ف48.16% (‏602) عند v140، ف46.16% (‏577) عند v143 بعد `b2-11` و`b1-20`،
ف44.4% (‏555) عند v144 بعد `b1-07` و`b1-12`، ثمّ 42.8% (‏535) عند v145 بعد 20 عنصرًا في `b1-16` و`b1-22` (60 مشتتة)،
ف41.92% (‏524) عند v146 بعد `b2-05`، و41.04% (‏513) عند v147 بعد `b2-13`، و40.16% (‏502) عند v148 بعد `b2-17`
(11 عنصرًا + إصلاح `b2-17-m2` بحكم الحارس = 12 مجموعة و36 مشتتًا)، **ف491 = 39.28% عند v149** بعد `b2-19` (11 عنصرًا
و33 مشتتة، وقبلتها الأداة من المحاولة الأولى لأن التأليف حاكى منطق `cue-batch.py`). **سقف ≤500 اُسْتُوفي إذن**،
ولا يُغلَق البند: البوابة المشتركة `lesson:quality:audit` خروجها 1 بملاحظتي الإنتاجية أحادية السلسلة (50.9% مقابل
25%) ووسيط الشرح (26 مقابل 60 بعد دفعة v155)، وكلتاهما خارج نطاق الطول؛ فهما الشغل التالي في المرحلتين 2 و4. أي دفعة موازة
إضافية الآن اختيارية (لا درس فوق عشرة عناصر قرينة)، وأقربها `a2-06` و`b2-09` و`b2-20` بعشرة عناصر لكلٍّ.

b2-12 (‏13 عنصرًا) ثم b1-05 وb2-07 وb2-08 (‏12 لكل منها) ثم b1-07 وb1-12 وb1-16 وb1-22 وb2-05 وb2-13 وb2-17 وb2-19
(‏11 لكل منها) — أي نحو اثني عشر درسًا.

**القاعدة التي طُبِّقت، وهي مقصودة**: تُعاد كتابة **المشتتات** لتوازي المفتاح لغةً وبنيةً وطولًا. لا تُحشو كلمة في مشتت
ليتعادل الطول، ولا يُجعَل المفتاح الأقصر (تلك قرينة معكوسة). لكل عنصر محرَّر: المفتاح ليس الأطول وحده ولا الأقصر وحده،
وفارق الأطوال ≤25 حرفًا، ومجموعة الخيارات بلغة واحدة، وصفر تكرار بعد الطي. نص المفتاح وموضعه وشرح الطالب لا تُمَسَّ
إلا إذا كان الشرح يسمّي مشتتًا قديمًا بالحرف.

**الأداة**: `python3 scripts/parallelise-options.py --batch <json> [--write]` — الدُفعة JSON بصيغة
`{ "<lessonId>": { "<itemId>": ["A","B","C","D"] } }`. locate `id:` → next `options:` → قوس بوعي اقتباس → استبدال جسم
المصفوفة فقط. ترفض الكتابة إن لم يكن المفتاح مطابقًا حرفيًا في موضعه أو خولفت أي قاعدة أعلاه.
ثم أضف معرّف الدرس إلى `PARALLELISED` في `tests/unit/mcq-option-parallelism.test.ts` (الحارس هو ما يجعل القاعدة دائمة).
المِسبار (probe): `npx tsx scripts/probe-option-cue.ts <ids>` لطباعة العناصر والانحراف وأطوال الخيارات.
الأداةان داخل المستودع لا في مجلد مؤقت خارجي، فتُشحنان مع الأرشيف.

**أدواتُ الدفعات كلها داخل المستودع الآن** (كانت خارجَه في `/home/user/scratch/` و`/home/user/tmp`): شروحُ A2 بـ`scripts/apply-explanation-batch.py --level a2 --payload docs/run-logs/a2-explanations-payloads/<a2eN>.json [--dry-run|--data-root DIR]` (قفلُ `oldLen`، ورفضُ مستوى بلا دروس، وعدُّ الحقول)؛ المشتتاتُ بـ`scripts/parallelise-options.py --batch <json> [--write]` (قفلُ «المفتاح يبقى حرفيًّا عند `correctIndex`»، ويرفض الأطولَ/الأقصرَ وحدَه والتوسّعَ فوق 25، ويرفض الكتابةَ كلها إن بقيت مشكلة)؛ بناءُ الحمولةِ من المسبار بـ`scripts/cue-batch.py <items.jsonl> <authored.json> <lessonId> <out.json>`؛ الوسمُ بـ`scripts/bump-pack-generation.py`؛ والشجرةُ التمرينيةُ بـ`scripts/recover-src-data.mjs <probe|restore|backup|verify>`؛ ومسبارُ التفاصيل `npx tsx docs/run-logs/2026-09-22-a2-explanations-10/expl_detail.ts <id,id,…>` (يعيد كائنَ البند الكامل **ونصَّ مصدرِ الدرس** حتى تُقتبس الجملةُ الفعلية) ونسخةُ مسابير الجماعة في `docs/run-logs/2026-09-21-a2-explanations-9/{pick_median,measure9,medlevel9,suffix9}.ts` بـ`npx tsx` **لا** `node --experimental-strip-types`؛ و**فاحصُ أطوالٍ محليٌّ قبل الكتابة** يقيس الوسيطَ المتوقع لكلَّ مستوى قبل أن تُرفَع الحمولةُ إلى المالك (انظر §1-ط في `docs/LESSON-QUALITY-FINDINGS-2026-09-17.md`)؛ والصفحةُ التي تُستدعى في المسبار ما تزال `lessons-a2.ts:1094` بوسم `A2 Erklärungen #9`.
التطبيق** بترتيب: المدى المحريري 156–212، ثم العبارة الموضعية بـ`scripts/rebalance-answer-positions.ts:21`
(مطابقة جزئية: «الأولى» و«الثاني» ممنوعتان)، ثم تسمية الحالة من `meaning-first-case.ts:5-7`، ثم تكرار النصوص.
الشرح الذي يسمّي حالة يُثبَّت في `discoveredSignals` فاردِّد التثبيت عند اللزوم (فُعِل في v151: ‏21 ⇒ 20؛ وفي v152 هبطت الإشارة إلى 19 حين حذفت صياغةٌ «بالمعنى فقط» اسمَي الحالتين من شرح `a2-04-e3`، فأُعيد النصّ مع الحالتين مسمّيتَين داخل جملة المعنى نفسها فرجعت إلى 20 — لا بتخفيف الحارس ولا بتغيير الرقم المتوقع). ونماذج الدفعات السابقة محفوظة كمرجع صيغة في
`docs/run-logs/2026-09-19-cue-parallelism-1/cue-b1-15.json` و`cue-b2-01.json` و`cue-b2-02.json`.

## 6) حلقة التحقق الإلزامية لأي دفعة محتوى (بهذا الترتيب)

1. `./node_modules/.bin/tsc --noEmit` يجب أن يكون صفرًا. لا تقرأ أي تقرير قبل ذلك.
2. `npm run lesson:quality` ثم بقية التدقيقات بـ`--write`: `content:audit:write` · `language:audit:write` ·
   `lexical:strategy:audit:write` · `study:modes:audit:write` · `similarity:audit:write` · `case:audit:write` ·
   `governance:audit:write` · `learning:architecture:audit:write` · `review:packet`.
3. ارفع كاش الحزمة (تمت v154 → v155 عند هذا الجيل) في **سبعة** ملفات: `public/sw.js` (ثلاثة أسطر)،
   `scripts/verify-continuation-handoff.mjs` (سطرا `PACK_CACHE`/staging وثابت ‏`cacheName !== "dwnb-full-pack-vNNN"`)،
   `tests/e2e/critical-flows.spec.ts` (ثلاثة مواضع `caches.open(...)`),
   `tests/unit/offline-pack-controls.test.ts`، `tests/unit/offline-recovery-partial-export.test.ts`،
   `tests/unit/today-offline-readiness.test.ts`، و`tests/unit/offline-curriculum-rollback.test.ts` (يثبت `previous-vNNN`).
   ابحث بالاسمين معًا: `grep -rl "dwnb-full-pack-v149\|staging-v148\|previous-v148"` لا بالاسم الحالي وحده.
4. البناء: `NODE_OPTIONS=--max-old-space-size=900 npm run build` — **بعد** تدفئة tsc كما قِيس في v151 وv152 (الصندوق 1,984 ميغابايت وبلا swap). **درسَان من v152**: أولًا أن `prebuild` يرفض البناء إن كانت `reports/` أو `docs/generated/` أقدَم من `src/data` بخطأ `Academic audit artifacts are not current` — فبعد أي كتابة على المحتوى شغّل سلسلة التوليد (`npm run content:audit:write` ورفاقها، أو `bash /home/user/tmp/writes.sh`) **قبل** البناء لا بعده. ثانيًا أن بناء v152 توقّف عند `Running TypeScript …  Failed to type check.` **بلا خطأ واحد مطبوع** بينما `tsc --noEmit` المستقل يخرج صفرًا في 26 ثانية: تلك إشارة إجهاد ذاكرة/بطء صريح لا فشل أنواع؛ الحلّ إعادة البناء بعد التسخين (نجح من المحاولة الثانية: Compiled في 12.4s و321/321 في 9.6s)، ولا يُقبل «فشل بلا رسالة» ولا يُصلَح بإسكات الفحص. (الصندوق بلا
   سوابل): شغّل `npm run build` حتى سطر `Compiled successfully` ثم أوقفه (فهذا يولّد `.next/types`) ⇒
   `node --max-old-space-size=900 node_modules/typescript/bin/tsc --project tsconfig.json --noEmit
   --declarationMap false --emitDeclarationOnly false --tsBuildInfoFile .next/cache/.tsbuildinfo` (‏26 ثانية، خروج 0) ⇒
   `npm run build` من جديد (خروج 0 في نحو 100 ثانية). التدفئة **قبل** وجود `.next/types` لا تكفي: بصمة البرنامج لا
   تطابق فتتعثر «Running TypeScript» وتنفرذ الذاكرة. لا تلمس `next.config` ولا تعطّل فحص الأنواع.
5. البوابات: `npm run --silent offline:size` ثم `npm run --silent js:budget` ثم `npm run --silent media:budget`.
   ثبِّت الأرقام التي تُطبع، لا التي تتوقعها.
6. `npx vitest run` كامل. الأرقام تتغير مع المحتوى (حجم فهرس البحث، تعداد الفجوات، البصمات) فأعد تثبيتها في
   `tests/unit/search.test.ts` و`tests/unit/lexical-target-gap.test.ts` و
   `tests/unit/academic-content-governance.test.ts` وفي `:630` و`:631` و`:633` من سكربت فحص التسليم — وفي
   `:456` و`:457` و`1066` حين يتغير عدد الاختبارات أو الملفات أو نتيجة المتصفح. لا تُصلِح الفشل بتخفيض السقف.
7. `rm -rf test-results playwright-report` ثم `npm run lint`.
8. المتصفح الكامل: شغّل `npx playwright test --project=chromium` (المكتبات التسعة التي كانت ناقصة صارت مثبّتة بـ `sudo apt-get install -y --no-install-recommends libnspr4 libnss3 libxdamage1 libasound2 libatk1.0-0t64 libatk-bridge2.0-0t64 libatspi2.0-0t64 libxkbcommon0 libcups2t64`، فلا حاجة إلى `LD_LIBRARY_PATH` ولا إلى فكّ حزَم يدوي). قبل أي إعادة تشغيل تأكد أن منفذ 3100 خالٍ: `ss -ltnp | grep 3100` ثم اقتل `next-server` اليتيم، وإلا رفض playwright بمنفذ مستعمل (هذا ما أحرّق محاولتين). لا تُشغّل الحزمتين معًا على صندوق بذاكرة 2 غيغابايت: انتظار تثبيت الحزمة الكاملة يفشل بالاختناق.
   تحويل الخرج إلى سجل الدفعة و`echo "PLAYWRIGHT_EXIT=$?"` في نهاية نفس السطر. الاختبار الكامل يستغرق 14–21 دقيقة.
9. أعد تثبيت الأرقام في `PROJECT_STATUS.md` و`README.md` و`docs/AGENT-HANDOFF-PROMPT-AR.md` و
   `PROFESSIONAL_CONTINUATION_PROMPT_AR.md`، ثم `npm run --silent handoff:check` يجب أن يخرج صفرًا.
   ثم حدِّث الوثائق الثلاث عشرة بأرقامها البائتة وارفع سطر `Sync batch` فيها إلى الجيل الجديد (ADR-079).
   **مسرد المزامنة الذي فُعِل في v151 وأُعيد في v152**: المسح على 16 ملفًا (وثيقة الاحتياط دخلت المسح هذه المرة ثم سُمِّيت)، مع تخطي
   مواضع التاريخ: `## 40)` في البرمت الرئيسي، و`دفعة شروحات A2 الثالثة` في `PROJECT_STATUS.md`، و`## دفعة v150 —` في
   `DECISIONS.md`، و`## 4t)` في وثيقة النتائج، وسطر 397 في `P1_AUDIT.md`؛ ثم **فحص ما لم يُدفَع**: `grep -rln v150
   --include="*.md" .` خارج `docs/run-logs/` يجب أن يُرجع مواضع التاريخ هذه وحدها، وإلا كان المسح قد دفع رقمًا بائتًا
   سطر `## ` منه)؛ وتسمية نسخة الاحتياط الجديدة وحذف القديمة في خطوة مستقلة مقاسة بـ`ls`.
10. `cd /home/user && bash make-zip.sh` ثم تحقّق داخل الأرشيف (sw.js، ملف درس محرَّر، السجل، البصمة في `PROJECT_STATUS.md`).

## 7) تجهيز البيئة بعد كل حدّ جلسة (يتكرر ولا بد منه)

لقطة مساحة العمل لا تحفظ: `node_modules`، `~/.cache/ms-playwright`، `~/.pwlibs`، `/swapfile`. الصندوق نواتان و1,984 ميجابايت.

1. `cd /home/user/der-weg-nach-berlin && npm ci --no-audit --no-fund` (‏471 حزمة، ~16 ثانية).
2. المبادلة: **مرفوضة في هذه البيئة** منذ v149 — `fallocate` و`mkswap` ينجحان لكن `/usr/sbin/swapon` يرجع
   `Operation not permitted`. لذلك يجري البناء بالتسلسل المقيس في §6.4 (تدفئة tsc بعد `.next/types` ثم سقف 900
   ميغابايت). لا تُضيِّع دورًا في محاولة `sudo swapon` ثانية.
3. المتصفح: `npx playwright install chromium` (يُنزِّل chromium و‏headless shell في دقائق)، ثم أمر المكتبات في الفقرة 8. لا حاجة إلى `~/.pwlibs` ولا إلى `deb.debian.org` بعد أن صار `apt-get` يعمل في هذا الصندوق.
   `~/.pwlibs/root` بـ`dpkg -x` مع `-A "Mozilla/5.0"` لـcurl (بلا UA تُرجع فهرسًا فارغًا):
   nspr، nss، atk1.0-0t64، atk-bridge2.0-0t64، atspi2.0-0t64، cups2، libxkbcommon0، alsa-lib (libasound2)،
   libxdamage1، avahi (libavahi-common3 وlibavahi-client3). **عائلة at-spi يجب أن تكون من سطر واحد**
   (‏2.56.2-1+deb13u2 للأسماء الثلاثة) وإلا فشل chromium بـ`undefined symbol: atk_object_get_help_text`.
   انتبه: علامة + في grep التمددي تعني تكرار ما قبلها، فلا تطابق الأصدارات التي تحمل `+deb13u2`؛ استعمل `grep -F` لها.
   تحقّق: `LD_LIBRARY_PATH=~/.pwlibs/root/usr/lib/x86_64-linux-gnu ldd ~/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome | grep -c "not found"`
   يجب أن يكون صفرًا، ثم شغّل `--version`.
4. تشخيص «Failed to type check.»: هذه الرسالة لا تفرّق بين نفاد الذاكرة وخطأ نوع حقيقي. الفصل بـ
   `./node_modules/.bin/tsc --noEmit` منفردًا (24–30 ثانية، ~500 ميجابايت): إن أعطى أسطرًا فالخطأ حقيقي،
   وإن أعطى صفرًا فالمسألة ذاكرة. ممنوع `typescript.ignoreBuildErrors` أو تعديل tsconfig أو `--no-lint`.
6. **الاستعادة بعد تراجع اللقطة** (حدث **سبع** مرات حتى 2026-09-21 — والسادسة والسابعة مسَّتا أرشيفَ التسليم نفسه: `node_modules` و`.next` صفر، `src/data` إلى جيل أقدم، مجلداتُ `docs/run-logs/` مفرَّغة، وقد يُستبدل أرشيفُ التسليم بنسخةٍ ملفوفة قديمة بينما يُبقي sidecar بصمةَ الجيل الجديد): الترتيب المقيس — `npm ci --no-audit` ← `git clone --depth 1 https://github.com/naderba69/wegberlin.git` ثم `cp -a …/src/data/. src/data/` ← **`node scripts/recover-src-data.mjs probe`** ليقيسَ fitness الشجرة (79 ملفًا هنا و`exercises/curriculum/micro-drills = missing` ⇒ لا تسليم) ويبحثَ عن نسخةٍ كاملة أو مرآةٍ قبل أن تستنتجَ أنك تستعيد ← **`restore --from <zip> --apply`** عند وجودها ← إعادةُ تطبيق الحمولات **الموجودة في المستودع**: شروحُ A2 بالترتيب `a2e1…a2e9` (+`a2e5-fix1`)، ثم دفعاتُ القرينة/الموازاة من `docs/run-logs/*cue-parallelism*/*-batch.json` (**نُفِّذت في 2026-09-22**: 113 بندًا/7 ملفات فأخضرَّت الفحوصاتُ الثمانية وعادت العداداتُ إلى 4,874/1,381/3,404)؛ وإن غابَ `tests/unit/delivery-layout.test.ts` (أعدمه التراجع عند v155) فأعِد كتابته قبل التغليف، وتحقَّق بـ`sha256sum -c` **عند التسليم** لا قبله بدقائق ← **إعادةُ القياس وعدم تصديق أي تقرير نجا من التراجع**: `lesson:quality` + سكان الوسيط + `npx tsc --noEmit` + `npx vitest run tests/unit` + `npm run handoff:check` ← حرفيةُ الكاش في السبعة ملفات بـ`scripts/bump-pack-generation.py` وثلاثةَ عشرَ وثيقةً على `Sync batch: vNNN` ← `writes.sh`. **ملاحظةُ ذاكرةٍ توفّر دورًا**: `test -e /home/user/.swapfile || sudo -n fallocate -l 2G … mkswap … swapon` قبل أي بناءٍ على هذا الصندوق.
7. **لا تحذف `.next` قبل البناء بلا تدفئة**: على هذا الصندوق (1,984 ميغابايت) `rm -rf .next` ثم `npm run build` يفشل بصامتٍ عند `Running TypeScript` برسالة «Failed to type check.» بلا أسطر خطأ، بينما `tsc --noEmit` منفردًا يعطي صفرًا. الترتيب المقيس: `./node_modules/.bin/tsc --noEmit --incremental --tsBuildInfoFile .next/cache/.tsbuildinfo` ثم بناءٌ واحد بـ`NODE_OPTIONS=--max-old-space-size=900`، ولا مسبار `tsx` يعمل معه.
9. **تدفئةُ الأنواع لا تكفي أحيانًا**: قيسَ v155 أن `npm run build` الكامل (17 تدقيقًا في `prebuild` + webpack + فحص أنواع) يفشل عند «Running TypeScript» بعد ~20 دقيقة **رغم** تدفئة `.tsbuildinfo`، بينما `tsc --noEmit` منفردًا يعطي صفرًا في 8 ثوانٍ. الحلّ المقيس: نفِّذ التدقيقات مرةً واحدة، ثم `NODE_OPTIONS=--max-old-space-size=1200 ./node_modules/.bin/next build --webpack` منفردًا (خرج 0 في 70 ثانية، 321/321 صفحة)، ثم `npm run offline:size && npm run js:budget && npm run media:budget` يدويًا. لا `typescript.ignoreBuildErrors` ولا `--no-lint`.
10. **فاحصا البنية عند التسليم**: `npm run archive:delivery` يبني الأرشيف من جذر المشروع ويرفض مجلد الظرف ويفرض `package.json` و`src/app/page.tsx` و`src/app/layout.tsx` و`public/sw.js` في الجذر، ويكتب `wegberlin-full.zip.sha256` ويعيد التحقق منه ومن `unzip -t`؛ ثم افحص مستقلًّا: `unzip -Z1 wegberlin-full.zip | grep -c "^src/app/"` يجب أن يكون > 0، و`unzip -Z1 wegberlin-full.zip | awk -F/ '{print $1}' | sort | uniq -c` يجب أن يُظهر `src`/`public`/`docs` لا `der-weg-nach-berlin`.
11. لا تقتل عمليات المنصة بأنماط تطابق سطر أوامرك (`pkill -f jupyter` قتلت شللي). اقتل برقم العملية من `ps`.

## 8) قواعد تحرير بيانات الدروس (دُفعت ثمنها)

- لا تُقطع محتويات مصفوفة لتُعاد ملفوفة: v136 أسقطت `}`، وv137 أسقطت 16 قوسًا `[` فاندلع 1,573 سطر خطأ في تسعة عشر
  ملفًا. الصيغة الآمنة: إدراج النص مباشرة بعد `[` المتعرَّف عليه، بلا مطابقة أقواس ولا إعادة تنسيق.
- الاستعادة من `wegberlin-full.zip` أرشيف الدفعة السابقة أفضل من ترميز الإصلاح: `unzip` ثم `cp` للملفات المتضررة
  (أسماء العضويات بلا `./`).
- لا تستخرج `textDe` بـregex: بعض الدروس تكتبه مقاطع متصلة فتقرأ نصف نص. اقرأ من `academicLessonList` المجمَّع.
- لا تربط عنصرًا بمعرّف درس حسب الترتيب الظاهري؛ حدّد نطاق الدرس بعلامة تبدأ بسطر جديد ثم `id: "bX-NN"`، وافحص التكرار داخل ذلك النطاق لا في الملف كله.
- الجذر المطوي لا يساوي الصورة السطحية للكلمة؛ فلا تقارنهما مباشرة. وترجمة أسماء العلم مضيعة.
- في `reading.glossary` أي `lemma` بحرف كبير يُعدّ مرشح اسم فينفتح سطر في قائمة البشر؛ الأفعال والصفات بلا مراسي آمنة.
- بعد أي كتابة عربية على الملفات: تحقّق أنها خالية من الحروف الصينية/اليابانية/الكورية،
  وأن مسافات علامات الترقيم سليمة (فشل مرارًا: «علىقرص»، «المطلوس»، «كمامسح»).
- `echo exit=$?` بعد أنبوب يكذب؛ استخدم `${PIPESTATUS[0]}`. و`npx tsc` طُعم: شغّل `./node_modules/.bin/tsc`.
- لا تُشغِّل التدفئة والبناء في سطر واحد (`tsc ... && npm run build` أو بـ`&`)؛ كلاهما ثقيل والذاكرة 1,984 ميغابايت،
  فافصل بينهما واقس `free -m` بعد كل خطوة.
- إذا انخفض `discoveredSignals.controlled` لأن شرحًا كفّ تسمية الحالة، فاردد التثبيت في
  `tests/unit/meaning-first-case.test.ts` وحده مع سطر صريح في سجل الدفعة، ولا تعد كتابة الشرح.

## 9) قواعد الرد بالعربية (طلب مالك صريح)

- نثر قصير مباشر. لا جداول ولا أنابيب ولا أقواس معشّكة.
- الأرقام داخل الجُمل عادية، والمعرّفات اللاتينية (`offline:size`، `handoff:check`) في سطر مستقل أو آخر الجملة لا في منتصفها.
- رقْم ما قِيس، وسمِّ البوابة التي قاسته. لا تنقل رقمًا من دفعة سابقة دون قوله «من vNNN».
- الأخطاء تُذكر كما وقعت: ما الذي انكسر، لماذا، وما البديل المقاس. لا «تمّ» للمؤجَّل.

## 10) ملفات تُقرأ قبل أي قرار

`PROJECT_STATUS.md` (الكتلة الحالية ~السطر 250 والبصمات) · `docs/AGENT-HANDOFF-PROMPT-AR.md` (الحالة الجارية وقائمة
«ما لم يُغلق») · `PROFESSIONAL_CONTINUATION_PROMPT_AR.md` (§1–§26 و§28–§41، القواعد الطويلة) ·
`docs/LESSON-QUALITY-FINDINGS-2026-09-17.md` (آخر فصل قياس عند v152 هو `## 4v)`) · `DECISIONS.md` · `P0_AUDIT.md` و`P1_AUDIT.md` و`P2_AUDIT.md` ·
`docs/adr/ADR-076…ADR-078` · آخر ثلاث سجلات في `docs/run-logs/` (وهي: `2026-09-20-a2-explanations-4` ثم
`2026-09-20-a2-explanations-3` ثم `2026-09-20-cue-parallelism-b2-19`). لا يوجد `.git`؛ المراجع هي الملفات والسجلات.

## 11) قواعد التوقف

- دفعة مغلقة = قياس أخضر + توثيق مُثبَّت بالأرقام المقيسة + أرشيف مُعاد بناؤه. عدّا ذلك «مفتوح».
- إذا خرجت بوابة بلون لا يعجبك: أصلِح المحتوى، لا العتبة ولا الإسكات. إن تعذّر الإصلاح في هذه الجلسة، اكتب الحالة
  «مفتوحة عند X%» برقمها.
- لا تُغلق بندًا تلقائيًا لمجرد أن السكربت أخضر. ولا تُعلن «اكتمل المشروع» قبل شروط الجاهزية السبعة.
- إن نفد الوقت أو الذاكرة: أختم بدفعة مكتملة صغيرة مُوثَّقة، وسجّل في `docs/run-logs/` وفي سجل البنود ما بقي بالضبط.

في v152 صار النصّ نفسه بعد دفعة شروحات A2 الخامسة: 24 شرحًا (29–32 حرفًا ⇒ **180–212**) بلا لمس للخيارات ولا للمفاتيح، فبقيت القرينة عند **39.28%** (491 من 1,250) تحت سقف ≤500 و≤40%، ونزلت `feedback.allUnder60Chars` من 1,274 إلى **1,250** من 1,733، ونزلت `feedback.under40Chars` من 1,031 إلى **1,021** (عشرة من الأربعة والعشرين كانت اختيارًا من متعدد)، وبقي `allMedianChars` عند 30 و`explanationMedianChars` عند 25، وصارت تمارين A2 عند **120/168** (المتبقي A2 ‏48 ثم A1 ‏167؛ 48 + 167 + 1,035 = 1,250 ✓). والمسافة إلى وسيط 60 مقيسة: 1,089 تحت الستين من 1,250 مُقيَّمًا ⇒ رفع **464** بندًا = **20** دفعة. وأُعيد قياس P1-398: 197 أحادي السلسلة من 387 و**0 قابلة للإضافة بثقة** (159 فراغًا و36 ترتيبًا و2 تصحيح)، فلو رُفعت البنود الـ38 لبقي 41.1% مقابل سقف 25% — القرار تعريفُ المؤشر أو إعادة تصميم الفراغات، وهو قرار مالك لا يُتَّخذ بتخفيف العتبة. البوابة المشتركة `lesson:quality:audit` ما تزال تخرج 1 بملاحظتين، فلا يُغلق P1-397 ولا P1-398 ولا P1-399، ولا يُقال عن البوابة خضراء. القياسات: **1,019** اختبارًا في **155** ملفًا (لا جديد) · tsc صفر · **lint صفر** بعد إصلاح ثلاثة أخطاء `any` في ملف مؤرشف · `case:audit` صفر فجوات ببصمة ‏167645dd621b (لم تتحرك) · بصمة المحتوى **d0c2079d038f** (تحرّكت لأن النصوص تغيّرت) · البناء خروج 0 (321 صفحة، SSG ‏9.6s، بصمة `offline:size` **c7f80adb201a** مع full 5,661,245، js ‏110/1,729,585/251,309، المنهاج 948,946 gzip) · `handoff:check` صفر. **حادثة هذا الجيل**: استُعيدت 15 ملف درس من أرشيف v151 بعد انقلاب الشجرة إلى حالة أقدم، والتدقيق لا يكتب بلا `--write` فلا تُصدَّق `reports/` قبل سطر `Wrote reports/...`. **عقد التعليم الموقّع هذا الجلسة** في `DECISIONS.md` (ADR-080) وفصله في `PROFESSIONAL_CONTINUATION_PROMPT_AR.md` تحت §42: تسعون دقيقة يوميًا، وشهادة B2 عامة (الجهة غير محسومة ⇒ telc افتراضًا في قالب المحاكاة حتى التحقق من المصدر)، والتمريض طبقة منسوجة «توابل» في A1/A2 و«طبق ثانٍ» من B1، بقواعد: لا نحو جديدًا من أجل الطبقة، ولا جواب لغوي معلَّق بمعرفة سريرية، والطبقة بارasitasية بقاطع رجوع؛ وكل وحدة تمريضية تقول «هذه صياغات لغوية لا إرشاد سريري». لا متصفح لهذا الجيل؛ آخر قياس متصفح مرجعي v143 (40 من 42). ملاحظة على هذا الملف: كل رقم فيه قِيس بالبوابات بعد الدفعة؛ إن تعارض رقم هنا مع `reports/` فالأخير هو المرجع. آخر تحديث: 2026-09-20 (v152).

**ابدأ الآن بقراءة §5 ونفّذه إن كانت الكلمة «واصل». وإن سأل المالك سؤالًا: أجب بالعربية بقواعد §9 بلا كود.**
