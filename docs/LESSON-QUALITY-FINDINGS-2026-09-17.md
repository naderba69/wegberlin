# تقرير نقائص الدروس المقيس — 2026-09-17

مسبار مستقل عن البوابات الحالية (`content:audit` يفحص البنية والتسريب والحالات؛ لا يفحص جودة
التغذية الراجعة ولا انحياز مواضع الإجابة ولا التزام النموذج بعدده). أُعيد تشغيله على 96 درسًا
(2,496 بندًا و‏1,250 سؤال اختيار من متعدد) في `defect-probe.ts` و`probe2.ts` مع `npx tsx`.
كل رقم هنا من مخرج الأمر، لا من تقدير.

## 1. نقائص مؤكَّدة (مرتبة بخطرها على ادّعاء «معلّم رسمي وحيد»)

| # | النقصة | القياس | الأثر التعليمي |
|---:|---|---|---|
| 1 | نموذج الكتابة لا يلتزم بالعدد المذكور في مطلوبه | **81 من 96 درسًا** (B2: 13). b2-01: النموذج 128 كلمة مقابل «Schreiben Sie 200–230 Wörter» · a1-01: 26 مقابل «30–40 Wörter» | المتعلّم يقلّد النموذج فيسقط في معيار الطول في الامتحان الحقيقي. هذا أصعب عيب لأن النصّ *يُعلّم طريقة غلط*. |
| 2 | انحياز موضع الإجابة الصحيحة | من 1,250 بندًا: A 466 · B 665 · C 114 · **D 5** (‏0.40%)، و**91 من 96 درسًا** لا تستخدم D إطلاقًا | يمكن رفع النتيجة بالتخمين المنهجي؛ النتيجة لم تعد مقيسًا صالحًا للإتقان. |
| 3 | تلميح «الأطول = الأصح» | الخيار الأطول هو الصحيح في **55.2%** إجمالًا و**76.4%** في B2 (المصادفة 25%) | تسريب شكلي للإجابة؛ يرفع الإيجابيات الكاذبة في مؤشرات الجاهزية. |
| 4 | صرامة التصحيح الإنتاجي — **نطاقها تصغّر بعد التصفية** | **201 من 387** تمرينًا إنتاجيًا (51.9%) له سلسلة مقبولة واحدة. مسبار `scripts/generate-accepted-answer-worklist.ts` صنّف الـ201 كلها: 147 جملة قصيرة لا بديل ترتيب لها، 33 نصًا فرعيًا/مجموعة مصدر بترتيب وحيد نحويًا، 17 بلا جرّة قابلة للتقديم، 4 اقتراحات مرفوضة ⇒ **0 قابل للإضافة بثقة آلية**. وسياسة «البدائل المصرَّح بها فقط» مُختبَرة عمدًا (‏`content-integrity.test.ts:77-78`) | العمل الحقيقي على **21 بندًا** لا 201، مع توثيق السياسة في `docs/adr/ADR-078-…` بدل تخفيف المُصحِّح. |
| 5 | فقر التغذية الراجعة | وسيط طول `explanationAr` ‏**23 حرفًا** عبر المستويات، و**1,134 من 1,250** بندًا أقل من 40 حرفًا («بيانات محددة.») | لا يعرف المتعلّم *لماذا* أخطأ؛ لا يمكن أن يستغني بمعلّم. |
| 6 | أقصر من تحمل الامتحان في الاستماع | وسيط المقطع: A1 ‏74 كلمة (≈25 ث) · B1 ‏98 (≈34 ث) · **B2 147 (≈50 ث)** بثابت 175 كلمة/دقيقة | لا تدريب على التحمل السمعي المطلوب في telc/Goethe B2، ولا مقاطع متعددة الأسلوب. |
| 7 | حمل معجمي ثقيل في النصوص | الكلمات غير المغطاة في نص القراءة (بعد احتساب glossary والمطابقة بالجذر): وسيط **49.7%** في B2 (أقصى 61.4% في b2-09) · A1 30.6% | النص ليس "مدخلًا مفهومًا": أكثر من نصف المحتوى المعجمي جديد بلا جسر. (سقف تقريبي؛ التطبيع الصرفي بدائي.) |
| 8 | «أهداف غير متتبَّعة» — **تبيّن أنها artefact قياس لا نقصة محتوى** | المسبار الأول flagى **37 هدفًا**؛ وبعد توسيع corpus القياس (شاملًا `entry/dialogue` وقبول أي كلمة محتوى بدل >4 حروف) صار **19** (A1 11 · A2 5 · B1 2 · B2 1). فُحص 8 منها يدويًا: **كلها مُدرَّبة ومقيسة** بأمثلتها (a1-04 بـ Mutter/Vater/Bruder، a1-18 بـ Wetter/Regen/Sonne، a1-19 بـ Fahrkarte/Zug، b2-22#4 عبر `b2-22-e8` والمثال الأخير في `t4`) لأن الهدف يسمّي الفئة والدرس يسمّي الأفراد | أُخرج البند من العتبات وأُعيدت تسميته `lexicalReviewPrompts`؛ المطلوب جعل التتبّع دلاليًا (قاموس فئات) لا معجميًا |
| 9 | تغطية صوتية جزئية | 6 عناصر `ipa` لكل درس مقابل 18 عبارة ⇒ **33%**، و‏213 سطر معجم فونيمات على 96 درسًا | الجزء الأكبر من العبارات بلا هدف نطق محدد — مع أن تقييم الفونيم أصلًا 0 من 9 خطوات دليل. |

## 1b. الحالة المقاسة بعد دفعتي b2-25 وb2-26 (2026-09-18)

الأرقام أعلاه لقطة 2026-09-17؛ هذا ما يقيسه المسبار الآن (`npm run lesson:quality`، و‏`reports/lesson-quality-audit.json`):

| النقصة |القياس الأول (09-17) | الآن (09-18، b2-26) | ما تغير |
|---|---:|---:|---|
| 1 · نماذج خارج النطاق | 81 | **68** (B2 ‏0) | 13 نموذج B2 أُلحقت بنطاقها المطلوب دون حشو؛ المتبقي A1 20 · A2 24 · B1 24 |
| 2 · انحياز الموضع | 91 درسًا بلا D | **3** دروس بلا D | تدوير بحراسات (‏931 بندًا) مع الحفاظ على الشروح الموضعية والترقيم |
| 3 · تلميح الأطول | 55.2% | **55.2%** | لم يُعالَج: يحتاج إعادة كتابة المشتتات، لا تدويرًا |
| 4 · إجابة وحيدة | 201 بندًا | **21** (و‏0 قابلة للإضافة الآلية) | المسبار الجديد صنّف الـ201؛ أغلبها ليس عيبًا أصلًا (‏147 جملة قصيرة، 33 نصًا فرعيًا) |
| 8 · «أهداف غير متتبَّعة» | 19 (بعد تصحيح المقياس) | **19** مؤشر غير معالج | 8 فُحصت يدويًا وكلها مُدرَّبة؛ المطلوب تتبّع دلالي لا تعديل دروس |
| 5 · شروحات قصيرة | وسيط 23 | وسيط 23 على الاختيار من متعدد (‏1,118 تحت 40 من 1,250)؛ **ولكل البنود 1,733: وسيط 25 و1,614 تحت 60 و0 بلا شرح** | أُصلحت 69 بندًا في B2 بشرح سببي؛ القياس كان أضيق من الحقيقة وصار أوسع وأصدق |

السياسة: لا يُقلَّب أي مؤشرات «ناجحة» لأن القياس صار أسهل؛ المؤشر الذي ثبت أنه يقيس شيئًا خاطئًا يُصحَّح تعريفه ويوثَّق (حالتا 4 و8)، والمؤشر الذي كشف عملًا حقيقيًا يبقى عتبةً فاشلة حتى يُنفَّذ (1، 3، 5).



## 2. ما استُبعد بعد التحقق (لا يُروى كعيب)

- «خيارات مكررة داخل السؤال» (b2-23-e2 · b1-13-e1 · a2-04-m5): تباين حالة/تقطيع **عمدي** لتدريب
  الإملاء وكتابة «Ihnen» الكبرى؛ القياس كان artefact من تطبيعي للنص.
- «بطاقة مثالها لا يحوي الكلمة» (75/74/67/115): نتيجة خاطئة لاستخراج الجذر عندي — مثال
  `b2-01-card-2` («eine These formulieren» → «Die These muss begrenzt sein.») سليم.
- «سلاسل لاتينية داخل الحقول العربية» في 96 درسًا: هي المصطلحات الألمانية المقصودة. بند P2-384
  يخص نصوص واجهة محددة، لا محتوى الدروس.

## 3. لماذا لا تكفي هذه الأرقام وحدها لإعلان الجاهزية الرسمية

البوابات خضراء (**985/985** وحدة، **82/82** متصفح، `check` خروج 0، 321/321 صفحة) لأنها تقيس
البنية والاتساق والأمان، لا الجودة البيداغوجية. وللاعتبارات التالية:

- **0 من 3,277** سجلًا محتوى له مراجعة بشرية مستقلة مكتملة؛ و**89** مرشح اسم و**4** مرشحات إطار
  فعل و**8** استبعادات هيكلية ما زالت *pending*.
- **0 من 96** مقطع استماع بصوت بشري؛ كلها TTS آلي بصيغة 24 kHz mono — لا تساوي صوت الامتحان.
- تقييم النطق: **0 من 9** خطوات دليل ⇒ لا درجة فونيم صالحة، ولا «طلاقة» مؤتمتة موثوقة.
- الكتابة: تصحيح قواعد آلي + Gemini **مستشار اختياري** خلف موافقة، بلا مُصحِّح بشري مدرَّب.
- جزء *Sprechen* في الامتحان تفاعلي (شريك + ممتحِن): لا تُغني عنه واجهة تطبيق منفردة.
- صيغ telc/Goethe مثبَّتة مقابل لقطات مؤرَّخة؛ **35** حقيقة مثبَّتة و**فجوتان مصرَّح بهما**،
  وتوزيع نقاط telc بلا مصدر رسمي؛ HTTP 200 لا يثبت ثبات الصيغة.
- قارئات الشاشة غير مغطاة باختبار آلي (P1-331 · P2-264) والمراجعات الحقوقية/السياسات (P1-377) مفتوحة.
- لا نشر من هذه البيئة: لا `.git`، وآخر دفع موثّق `be56463e` (2026-09-04) ولا يُعاد ادعاؤه.

## 3b. البوابة صارت موجودة (هذا الدور)

`scripts/generate-lesson-quality-audit.ts` + `npm run lesson:quality` (تقرير) و`npm run lesson:quality:audit`
( صارم، **يفشل اليوم** بخروج 1 و7 انحرافات موثّقة في `reports/lesson-quality-audit.json`). تُوصَّل بـ
`npm run check` بعد إغلاق P1-397/398/399 وP2-400/401، لا قبلها: بوابة حمراء في `check` تُعلِّم الفريق
تعطيل البوابة بدل إصلاح البيانات.

## 4. ما يمكن إصلاحه آليًا في الدور القادم (دفعة «تقوية الجودة»)

1. إعادة توازن `correctIndex` إلى ≈25% لكل موضع مع كسر نمط الأطول، لكل 1,250 بندًا، ببذرة ثابتة.
2. **لا** يُمسّ `normalizeGermanText`/`compareAccepted`: رفض البدائل غير المصرَّح بها سياسة مُختبَرة.
   الإصلاح في البيانات: توسيع `acceptedAnswers` يدويًا لكل بند من الـ201 (بدائل ترتيب/Artikel/ترقيم)،
   مع ADR يوثّق السياسة الحالية صراحةً (هي اليوم مُختبَرة بلا وثيقة قرار).
3. رفع نماذج الكتابة الـ81 إلى نطاقها المعلن (تأليف محتوى فعلي، لا ترقيم).
4. شروحات ≥60 حرفًا لكل بند مُصحَّح، مع سبب القواعد لا تسمية القاعدة.
5. ~~سدّ 37 هدفًا~~ → تبيّن أن البند artefact: المطلوب قاموس فئات يجعل التتبّع دلاليًا، لا تعديل دروس (فُحص 8 من 19 يدويًا فكلها مُدرَّبة).
6. بوابة `npm run lesson:quality:audit` (فشل عند انحراف أيٍّ من (1)–(5)) — **شُحِّنت في هذا الدور**: `scripts/generate-lesson-quality-audit.ts`، وتقريرها `reports/lesson-quality-audit.json` يضع 7 انحرافات حالية، لذلك تُوصَّل بـ `npm run check` بعد الإصلاح لا قبله (بوابة حمراء ثابتة تُعلِّم تعطيل البوابة).

بند الاستماع (6) كان محجوبًا بالهامش: **606,157 بايتًا** تحت سقف 63,000,000 (ADR-076). قرّر المالك في هذا الدور
رفع السقف إلى **70,000,000** (ADR-077) فصار الهامش **6,556,157 بايتًا** (+23.3% من صوت الدروس). هذا يكفي تمديدًا
جزئيًا لا كاملًا: تمديد B2 وحده من 147 إلى ≈230 كلمة يكلّف نحو 9.2 MB، فيُنفَّذ بترميز Opus ‏20 kb/s للملفات المُعاد
توليدها فقط (بلا مسّ 92 ملفًا مُسلَّمًا)، أو بتمديد B1+B2 بدل 96 درسًا، أو برفع إضافي لاحقًا بقرار جديد.

## 1c) إعادة قياس 2026-09-18 — الطبقة أُغلقت

- `npm run lesson:quality` الآن: **writing-range violations 0** على **92** مهمة كتابة لها نطاق معلن (كانت **68** مخالفة عند تحرير هذا التقرير).
- ما فُعِل: A1 ‏20 ثم A2 ‏24 ثم B1 ‏24 نموذجًا مُدِّدت بجمل مؤلَّفة من متطلبات معلنة غير مغطاة، والإدراج قبل صيغة الختام، ومطابقة منتصف النطاق.
- أوساط أطوال النماذج بعد الإغلاق: A1 ‏56.8 (28–77) · A2 ‏115.3 (89–135) · B1 ‏182.5 (163–205) · B2 ‏212.5 (170–248).
- فحوص الرفض التي أبقيت: لا حشو (0 جملة مكررة داخل النماذج المعدَّلة)، ولا إعادة صياغة (0 إضافة تشارك ≥0.6 من محتوى جُمل أصلها)، ولا تكرار عابر للدرسَين خارج الصيغ الثابتة (رقم الهاتف فُرِّق؛ بقيت جملتان صيغتيتان قدماء لا علاقة لهما بهذه الدفعة).
- ما زال مفتوحًا من هذا التقرير: وسيط الشرح **23** حرفًا و**1,614** بندًا تحت 60 من **1,733**، وتلميح «الأطول = الأصح» ‏**55.2%**، والإجابات الوحيدة ‏**51.94%**، ومراجعة بشرية **0/3,277**.

## 1d) إعادة قياس 2026-09-18 (2) — طبقة الشروحات تحرّكت أول مرة

- بعد تأليف 48 شرحًا في تمارين B1: `allUnder60Chars` ‏**1,614 → 1,566**، و`allMedianChars` ‏25 → 26، و`under40Chars` (مقياس الاختيار من متعدد) ‏1,118 → **1,099**، وبقي `allMissing` ‏0.
- الحصر الأدق الذي يجب اعتماده من الآن: 1,566 بندًا ليست كلها تمارين. على مستوى `lesson.exercises` وحده: B1 ‏**120** · A1 ‏167 · A2 ‏168 · B2 ‏76 = **531**؛ والباقي (**1,035**) في أسئلة القراءة والاستماع و`miniTest`. هذا يفصل بين «عمل تأليف تمارين» و«عمل تأليف أسئلة مراحل» ولا يخلط الحجمين.
- B1 كاملة كانت تحت الحد: 168 تمرينًا، 48 منها الآن ≥60 حرفًا وأطولها 211، و120 ما زالت 15–43 حرفًا.
- ما لم يتغير: تلميح «الأطول = الأصح» ‏55.2%، والإجابات الوحيدة ‏51.94%، و19 هدفًا بلا تتبّع معجمي (مؤشر مراجعة)، والمراجعة البشرية ‏0/3,277.

## 1e) إعادة قياس 2026-09-18 (3) — B1 إلى النصف

- بعد 48 شرحًا ثانيًا: `allUnder60Chars` ‏**1,518** · `allMedianChars` ‏27 · `under40Chars` ‏1,087 · `allMissing` ‏0.
- تمارين B1: 168، منها **96** الآن فوق الحد و**72** تحتها. أطوال الشروحات المؤلَّفة 156–212 حرفًا، و11 منها قُصّت بعد أن تجاوزت الحد، وقُلعت كلمة إنجليزية أجنبية من شرح واحد.
- فصل الأحجام مقياسًا: المتبقي في التمارين 483 (B1 ‏72 · B2 ‏76 · A2 ‏168 · A1 ‏167) والباقي **1,035** في أسئلة القراءة والاستماع و`miniTest`. لا يصحّ أن يُروَّج عمل المراحل عملًا على التمارين أو العكس.
- التكلفة الجانبية الموثَّقة: إنقاص الأسماء الإعرابية من الشروحات خفّض `discoveredSignals.controlled` ‏28 → 26، وهي إشارة حيازة الدرس للحالة، لا عيب؛ عولجت بإعادة تثبيت الاختبار لا بتخفيف المُدقِّق.

### 1f — Dfinite close of level B1 (batch 4, 2026-09-18)

72 explanations rewritten in four tranches (24 + 12 + 12 + 24), all in the 156–212 character house style (rule → shape of the wrong alternative → «فحص ذاتي»).

| measure | before | after |
| --- | --- | --- |
| `feedback.allUnder60Chars` (1,733 items) | 1,518 | **1,446** |
| `feedback.under40Chars` (1,250 items) | 1,087 | 1,070 |
| B1 exercises with a full explanation | 96 / 168 | **168 / 168** |
| B1 explanations under 156 or over 212 chars | 11 | 0 |
| positional cue tokens inside `explanationAr` (B1, ≥60 chars) | — | 0 |
| case names without a contract (B1) | — | 0 |
| duplicated explanations (B1, ≥60 chars) | 0 | 0 |
| `discoveredSignals.controlled` | 26 | 24 |

`allMedianChars` stayed at 27: with 1,733 items and 1,446 still short, a 72-item tranche moves the median, not the tail. The absolute delta is the honest signal (−72 this batch, −48 per 48-item batch before it).

Two explanations that used to name a grammatical case were rewritten without case metalanguage, which is why `case:audit` dropped `controlled` from 26 to 24; the count is the only figure I can stand behind, because the superseded texts are not kept. After this batch the count of B1 `explanationAr` strings naming a case is 0 (measured over `academicLessonList`), while the same data files still carry 33 case terms in fields that own a case contract (`theoryBlocks`, prompts, options) — those are legal and untouched. The pinned expectation in `tests/unit/meaning-first-case.test.ts` was moved to 24 — the auditor itself was not relaxed.

Remaining exercise-level work is B2 76 · A2 168 · A1 167; the other 1,035 short strings live in reading/listening/`miniTest` stage items, which the same style can serve once the exercise levels are closed.

### 1g — B2 closed at exercise level, plus a real bundle-side fix (2026-09-19)

76 B2 exercise explanations were rewritten in three tranches (24 + 24 + 28) in the same house style.

| measure | before | after |
| --- | --- | --- |
| `feedback.allUnder60Chars` (1,733 items) | 1,446 | **1,370** |
| `feedback.under40Chars` (1,250 items) | 1,070 | 1,062 |
| B2 exercises with a full explanation | 97 / 173 | **173 / 173** |
| B1 exercises with a full explanation | 168 / 168 | 168 / 168 |
| new texts outside 156–212 chars | — | 0 |
| positional tokens / case names / duplicates in the new texts | — | 0 / 0 / 0 |
| `discoveredSignals.controlled` | 24 | 23 |

`b2-09-e4` used to read `mit + Dativ`; the rewrite states the endings without case metalanguage, so the case auditor discovered one less controlled signal. The pinned number in `tests/unit/meaning-first-case.test.ts` was moved to 23; the auditor still reports `ok: true` with 0 issues.

Pre-existing and intentionally untouched: 16 B2 explanations keep positional wording (`b2-24-e3`, `b2-21-e8`, …) because they are the guarded items from the answer-position rebalance — rotating them would contradict their own text. One B2 explanation sits at 220 chars (`b2-22-e7`), above the editorial band but above every gate.

**Bundle consequence, measured and fixed.** Adding 148 explanations across two turns grew the shared curriculum chunk past the ADR-049 JS safety ceiling: `maxSingleChunkGzipBytes` was 642,417 against a ceiling of 637,500 (hard limit 750,000). `npm run build` failed on `js:budget`. The fix was structural, not a policy edit: `next.config.ts` now splits `src/data/lessons-<level>-module*` into one chunk per level, so the largest emitted chunk is **243,237** gzip bytes and total chunks are 110 / 1,705,576. A new unit test `tests/unit/curriculum-chunk-split.test.ts` pins both the split and the untouched budget script, so neither can be quietly undone. Offline packs grew slightly because HTML now references four more chunks: full pack 5,590,306 gzip (was 5,532,239).

Remaining exercise-level work: A2 168 · A1 167; the 1,035 stage-item strings are unchanged.
### 1h — A2 opens: 48 exercise explanations in two tranches (batch 6, 2026-09-19)

48 A2 exercise explanations were rewritten (24 + 24) in the same house style — rule, the shape of the wrong alternative, then a «فحص ذاتي» line — at the A2 register: one named construction, no grammar vocabulary beyond the word the exercise itself uses.

| measure | before (v131) | after (v132) |
| --- | --- | --- |
| `feedback.allUnder60Chars` (1,733 items) | 1,370 | **1,322** |
| `feedback.under40Chars` (1,250 items) | 1,062 | 1,047 |
| `allMedianChars` | 27 | 28 |
| A2 exercises with a full explanation | 0 / 168 | **48 / 168** |
| new texts outside 156–212 chars | — | 0 (min 156, max 212) |
| positional tokens / case names / duplicates in the new texts | — | 0 / 0 / 0 |
| `discoveredSignals.controlled` | 23 | **21** |

The case auditor moved twice this batch (23 → 22 → 21): `a2-04-e4` no longer says `in + Dativ` and `a2-23-e7` no longer says `Nominativ`, both rewritten as endings and word order. The pin in `tests/unit/meaning-first-case.test.ts` was moved to 21; `case:audit` still reports `ok: true` with 0 issues.

Two process notes worth repeating. First, the positional guard fires on ordinals and on the word inside a noun phrase: «الجملة الأولى» and «الطرف الثاني» were rejected for `الأول` / `الثاني`, so four candidate texts in tranche 2 never reached the data files — the spec was re-worded, not the guard. Second, the band is measured after applying, not before: one text at 220 chars was trimmed to 212 in the spec and re-applied.

Build incident, recorded rather than hidden: the first `npm run build` for v132 never finished — it sat in "Running TypeScript" with 14 MB free and had to be killed. Re-run with `NODE_OPTIONS=--max-old-space-size=768` it completed in 9.0 s: 321/321 pages, fingerprint `0a0bfdd2c01f`, full pack 5,603,006 gzip. Lesson files, gate and cache contracts are unchanged by that retry; the log of the killed attempt stays in the run-log directory (`build.log`).

Gates on v132: `tsc` 0 · vitest **151 files / 988 tests** · lint 0 · `handoff:check` 0 · Playwright 6/6 (1.2 min) plus the full-pack offline test 2/2 (56.9 s, chromium and mobile-chromium). Content SHA `cd4e9b124b6b0741…4b9b418f`; `review:packet` 3,277 records / 17 sheets / 196 flagged / 1,104 unresolved / 16 scopes.

Remaining exercise-level work: **A2 120 · A1 167**; the 1,035 reading/listening/`miniTest` stage strings are unchanged, as is condition 3 (longest cue 55.2 % against a ≤40 % target) and the 0/3,277 human review.
### 1i — القياس صُحِّح أولًا، ثم عولِج حمل القراءة (دفعة 2026-09-19)

بدأت الدفعة بمقياس لا بمحتوى. في `scripts/generate-lesson-quality-audit.ts` كان `fold()` يفتح بـ
`normalize("NFKD")`، فتتفكك كل لوترة إلى حرف وعلامة، ثم تُستبدل العلامة بمسافة. سطور استبدال
ä/ö/ü/ß كانت بالتالي ميّتة، وكانت الكلمة الواحدة تُعدّ كلمتين في موضعين: حمل القراءة وتتبّع الأهداف.
بعد الإصلاح (NFC، استبدال صريح، ثم تجريد؛ واستبعاد الرموز الرقمية لأن الأرقام ليست مفردات) صار
`reading.medianUnknownWordPct` **44.9%** بالتعريف القديم نفسه بدل 42.0 — أي أن العيب كان يُخفّف
الرقم لا يُضخّمه. هذه نقطة أمانة: تصحيح القياس رفع المعلَنة عن العيب قبل أن تُعالَج.

ثانيًا: التعريف v2. الكلمة مجهولة فقط إن لم يعلّمها درس في المستوى أو ما دونه. التعريف v1 كان يقارن
نص B2 بمواد B2 وحدها. النتيجة المقاسة بعد الإصلاحين: وسيط عام 32.4%، وأوساط A1 32.5 وA2 27.6 وB1 32.9
وB2 34.1، و4 نصوص فوق 45%، وأقصى 50.0%.

ثالثًا: العلاج بالمحتوى. ‏109 إدخالًا في `reading.glossary` لأربعة دروس هي الأسوأ (`a1-02`، `b1-01`،
`b2-06`، `b2-09`)، و53 مرساة اسم (جنس وجمع ومعنى) لأن شرح اسم بلا مرساة ينقله المُدقِّع المعجمي إلى
طابور القرار البشري. النتائج: النصوص الأربعة أصبحت عند 18.0% و22.6% و24.8% و27.3%، والوسيط العام
**29.8%** (A1 28.9 · B1 31.6 · B2 30.5)، وعدد النصوص فوق 45% **صفر**، وأقصى نص 42.4% في `b1-23`.
طابور `pending-human` بقي **89** بعد أن قفز مؤقتًا إلى 142، والمغطى 1,320 → 1,376، والمراسات
1,244 → 1,297، وجذر Zod 4,292 → 4,345، وفهرس البحث 3,512 → 3,750.

ولم يُجعَل حمل القراءة ختمًا ملزمًا الآن: بدون مُلمْعِز يخلط المقياس تصريفَ معلَّم (spricht/sprechen،
Satz/Sätze) بمفردة جديدة، فيصرخ الختم بلا سبب. المطلوب أولًا مقياسٌ بمرجع صرفي، وهذا بند مفتوح صريح.

### 1j. دفعة البدائل الإنتاجية رقم 2 — نُفِّذت في 2026-09-19 (v134)

**ما الذي تغيّر فعليًا**: أربعة تمارين إنتاجية صار لها بدائل صريحة مُدرَجة، فانتقلت
الإجابات الوحيدة من 201 إلى **197** من 387، أي من 51.9% إلى **50.9%**. البنود:
`b1-06-e3` (+1)، `b2-14-e3` (+1)، `b2-15-e4` (+1)، `b2-24-e4` (+2). في كل بند ذُكر البديل
داخل `explanationAr` في السطر نفسه، وبقي الشرح داخل نطاق 156–212 حرفًا بلا مصطلح إعرابي
ولا إشارة إلى مواضع الخيارات.

**تصحيح تقديري السابق**: قدرتُ أن إغلاق البند يحتاج ~104 بنودًا. التصنيف المسجَّل في
ADR-078 كان يقول غير ذلك: من الـ201 هناك 147 جملة قصيرة لا عنصر ثانٍ يُقدَّم فيها، و33
ترتيبها مربوط بمجموعة المصدر أو بالنص الفرعي، و17 بلا جرّة خلفية قابلة للتقديم، و4 يفكك
تقديمُها رصيد الرموز، و**صفر** ما يُضاف بثقة آلية. فالنطاق القابل للتنفيذ 21 بندًا، وبعد
هذه الدفعة بقي **17**، وكلها قرار مؤلِّف لا حيلة لها. لذلك تبقى البوابة حمراء: السقف 25%
لا يُبلَغ بمئات البدائل المصنوعة، بل بإعادة كتابة التمارين نفسها.

**ما رُفض**: 159 مرشحًا من نوع «املأ الفراغ» (أداته `ohne` مقابل `mit`، `keinen` مقابل
`einen`، `der` مقابل `einer`) — البدائل فيها تغيّر المعنى أو تُسقط البنية المُعلَّمة؛
و`b2-16-e4` لأن بدائله كلها تبدأ بـ `als ob` وتُبقي الجملة غير طبيعية.

**كيف ثُبِّت القبول**: عبر `evaluateExercise` الحقيقي (لا إعادة كتابة للمنطق): الإجابة
الأصلية مقبولة، والبديل الجديد مقبول، وصيغة السحب والإفلات بحروف صغيرة مقبولة، والخطأ
الشائع ما زال مرفوضًا لكل بند. تقرير `reports/accepted-answer-worklist.md` أُعيد توليده
بالأداة نفسها.

**ما لم يُنفَّذ بعد**: الدفعة الثانية من حمل القراءة (`b1-23` ‏42.4% و`b2-07` ‏41.1%
وغيرهما) مؤجَّلة عمدًا لصالح هذه الدفعة، وتحرير المشتتات (55.2%) لم يبدأ، والاختبار
الحامي ضد تعليم خيار طويل كمفتاح يبقى مقترحًا لم يُنفَّذ. التفاصيل في
`docs/run-logs/2026-09-19-productive-variants-2/README.md`.

### 1k — دفعة حمل القراءة 2: الوسيطان المفتوحان أُغلقا بـ53 ترجمة (2026-09-19)

**الهدف المُعلَن**: وسيط ≤25% لكل مستوى ولا نص فوق 45%. قبل الدفعة: 28.9 / 27.6 / 31.6 / 30.5،
و73 درسًا فوق 25% من 96. الدفعة السابقة عالجت أسوأ النصوص فحركت الحد الأعلى ولم تحرك الوسيط؛
هذه الدفعة اختيرت من النطاق القريب من 25% لأن الوسيط هو رقم الصف الثالث عشر من أربعة وعشرين.

**ما قُبل**: الإدراج في `reading.glossary` فقط — وعاء المعرفة المعلَّمة عند البوابة. 53 إدخالًا:
a1-12 ‏3 · a1-17 ‏4 · a1-20 ‏5 · a1-21 ‏4 · a1-22 ‏6 · a2-04 ‏8 · a2-11 ‏2 · a2-14 ‏5 · a2-19 ‏12 · a2-24 ‏4.
كلها أفعال وصفات وظروف وضمائر وأعداد. كل `surfaceForm` منطوقًا كما ورد في `textDe` (لا اشتقاق
افتراضي)، الشرح العربي يعطي معنى السياق لا القاموس المجرد، ولا إدخال يتكرر داخل الدرس.

**ما رُفض صراحةً**:
- تضييع القائمة الموقوفة: إخراج الأسماء العلم (Nora ، Leila ، Dresden) وأيام الأسبوع من العدّ يُسقط
  الوسوط كلها بلا أن يتعلم المتعلم كلمة. البوابة تُقرأ كما هي.
- مطابقة جذر فضفاضة (`sagt` ≈ `sagen`): القياس أعطى 28.9 / 27.1 / 30.8 / 29.5 — نصف نقطة إلى نقطة
  واحدة، أي أن الكلمات المُصرَّفة غير موجودة فعلًا في القوائم، لا أنها خطأ قياس.
- توسيع الوعاء ليشمل نصوص التمارين والاستماع: يجعل الشرط أخضر آليًا. القاعدة المُختبَرة أن
  المفردات تُدرَج صراحة في قائمة المفردات، ولها بطاقات مراجعة؛ من يريدها يوثّق ADRًا مستقلًا.
- إضافة مراسي أسماء في نفس الدفعة: lemma بحرف كبير في `reading.glossary` يدخل `pending-human` بلا
  مرسى، والمرسى يضيف اسمًا إلى لوحة «Weitere Zielnomen» (a1-04 عنده 39 اسمًا إضافيًا فعلًا). أسماء
  B1/B2 عمل مستقل بمقايضته الواضحة، لا حشو في دفعة قراءة.

**القياس بعد**: A1 ‏21.6 (من 28.9) · A2 ‏19.0 (من 27.6) · B1 ‏29.7 · B2 ‏28.6 · الوسيط العام 26.9 ·
أعلى نص 41.2 · نصوص فوق 45% ‏0 · `lessonsOver` العشرة المتبقية فوق 25% كلها من B1/B2 أو من أعلام
A1 (a1-04 ‏35.2، a1-05 الذي خرج من القمة بفضل الأثر التراكمي لقوائم A1). A1 وA2 **أغلقا المعيار**.

**الأداة**: `scripts/generate-lesson-quality-audit.ts` صار يكتب `reading.unknownByLesson` — لكل درس
فوق 25% قائمة كلماته المجهولة المطوية (حتى 16 درسًا و16 كلمة). لا تغيير في العتبة ولا في `fold`.

**الخلل الذي حدث أثناء الإدراج** (مفيد لتكرار المحاولة): أول قطع للأقواس اعتمد أن آخر مدخل في
`glossary` هو بالضبط `…}]`، فزحزح قوسًا وأنتج `}}]` ناقصًا — كشفه المترجم لا الاختبارات. والمهم
أكثر: في هذه البيئة غاب `node_modules/.bin` لفترة فأصبح `npx tsc` ينزّل حزمة اسمها `tsc` من npm
بدل مترجم المشروع ويعطي انطباع نجاح كاذب؛ الفحص الحقيقي `./node_modules/.bin/tsc --noEmit`.

**الأرقام المصاحبة**: البناء 321/321، بصمة `offline:size` ‏10e6f1fe9fd3، الحزمة الكاملة 5,613,982
gzip (+2,989 عن v135)، `js:budget` ‏1,714,162 (+989)، المنهاج الصوتي 934,300، الحاكِم 3,277 سجلًا
بلا تغيير، ودقيق الجودة ما يزال `fail` بثلاثة إنذارات: التلميح 55.2%، أحادية البديل 50.9%، وسيط
الشرح 24 حرفًا. حمل القراءة لم يعد من بينها.

### 1l — إغلاق معيار القراءة على المستويات الأربعة (دفعة 3، v137)

**الحالة قبلها**: A1 ‏21.6 وA2 ‏19.0 مغلقان؛ B1 ‏29.7 وB2 ‏28.6 مفتوحان، بتسعة عشر درسًا فوق 25% في كل
منهما. المعيار وسيطٌ، فالعمل على الذيل لا يُغلقه.

**ما نُفِّذ**: 76 ترجمة في `reading.glossary` على ستة عشر درسًا من النطاق القريب من العتبة.
B1: b1-03 · b1-04 · b1-13 · b1-15 · b1-17 · b1-18 · b1-19 · b1-24. B2: b2-03 · b2-09 · b2-13 ·
b2-16 · b2-18 · b2-20 · b2-21 · b2-22. كل المدخلات أفعال وصفات وظروف وضمائر، وكل `surfaceForm`
مأخوذ حرفيًا من `textDe` المجمَّع لا من تقدير اشتقاقي.

**النتيجة**: B1 ‏29.7 ⇒ 23.9 وB2 ‏28.6 ⇒ 23.3، والوسيط العام 26.9 ⇒ 22.7، وأعلى نص 41.2 ⇒ 39.1،
ونصوص فوق 45% صفر. دروس فوق 25%: ‏11 في B1 و11 في B2 (كان 19+19)، و10 في A1 و9 في A2.
**معيار المرحلة الأولى مُغلق على A1/A2/B1/B2**.

**صفر أثر على قرارات البشر**: `nounSummary` بقي 4,685 / ‏1,376 / ‏89 / ‏3,220 و`verbFrameSummary`
بقي 1,180 / ‏134 / ‏4 / ‏1,042 — مطابقًا حرفيًا لحالة v135 المقاسة من أرشيف التسليم. السبب أن
القاعدة تعدّ `lemma` بحرف كبير في قاموس القراءة مرشحَ اسم؛ وباستخدام أفعال فقط لا يفتح أي بند.

**الأداة**: أُلغي سقف 16 درسًا في `reading.unknownByLesson` (صار يسرد الـ57 درسًا فوق العتبة).
السقف كان يُظهر الذيل فقط بينما الدفعة تُخطِّط للنطاق القريب من العتبة — أي أن الأداة كانت
تُخفي ما يلزم عمله بالضبط.

**قاعدة بيانات مكتسبة (بعد كسرين)**: لا تُقطع محتويات مصفوفة لتُعاد ملفوفة. v136 أسقطت `}`
من آخر مدخل، وهذه الدفعة أسقطت `[` الافتتاحي فاندلع 1,573 سطر خطأ في تسعة عشر ملفًا. الصيغة
الآمنة: إدراج النص الجديد مباشرة بعد `[` المتعرَّف عليه ببحث بسيط، بلا مطابقة أقواس ولا
`rstrip` لمحتوى. ومعه: استخراج النصوص من `academicLessonList` المجمَّع، لأن بعض الدروس تكتب
`textDe` كمقاطع متصلة فتقرأها Regex نصف نص — وهذا تحديدًا ما جعل ثلاث دفعات تُنسَب إلى دروس
خطأ قبل أن يرفضها الفحص.

**البيئة**: نواتان و1,984 ميجابايت. `next build` قُتل ثلاث مرات داخل فحص الأنواع (heap OOM عند
768 و1024 و1400، وإخفاق صامت عند 768 مع ضغط ذاكرة). فُعِّل ملف مبادلة 4 غيغابايت فنجح البناء
321/321 في نفس المحاولة التالية. لا عتبة cُدِّلت ولا `ignoreBuildErrors` ولا إسكات فحص: الفحص
قام مستقلًا (`tsc --noEmit` = صفر أخطاء، 24 ثانية) وقام داخل البناء بعد توفير الذاكرة.

**الذيل يبقى مفتوحًا بملء الإرادة**: b2-07 ‏39.1، b2-01 ‏38.2، b1-23 ‏36.5، b1-12 ‏35.4، b2-12 ‏35.0،
a1-04 ‏35.2 — مركّبات وأعلام أسماء. خفضها يحتاج مراسي أسماء (وبلوحة أسماء أطول)، وهو قرار موثق
لا حشو لخفض رقم. ولا شيء من هذا يُغلق أي بند جاهزية: البوابة ما تزال `fail` بثلاثة إنذارات
(تلميح 55.2%، أحادية البديل 50.9%، وسيط شرح 24 حرفًا).

### 1m — كسر قرينة الطول في الخيارات (توازٍ 1، v138)

**المقياس قبل الدفعة**: في 55.2% من 1,250 عنصر اختيار يكون المفتاح هو الأطول وحده. بالتوزيع: B2 ‏76.4% وB1 ‏70.8%
وA2 ‏47.1% وA1 ‏26.3%، والأخطر في أسئلة القراءة (‏71.2%) ثم الاستماع (‏59.7%). الفجوة ≥16 حرفًا في 321 عنصرًا، أي أن
البنية معتادة لا استثنائية: مفتاح جملة كاملة ومشتتات مقاطع قصيرة أو كلمات عربية.

**ما نُفِّذ**: ثلاثة دروس كاملة (b1-15 ‏13، b2-01 ‏13، b2-02 ‏13) — 117 مشتتة مكتوبة من جديد بألمانية على وزن المفتاح.
لم يُمَسَّ نص المفتاح ولا موضعه ولا تعليمة السؤال ولا شرح الطالب (الشروح هنا معيارية لا تعدد المشتتات، فبقيت صادقة).
مشتتات عنصرَي الترتيب (b2-02-m2/m3) صارت أزواجًا صغرى من نفس الجملة بأخطاء ترتيب مختلفة — أصفى قياسيًا وأطوالها متطابقة.

**النتيجة**: ‏55.2 ⇒ **52.08%** (‏690 ⇒ 651 عنصرًا). المواضع كما هي (324/298/305/323)؛ لم تُلمَس عتبة ولا أُسكِت فحص.
المرحلة الثالثة **مفتوحة**: الحد 40%، ويبقى نحو اثني عشر درسًا.

**الأداة**: `scratch/cuefix.py`. تبحث عن `id:` العنصر، ثم عن `options:` التالية لها، وتفحص الأقواس بوعي الاقتباس
وتستبدل جسم المصفوفة فقط، وترفض إن لم يكن المفتاح مطابقًا حرفيًا في موضعه، أو إن بقي الأطول وحده أو صار الأقصر وحده،
أو تجاوز فارق الأطوال 25، أو تكرر خيار، أو دخلت حروف صينية/عبرانية. هذه القاعدة آخر ما تعلمناه من إفساد 9 ملفات في
الدفعة السابقة، وقد منعت هذه المرة كسرًا آخر: الحارس اكتشف أن b1-15-lq3 صار مفتاحه الأقصر وحده فأُصلح قبل القياس.

**الأثر على الفجوات المعجمية (لا يُخفى)**: المُرشِّحات تقرأ نصوص الخيارات، فزاد تعداد مرشحي الأسماء ‏4,685 ⇒ 4,717
والسياقية ‏3,220 ⇒ 3,251 والمغطاة ‏1,376 ⇒ 1,377، وأطر الأفعال ‏1,180 ⇒ 1,200. لم يزد `pendingHuman` (‏89 و4) لأن
أسماء المشتتات لا تُسجَّل كمداخل `lemma` في القاموس، والمراسي بقيت ‏1,297/134. أي: لوحة قرارات البشر لم تنتفخ، وتغيّرت
أرقام الجرد فقط، وأُعيد تثبيتها في `lexical-target-gap.test.ts` و`academic-content-governance.test.ts` وفي `:630` و`:633`
من `scripts/verify-continuation-handoff.mjs`، لا في نص التاريخ.

**خلل أُصلح عرضًا**: خيار في b1-15-lq3 كان يحمل أربعة حروف عبرانية ملتحمة بذيل جملة ألمانية وسط مزيج عربي/ألماني. مُسحت الحروف، وأُضيف مسح وقائي لكل الخيارات في الحارس الجديد فلا تتكرر.
وأُضيف مسح وقائي لكل الخيارات في الحارس الجديد فلا تتكرر بصمت.

### 1n) دفعة موازة الخيارات الثانية — 2026-09-20، v140

**القياس بعد الدفعة**: قرينة «الخيار الأطول = الأصح» **602 من 1,250 = 48.16%** (كانت 651 = 52.08%).
توزيع المواضع الموضوعة يدويًا بقي كما هو: ‏A 55 · B 446 · C 348 · D 301.

**الصفوف الأربعة**: ‏`b2-12` (‏13 من 13) و‏`b1-05` (‏12 من 13) و‏`b2-07` (‏12 من 13) و‏`b2-08` (‏12 من 13)،
أي **49 عنصرًا**. لكل عنصر كُتبت ثلاثة تشتيتات ألمانية جديدة: واحد أطول من المفتاح بحرفين إلى أربعة،
وواحد أقصر كذلك، والثالث داخل نافذة تباعد لا تتجاوز 25 حرفًا، والكل جمل تامة لا كلمات مفردة.
لم يُمسّ نص المفتاح ولا الإجابة الصحيحة ولا موضعها.

**ما لم يكن متوقعًا**: بإدراج ‏`b2-07` و‏`b2-08` في قائمة ‏`PARALLELISED` صار الحارس يفحص **كل** عناصر الدرس
لا عناصر القرينة وحدها، فرفض عنصرين في اختبارين مصغّرين لم يوردهما المسبار أصلًا: أحدهما بتباعد 32 حرفًا
(خُفّض إلى 16) وثانيهما خيار ألماني يحمل كلمة عربية ملتصقة داخله (بُدّل بتشتيت ألماني). لم يُخفَّ الحارس؛
بُلِغ الخللان وأُصلحا.

**الأثر المعجمي الموثق**: عدّاد ‏`scripts/lesson-lexical-gap.mjs` يُبنى على نصوص الخيارات، فتحرّكت أرقامه إلزامًا:
المرشحات 4,717 ⇒ **4,802**، والمغطى 1,377 ⇒ **1,380**، والسياقي 3,251 ⇒ **3,333**، والأهداف المكتوبة 1,466 ⇒ **1,469**،
وأطر الأفعال 1,200 ⇒ **1,233** (خارج الهدف 1,062 ⇒ **1,095**). أُعيد تثبيتها في
‏`tests/unit/lexical-target-gap.test.ts` و‏`tests/unit/academic-content-governance.test.ts` وفي أسطر الفاحص وكل الوثائق الجارية،
وبصمة المحتوى الجديدة ‏`e9c471b4046a`.

**المفتوح بعد الدفعة**: 102 بندًا للوصول إلى ≤40% (تقدير المسبار: 9 دروس). أكبر ثلاثة صفوف متبقية:
‏`b2-11` (‏13 من 13، وسيط الفارق 27 حرفًا) ثم ‏`b1-20` (‏12 من 13) ثم ‏`b1-07` (‏11 من 13).
بقيت السجلات اليدوية عند 0/3,277، وأعلام الاستماع مطفأة، ولا أثر لهذه الدفعة في أي قياس مسموع.

## 3n) قياس دفعة v143 — `b2-11` و`b1-20` (2026-09-20)

بعد تأليف 75 مشتتًا ألمانيًا في الدرسين: **577 من 1,250 = 46.16%** من العناصر المفتاح فيها هو الأطول وحده،
بعد أن كان 602 = 48.16% عند v142 و690 = 55.2% عند القياس الأول. بالتوزيع المقاس: ex ‏87 من 194 (44.8%) ·
rd ‏178 من 288 (61.8%) · ls ‏145 من 288 (50.3%) · mt ‏167 من 480 (34.8%). والمستويات: A1 ‏82 من 312 (26.3%) ·
A2 ‏147 من 312 (47.1%) · B1 ‏184 من 312 (59.0%) · B2 ‏164 من 314 (52.2%). الحارس `tests/unit/mcq-option-parallelism.test.ts` يفرض القاعدة الآن على
تسعة دروس محوَّلة: `b1-05` · `b1-15` · `b1-20` · `b2-01` · `b2-02` · `b2-07` · `b2-08` · `b2-11` · `b2-12`.
المواضع كما هي A 324 · B 298 · C 305 · D 323 و3 دروس بلا D. السقف 40% أي ≤500 عنصرًا: يلزم 77 عنصرًا،
وتغطيها سبعة دروس تالية (‏`b1-07` · `b1-12` · `b1-16` · `b1-22` · `b2-05` · `b2-13` · `b2-17`)، كلٌّ 11 عنصرًا.

## 4n) قياس دفعة v144 — `b1-07` و`b1-12` (2026-09-20)

القرينة بعد تحويل الدرسين: **555 من 1,250 = 44.4%**، وكانت 577 = 46.16% عند v143 و602 = 48.16% عند v142.
بالمصدر (مقيس بـ `scripts/probe-option-cue.ts`): ex ‏83 من 194 (42.8%) · rd ‏172 من 288 (59.7%) ·
ls ‏140 من 288 (48.6%) · mt ‏160 من 480 (33.3%) — مجموع 555. وبالمستوى: A1 ‏82 من 312 (26.3%) ·
A2 ‏147 من 312 (47.1%) · B1 ‏162 من 312 (51.9%) · B2 ‏164 من 314 (52.2%) — مجموع 555 أيضًا؛ B1 نزل 22 عنصرًا
وهو كل الفرق، وA1 وA2 لم يتحركا لأن الدرسين المحوَّلين من B1. المواضع كما هي (A 324 · B 298 · C 305 · D 323) و3 دروس بلا D.
الحارس يغطي 11 درسًا محوَّلًا، ويكشف إضافة إلى القرينة قواعد اللغة الواحدة والتوسيع والتكرار — وبه ظهرت
المجموعة المختلطة في `b1-12-m5` وأُصلحت. للنزول إلى ≤40%: 55 عنصرًا، خمسة دروس تالية (‏`b1-16` · `b1-22` ·
`b2-05` · `b2-13` · `b2-17`)، 11 عنصرًا لكلٍّ.


## 4o) قياس دفعة v145 — `b1-16` و`b1-22` (2026-09-20)

القرينة بعد تحويل 20 عنصرًا: **535 من 1,250 = 42.8%**، وكانت 555 = 44.4% عند v144 و577 = 46.16% عند v143 و
602 = 48.16% عند v142. بالمصدر (مقيس بـ`scripts/probe-option-cue.ts`): ex ‏82 من 194 (42.3%) · rd ‏166 من 288 (57.6%) ·
ls ‏135 من 288 (46.9%) · mt ‏152 من 480 (31.7%) — مجموع 535. وبالمستوى: A1 ‏82 من 312 (26.3%) · A2 ‏147 من 312 (47.1%) ·
B1 ‏142 من 312 (45.5%) · B2 ‏164 من 314 (52.2%) — مجموع 535 أيضًا؛ كل الفرق في B1 (162 ⇒ 142) لأن الدرسين من B1،
ولم تتحرك A1 وA2 ولا B2. المواضع كما هي (A 324 · B 298 · C 305 · D 323، أي 25.92/23.84/24.4/25.84) و3 دروس بلا D.
قائمة القيادة (مقيسة داخل أعلى 26 درسًا في `scripts/probe-option-cue.ts`): أربعة دروس بـ11 عنصرًا — `b2-05` · `b2-13` ·
`b2-17` · `b2-19` (كلٌّ 11 من 13 = 85%) — واثنا عشر درسًا بـ10 (‏`a2-06` · `a2-12` · `b1-02` · `b1-09` · `b1-13` ·
`b2-06` · `b2-09` · `b2-10` · `b2-14` · `b2-16` · `b2-20` · `b2-21`) وسبعة بـ9 وثلاثة بـ8.
الحارس ما يزال على 11 درسًا: `b1-16` و`b1-22` لم يُدرَجا لأن في كلٍّ منهما عنصر قرينة عربيّ المفتاح ترفض
`cue-batch.py` تأليف مشتتاته، فلا معنى لاستثناء يُرخي قاعدة اللغة الواحدة. للنزول إلى ≤40%: **35** عنصرًا؛ ثلاثة دروس
تعطي 33 ولا تكفي، فيلزم رابع أو دعم الأداة للمجموعات العربية.

## 4p) قياس دفعة v146 — `b2-05` (2026-09-20)

القرينة بعد تحويل 11 عنصرًا في درس واحد: **524 من 1,250 = 41.92%**، وكانت 535 = 42.8% عند v145 و555 = 44.4% عند
v144 و577 = 46.16% عند v143. بالمصدر (مقيس بـ`scripts/probe-option-cue.ts`): ex ‏80 من 194 (41.2%) ·
rd ‏163 من 288 (56.6%) · ls ‏133 من 288 (46.2%) · mt ‏148 من 480 (30.8%) — مجموع 524. وبالمستوى: A1 ‏82 من 312 (26.3%) ·
A2 ‏147 من 312 (47.1%) · B1 ‏142 من 312 (45.5%) · **B2 ‏153 من 314 (48.7%)** — مجموع 524 أيضًا؛ الفرق كله في B2
(164 ⇒ 153) لأن الدرس من B2. المواضع كما هي (A 324 · B 298 · C 305 · D 323، أي 25.92/23.84/24.4/25.84) و3 دروس بلا D.
توزيع الفجوة (حروفًا فوق أطول مشتت) بقي كثيف الذيل: 220 عنصرًا عند 16 حرفًا أو أكثر و39 عند حرف واحد — أي أن
معظم البنود ما تزال «الأطول واضحًا» بمقدار كبير، وهذا ما تعالجه الدفعات لا التغليف.
قادة البقايا بعد الدفعة: `b2-13` · `b2-17` · `b2-19` (11 من 13 لكلٍّ = 85%)، ثمّ اثنا عشر درسًا بعشرة عناصر.
الحارس صار على **12** درسًا محوَّلًا (أُضيف `b2-05`)، وتمرّ معه بنود الدرس غير القرينيين بلا تعديل. للسقف ≤40%
(≤500): يتبقى **24** عنصرًا، ودراستان (22) لا تكفيان — يلزم `b2-19` أو دعم `cue-batch.py` للمجموعات العربية
النقية لإغلاق عنصري `b1-16-e6` و`b1-22-e6`.

## 4q) قياس دفعة v147 — `b2-13` (2026-09-20)

القرينة بعد تحويل 11 عنصرًا في `b2-13`: **513 من 1,250 = 41.04%**، وكانت 524 = 41.92% عند v146 و535 = 42.8% عند
v145 و555 = 44.4% عند v144. بالمصدر (مقيس بـ`scripts/probe-option-cue.ts`): ex ‏78 من 194 (40.2%) ·
rd ‏160 من 288 (55.6%) · ls ‏131 من 288 (45.5%) · mt ‏144 من 480 (30.0%) — مجموع 513. وبالمستوى: A1 ‏82 من 312 (26.3%) ·
A2 ‏147 من 312 (47.1%) · B1 ‏142 من 312 (45.5%) · B2 ‏142 من 314 (45.2%) — مجموع 513 أيضًا؛ الفرق كله في B2
(153 ⇒ 142) لأن الدرس من B2. المواضع كما هي (A 324 · B 298 · C 305 · D 323) و3 دروس بلا D.
توزيع الفجوة (حروفًا فوق أطول مشتت): 1→39، 2→28، 3→26، 4→26، 5→23، 6→20، 7→19، 8→20، 9→17، 10→15، 11→14،
12→13، 13→16، 14→13، 15→11، و**16+→213** — أي أن الذيل الثقيل ما يزال معظم الانحراف: 213 بندًا تتجاوز المفتاح
بستة عشر حرفًا أو أكثر. القادة بعد الدفعة: `b2-17` و`b2-19` (11 من 13 لكلٍّ = 85%)، ثمّ اثنا عشر درسًا بعشرة عناصر.
الحارس صار على **13** درسًا (أُضيف `b2-13`) و**14** اختبارًا في ملفه؛ وسُجِّل أن ثلاثة بنود محوَّلة صارت متعادلة
الطول تمامًا مع أطول مشتت (28/28، 63/63، 49/49) وهو مقبول لأن القرينة تُعرَّف بالأطول وحده. للسقف ≤40% (≤500):
يتبقى **13** عنصرًا، ودرس واحد (11) يترك 502 فلا يغلق؛ يلزم `b2-19` أو عنصرا `b1-16-e6` و`b1-22-e6` العربيّان.

## 4r) قياس دفعة v148 — `b2-17` (2026-09-20)

القرينة بعد تحويل 11 عنصرًا في `b2-17` وإصلاح مجموعة كشفها الحارس: **502 من 1,250 = 40.16%**، وكانت 513 = 41.04%
عند v147 و524 = 41.92% عند v146 و535 = 42.8% عند v145. أي **عنصران فوق السقف** (≤500) لا غير. بالمصدر
(مقيس بـ`scripts/probe-option-cue.ts`): ex ‏77 من 194 (39.7%) · rd ‏157 من 288 (54.5%) · ls ‏128 من 288 (44.4%) ·
mt ‏140 من 480 (29.2%) — مجموع 502. وبالمستوى: A1 ‏82 من 312 (26.3%) · A2 ‏147 من 312 (47.1%) · B1 ‏142 من 312 (45.5%) ·
B2 ‏131 من 314 (41.7%) — مجموع 502 أيضًا؛ كل الفرق في B2 (142 ⇒ 131). المواضع كما هي (A 324 · B 298 · C 305 · D 323،
أي 25.92/23.84/24.4/25.84) و3 دروس بلا D. توزيع الفجوة: 1→39، 2→28، 3→26، 4→25، 5→23، 6→20، 7→18، 8→19، 9→17،
10→15، 11→14، 12→13، 13→16، 14→12، 15→11، **16+→206**.
القادة بعد الدفعة: `b2-19` وحده بـ11 من 13 (85%)، ثمّ اثنا عشر درسًا بعشرة عناصر. درس واحد (`b2-19`) ينزل إلى 491
= 39.28% فيُغلق البوابة؛ والدرسان العربيّان (`b1-16-e6` و`b1-22-e6`) ينزلان إلى 500 = 40.0% أي عند السقف تمامًا،
وكلاهما معلّق على قرار أداة لا على تأليف يدوي.
حالة الدرس المحوَّل تُسجَّل للتاريخ: `b2-17-m2` (‏"einzureichen" مقابل "einreichen zu" / "zu einreichen" /
"einreichen zu sein") كان **مخالفًا لقاعدة الطول من الجهة الأخرى** — مفتاح من 12 حرفًا هو الأقصر وحده — ولم يظهر في
قائمة القرينة إطلاقًا؛ مُرِّر عبر `cue-batch.py` بمشتتات 10/13/13 فمرّ الحارس (15 اختبارًا في ملفه، 14 درسًا في
القائمة).

## 4s) قياس دفعة v149 — `b2-19` وإغلاق ملاحظة السقف (2026-09-20)

القرينة بعد تحويل 11 عنصرًا في `b2-19`: **491 من 1,250 = 39.28%**، وكانت 502 = 40.16% عند v148 و513 = 41.04% عند
v147 و524 = 41.92% عند v146 و535 = 42.8% عند v145 والبداية 690 = 55.2% عند 2026-09-17. السقف ≤500 **بُلِغ** (9 عناصر
فضلًا)، و`reports/lesson-quality-audit.json` يطبع `multipleChoice.longestOptionIsKeyPct = 39.28`. ملاحظة
«الأطول=الأصح» تُغلَق قياسًا بهذا؛ وبوابة `lesson:quality:audit` ما تزال **خروج 1** بملاحظتين لا علاقة لهما بالطول
(الإنتاجية أحادية السلسلة 50.9% مقابل سقف 25%، وسيط الشرح 24 مقابل أدنى 60)، فلا تُعلَن خضراء ولا يُغلَق P1-397.
بالمصدر: ex ‏76 من 194 (39.2%) · rd ‏154 من 288 (53.5%) · ls ‏125 من 288 (43.4%) · mt ‏136 من 480 (28.3%) — مجموع 491.
بالمستوى: A1 ‏82 من 312 (26.3%) · A2 ‏147 من 312 (47.1%) · B1 ‏142 من 312 (45.5%) · **B2 ‏120 من 314 (38.2%)** —
مجموع 491؛ كل الفرق في B2 (131 ⇒ 120). المواضع كما هي: mix ‏25.92/23.84/24.4/25.84 (A 324 · B 298 · C 305 · D 323)
و3 دروس بلا D — الموازنة لا تتأثر بتأليف المشتتات لأن `correctIndex` لا يتغير. توزيع الفجوة: 1→39، 2→28، 3→25،
4→25، 5→22، 6→19، 7→18، 8→19، 9→17، 10→15، 11→14، 12→13، 13→16، 14→11، 15→11، 16→199 (المجموع 491).
القادة: اثنا عشر درسًا بعشرة عناصر (a2-06 · a2-12 · b1-02 · b1-09 · b1-13 · b2-06 · b2-09 · b2-10 · b2-14 · b2-16 ·
b2-20 ‏10 من 14 · b2-21) ثمّ تسعة؛ أي لم يبقَ تركّز في درس واحد، فالبند لم يعد «دفعة واحدة من الإغلاق» بل اختيار
متبقٍّ لمن يريد هامشًا أوسع. العنصران العربيّا المفتاح (`b1-16-e6` و`b1-22-e6`) لم يعودا ضروريين لأي سقف؛ يبقى
منعهما قرار أداة (`cue-batch.py` تشترط ألمانية نقية).
الحارس: أُدرج `b2-19` ⇒ **15** درسًا في `PARALLELISED` و**16** اختبارًا في ملفه، ونجح من أول تشغيل بلا حالة
«المفتاح الأقصر وحده» (خلافًا لـ`b2-17-m2` عند v148 و`b1-12-m5` عند v144). الإجمالي: **155 ملفًا / 1,019 اختبارًا**،
tsc صفر، lint صفر، `handoff:check` صفر، وبناء خروج 0 (321 صفحة، SSG ‏8.8s، بصمة `offline:size` ‏86118504e7f8،
full ‏5,641,870).

## 4t) قياس دفعة v150 — شروحات A2، الدفعة الثالثة: 24 بندًا (2026-09-20)

**ما قيس قبلها** (`reports/lesson-quality-audit.json`): `feedback.allUnder60Chars` **1,322** من 1,733 ·
`allMedianChars` **28** · `explanationMedianChars` **24** · `feedback.under40Chars` **1,047** من 1,250.
استقصاء أقصر شرح في كل تمرين (`scratch/expl_detail.ts a2 0 24`) أعطى **TOTAL 120** بندًا في A2 تحت 60، كلها بين
21 و25 حرفًا، أي أن الأضطرّ في A2 هو هذا النطاق لا ذيل أطول.

**ما عُمل**: 24 نصًا مؤلَّفًا بالمدى المحريري **156–212** (الواقع 168–210)، مع حارسين آليين قبل التطبيق: لا عبارة
موضعية (`scripts/rebalance-answer-positions.ts:21`، مطابقة جزئية فـ«الأولى» ممنوعة) ولا تسمية حالة
(`src/core/content-validation/meaning-first-case.ts:5-7`). وطولُ كل بند تحقق منه `scratch/set_expl.py` حرفيًا قبل
الكتابة ⇒ **applied: 24 of 24 missing: []**؛ ولم تُمَسّ `acceptedAnswers` ولا نصّ الخيارات.

**ما قيس بعدها**: ‏**1,298** تحت الستين (**−24** بالضبط) · وسيط البنود **29** · وسيط الشرح **25** ·
`feedback.under40Chars` **1,035** (‏−12؛ 12 من البنود الـ24 اختيار من متعدد والباقي fill-blank/matching/
word-ordering/error-correction) · تمارين A2 بشرح ≥60: **72/168** (المتبقي **96**) · المكوّنات A2 96 + A1 167 +
أسئلة المراحل 1,035 = **1,298** ✓ · القرينة **39.28%** بلا حركة (الدفعة لا تكتب الخيارات) · حارس الدروس الموازية
**15** درسًا / 16 اختبارًا · `case:audit` **0 فجوات** لكن `discoveredSignals.controlled` **21 ⇒ 20** (شرحَا
`a2-04-e1`/`a2-04-e2` كفّا تسمية Dativ؛ رُفِع التثبيت في `tests/unit/meaning-first-case.test.ts` وحده) · البوابة
الصارمة **خروج 1** بملاحظتين: وسيط 25 مقابل 60، والإنتاجي أحادي السلسلة **50.9%** (قرار ADR-078، لا يُقلَب آليًا) ·
tsc صفر · lint صفر · vitest **1,019** في 155 ملفًا (بلا اختبار جديد) · `handoff:check` صفر · بناء خروج 0 (321 صفحة،
SSG ‏9.7s، بصمة `offline:size` ‏90a27131fd55، full ‏5,649,071، المنهاج 945,047 gzip).

**ملاحظة مفتاح**: المفتاح الصحيح اليوم هو `feedback.under40Chars` (‏1,035 من 1,250 في `feedback.ofItems`)؛
سجلات `b1-explanations-3` و`a2-explanations-1` و`b2-explanations-1` تذكر `multipleChoice.under40Chars` لأن القياس كان
هناك تحت `multipleChoice` قبل توسيع المؤشر إلى كل البنود. والرقم 1,035 يتكرر في هذه الدفعة بمعنى آخر (باقي أسئلة
المراحل بعد طرح 96 و167 من 1,298) فليس خطأ نسخيًا ولا مقياسًا واحدًا.

**التعديل المقترح**: متابعة D2 بـ24 بندًا في كل دفعة (المتبقي على مستوى التمارين 263: A2 96 ثم A1 167)، مع رقمي
`allUnder60Chars` قبل/بعد وفحص المجموع في كل سجل؛ ولا تُنقل P1-399 إلى implemented قبل بلوغ **الوسيط 60** لا مجرد
نزول العدّاد؛ وأي دفعة تُلغي تسمية حالة من شرح تُثبَّت في `discoveredSignals` تُحدِّث التثبيت يدويًا مع سطر صريح في
سجل التشغيل (وإلا ظهر الانزياح «فشلًا» في `handoff:check` لا «قرارًا»).

## 4u) قياس دفعة v151 — شروحات A2 الدفعة الرابعة، وبينة قرار P1-398 (2026-09-20)

**قبل/بعد من `reports/lesson-quality-audit.json`**: `feedback.allUnder60Chars` ‏1,298 ⇒ **1,274** من 1,733 (الفرق 24
بالضبط) · `allMedianChars` ‏29 ⇒ **30** · `explanationMedianChars` ‏25 ⇒ **25** (بلا حركة: 4 فقط من الـ24 كانت اختيارًا
من متعدد) · `feedback.under40Chars` ‏1,035 ⇒ **1,031** · تمارين A2 بشرح ≥60 ‏72/168 ⇒ **96/168** والمتبقي على مستوى
التمارين A2 ‏72 ثم A1 ‏167؛ والفصل 72 + 167 + 1,035 = 1,274 ✓. الدفعة كتبت 24 نصًا بواقع **174–212** حرفًا بعد أن رفض
فاحصٌ محلي مسودةً فيها تسعة نصوص خارج المدى؛ لم تُمَسّ الخيارات ولا `acceptedAnswers`، فبقيت القرينة **39.28%**
والحارس **15** درسًا و`case:audit` ببصمة ‏167645dd621b و0 فجوات (لم تُسمَّ حالة في أي نصّ جديد).

**بينّة P1-398 (لا عمل مؤجَّل بلا رقم)**: `npm run lessons:variant-worklist` ⇒ `reports/accepted-answer-worklist.md`:
**197** بندًا إنتاجيًا بإجابة وحيدة، منها **0 قابلة للإضافة بثقة** تحت قاعدة ADR-078 (لا يُخفَّف المُصحِّح؛ البديل
يُشتَقّ فقط من تقديم جرّة خلفية مع بقاء الفعل المُصرَّف في موضعه). التوزيع بالنوع: **159 fill-blank · 36 word-ordering ·
2 error-correction**؛ وبالرفض: 147 «جملة قصيرة جدًا»، 32 «نص فرعي أو مجموعة المصدر»، 14 «لا جرّة خلفية»، 4 «لا يحفظ
الرصيد». الحساب: السقف 25% من 387 يعني ≤96، أي رفع **101** بندًا؛ وبافتراض رفع البنود الـ38 كلها (ترتيب + تصحيح) يبقى
**159/387 = 41.1%**. والباقي فراغاتٌ بكلمة واحدة صحيحة نحويًا («bist»، «um»، «der»)، فأي بديل ثانٍ يُقبل فيها يعني
إمّا خطأً مقبولًا أو تغييرَ البند. **القرار إذًا ليس التحرير بل التعريف**: إمّا (1) إعادة تصميم مجموعة من الفراغات
لتقعد على سياق يقبل مرادفًا حقيقيًا (يُقاس حينها عدد البنود المعدَّلة)، أو (2) قياس المؤشر على البنود المفتوحة
الإجابة (ترجمة/تصحيح/إنتاج حر) وإبقاءُ الفراغ ذا الكلمة الواحدة خارجَه مع سطر ADR صريح، أو (3) إبقاء البوابة حمراء.
لا تُرخى العتبة ولا تُوسَّع `normalizeGermanText` في هذه الجلسة؛ وتُطرح الخيارات على المالك.

**اقتراح حارس مرافق (لم يُنفَّذ، يحتاج دورًا مستقلًا)**: اختبار وحدة يمنع «الحشو بالإجابة الثانية» — أن يختلف كل
زوج من `acceptedAnswers` بعد الطي بأكثر من حالة الأحرف وعلامات الترقيم، وإلا كان البديلُ حشوًا لرفع العدّاد لا
تعلّمًا؛ يُضاف هذا الحارس مع أي دفعة بدائل حقيقية، وإلا صار إغلاق P1-398 قابلًا للتزوير بسطر.

**البوابات**: tsc صفر · lint صفر · vitest **155 ملفًا / 1,019 اختبارًا** (بلا اختبار جديد) · `handoff:check` صفر ·
`lesson:quality` صفر · **`lesson:quality:audit` خروج 1** بملاحظتين (50.9% مقابل 25%، ووسيط 25 مقابل 60) ·
البناء خروج 0: 321 صفحة (SSG ‏9.8s)، بصمة `offline:size` ‏8d3241e662dd، full ‏5,655,234، المنهاج 946,991 gzip.

**المسافة محسوبة لا مقدَّرة**: بإعادة إنتاج مجتمع البوابة (تمارين اختيار + أسئلة قراءة/استماع + `miniTest`) يوجد
‏1,250 بندًا، منها **1,099** تحت الستين و**1,031** تحت الأربعين؛ ولأن الوسيط =‏ sorted[floor(n/2)] فشرط 60 هو ألّا يبقى
أكثر من **625** تحت الستين ⇒ **474 بندًا** = **20 دفعة** بـ24، موزعة A1 ‏311 · A2 ‏281 · B1 ‏264 · B2 ‏243. لا جدوى من
حصر العمل في تمارين A2/A1: المستويان الأعلى فيهما أسئلة قصيرة البقية.

**التعديل المقترح**: تُدفع دفعات الشرح بـ24 بندًا حتى A2 = 168/168 ثم A1، ولا يُنقل P1-399 إلى implemented قبل
بلوغ **وسيط 60**؛ وبند P1-398 يُستدعى فيه قرار المالك بالأرقام الثلاثة أعلاه، ولا يُلمَس لاثنَين: العتبة أو المُصحِّح.


## 4v) قياس دفعة v152 — شروحات A2 الدفعة الخامسة، واستعادة الشجرة، وعقد التعليم (2026-09-20)

**الدفعة.** أربعة وعشرون شرحًا في تمارين A2، أدناها 29 وأعلاها 32 حرفًا، أُعيد تأليفها بين **180 و212** حرفًا بالنسق المعتمد: القاعدة، مثال ألماني داخل «»، ثم سبب سقوط البديل الخاطئ، ثم «فحص ذاتي» واحد. الكُتِبَت عبر `tmp/a2e5.py` (فاحصٌ يمنع ألفاظ المواضع وأسماء الحالات والحروفCJK والتكرار ويفرض المدى) وطُبِّقت بـ`scratch/set_expl.py a2` الذي يقارن طول النصّ القديم قبل الكتابة ⇒ `applied: 24 of 24 missing: []`. رُفضت ثلاث جولات مسوّدات (236→229→214 وسَطِيًّا) قبل القبول؛ لم يُلغَ أيّ بند ليُجمَّل العدد.

**المقيس بعد التوليد** (`npm run lesson:quality` الذي يكتب فعلاً بـ`--write`): `feedback.allUnder60Chars` ‏1,274 ⇒ **1,250** من 1,733 (نقصٌ بمقدار 24 بالضبط) · `feedback.under40Chars` ‏1,031 ⇒ **1,021** من 1,250 (عشرة من الأربعة والعشرين كانت اختيارًا من متعدد) · `allMedianChars` ‏**30** · `explanationMedianChars` ‏**25** · `allMissing` ‏0. على مستوى التمارين: A2 قصير الشرح 72 ⇒ **48** (وتمارين A2 عند **120/168**)، A1 ‏167، وB1 وB2 صفر؛ والباقي **1,035** في أسئلة القراءة والاستماع و`miniTest` (48 + 167 + 1,035 = 1,250 ✓). لم تتحرك موازنةُ الخيارات ولا القرينة: **39.28%** (491 من 1,250) وتوزيع 25.92/23.84/24.40/25.84 و`lessonsWithoutPositionD` ‏3، لأن الدفعة نصٌّ لا هندسة خيارات؛ ولا يُقرَب سقفُ ≤500 ولا سقف 25% بالإعلان.

**سقوط الحارس وتصحيحُه.** بعد التطبيق أسقط `tests/unit/meaning-first-case.test.ts`: `discoveredSignals.controlled` صار **19** بدل 20. السبب أن الشرح القديم لـ`a2-04-e3` كان يذكر `Dativ` و`Akkusativ` صراحةً، فحذفتهما صياغة «بالمعنى فقط»، ففقد التدقيق إشارةً موجودة في ممارسة مُتحكَّم فيها. المعالَج: أُعيد الشرح مع الحالتين مسمّيتَين داخل جملة المعنى نفسها (209 حرفًا) فرجع العدّ إلى 20 **ولم تتحرك بصمة `case:audit`** (‏167645dd621b، صفر فجوات). لم تُمَسّ العتبة ولم يُحدَّث الرقم المتوقع في الاختبار. القاعدة الدائمة: إن كان الشرح القديم في تمرين ممارسة مُتحكَّم فيها يحمل اسم حالة أو علامة تبعية، فليبقَ الاسم في الجديد مع المعنى — والمرجع `tests/unit/meaning-first-case.test.ts` لا عدّاد الفجوات.

**حادثة الشجرة المقلوبة (تُقرأ ولا تُنسى).** فُتح هذا الجيل و`src/data` في خمسة عشر ملف درس (8 ملفات A2 و5 ملفات B1 وملفّان B2) على حالة أقدم: شروحات دفعتَي A2 السابقتين غائبة، وموازنة قرينة الخيارات قديمة — بينما `reports/` والوثائق على حالة v151. القياس كشفها: القرينة طلع **48.16%** بدل 39.28%. التسوية: `diff -rq` مع أرشيف v151 المسلَّم (فروق الملفات الخمسة عشر؛ `lessons-a2-module7.ts` مختلف في الشجرة وحده: 64,476 مقابل 66,098 بايتًا)، ثم استعادة الملفات الخمسة عشر من الأرشيف، فأعاد توليد التدقيق **ملفًا مطابقًا حرفيًا** للتقرير المسلَّم. والسبب الذي كان سيُخفي كل هذا: `scripts/generate-lesson-quality-audit.ts:189` يكتب فقط بعلم `--write`؛ تشغيله بلا علم يطبع ملخّصًا ويترك `reports/` قديمًا، فتطابق «ملفٍ لم يُولَّد» مع توقّعك ليس دليلًا. لا تُصدّق رقمًا من `reports/` قبل `Wrote reports/...` أو `mtime` طازج مع خروج 0.

**المسافة الباقية، مقيسة.** بإعادة تشغيل `docs/run-logs/2026-09-20-a2-explanations-4/distance-to-close.ts` (أُعيدت أنواعه صريحة لأن `eslint` كان يرفض `any` في الملف المؤرشف — lint صار صفرًا): من 1,250 بندًا مُقيَّمًا **1,089** تحت ستين حرفًا (1,021 تحت أربعين) ووسيط 25؛ ولأن الوسيط `sorted[floor(n/2)]` يجوز أن يبقى تحت الستين 625 بندًا كحدّ أقصى ⇒ يجب رفع **464** بندًا = **20** دفعة من أربعٍ وعشرين. توزيعها على السكان المُقيَّمين: A1 ‏311 · A2 ‏271 · B1 ‏264 · B2 ‏243 — أي أن قراءة/استماع/`miniTest` في B1 وB2 هي الكتلة الأكبر مع اكتمال تمارينها. الإيقاع الذي اختاره المالك: 24 في كل جولة بالترتيب الحالي.

**P1-398 على حاله، وهو قرارُ تعريف لا شغلُ تأليف.** أُعيد `npm run lessons:variant-worklist` عند هذا الجيل: **197** تمرينًا إنتاجيًا بسلسلة مقبولة واحدة من 387 (= **50.9%** مقابل سقف 25%)، و**0 قابلة للإضافة بثقة**؛ بالنوع 159 فراغًا بكلمة واحدة و36 ترتيب كتل و2 تصحيح. ولو رُفعت البنود الـ38 كلها لبقي **159 من 387 = 41.1%**، فلا يُبلَغ السقف بتأليف بدائل. خيارات المالك الأربعة مشروحة في §4u (تغيير مقياس ADR باستثناء فراغات الكلمة الواحدة ⇒ 16.7% من 228 مع سطرٍ منفصل للـ159 المُستثناة، أو إعادة تصميم 101 فراغ ليقبلوا جوابَين ≈ خمس جولات، أو إبقاء البند مفتوحًا والبوابة حمراء، أو قبول بدائل تجميلية — **مرفوضة**). لم يُختَر شيء بعد، فلا يُمَسّ سقف 25% ولا `normalizeGermanText`. الحارس الذي يمنع بدائل تختلف عن المفتاح بحرف كبير أو علامة ترقيم **اقتراحٌ لم يُنفَّذ**.

**عقد التعليم الذي وقّعه المالك هذا الجلسة، وملزوم للتصميم:** تسعون دقيقة يوميًا (50 عمودًا فقريًا + 25 إنتاج شفهي مُسجَّل + 15 شكل امتحان + 10 دفتر أخطاء)، السبت محاكاة (كاملة كل أسبوعين) والأحد راحة يقررها الإنهاك؛ شهادة **عامة** B2 من Goethe أو telc أو ÖSD لا علاقة لها بالتمريض، والجهة غير محسومة فتُعتمد telc افتراضًا في قالب المحاكاة حتى يتحقق من الصفحة الرسمية بتاريخ ورابط؛ الانطلاق من الصفر مع صدق الحساب (B1 مؤكد في اثني عشر شهرًا، B2 عند أربع عشرة إلى ستّ عشرة أو مع فترة مكثّفة)؛ ومسار التمريض **طبقة منسوجة**: «توابل» في A1/A2 بلا كلمة مهنية تدخل استرجاعًا أو امتحانًا، و«طبق ثانٍ» من B1. ثلاث قواعد لا تُكسر: **(أ)** لا قاعدة نحوية جديدة من أجل الطبقة — مشهدٌ يحتاج لغة أعلى يُرحَّل؛ **(ب)** لا يجوز أن يتوقف الجواب الصحيح في تمرين لغوي على معرفة سريرية — تمرين يحتاج قرارًا طبيًا ليُحلّ فاسد عندنا؛ **(ج)** الطبقة بارasitasية بميزانية كلمات معلنة وقاطع رجوع: إن انخفض أداء مهام الامتحان العام تُخفَّض الطبقة وتُقدَّم الشهادة. عناقيد الأمان (أسماء الأدوية والوحدات، الطريقة والتوقيت، تأكيد الأمر المسموع، سؤالان قبل أمر مبهم) تُبنى **عادةً تُفرض ببيئة التمرين** لا قاعدةً تُحفظ. وكل وحدة تمريضية تحمل صراحةً: **هذه صياغات لغوية لا إرشاد سريري**. **ما لا يُدَّعى**: لا تصحيح نطق معتدّ به، ولا مراجعة بشرية (0 من 3,277)، ولا متصفح لهذا الجيل (آخر مرجعي v143)، ولا إغلاق لـP1-397/398/399 ما دامت البوابة تخرج 1.


## 4w) قياس دفعة v153 — شروحات A2 الدفعة السادسة، وتصحيح جماعة الاختيار، ورجوع الشجرة الثالث (2026-09-20)

**الدفعة.** أربعة وعشرون شرحًا في تمارين A2 كانت بين **32 و35** حرفًا، رُفعت إلى **174–212**: ضمائر Dativ/Akkusativ، و`dass`، و`obwohl`/`weil`، و`wenn`، وأفعال الوضع `können/müssen/dürfen/sollen`، وعبارات الهاتف، وعقد المهنة، و`Buchungsbestätigung` مقابل `Bestätigung` و`Rechnung`، وملاءمة اللباس حسب المناسبة، و`arbeiten/gehen/fahren`. طُبّقت بـ`scratch/set_expl.py a2 /home/user/tmp/a2e6.json`: **applied: 24 of 24 · missing: []**.

**المقيس بعد التوليد** (`npm run lesson:quality`، وهو من يكتب فعلًا): `feedback.allUnder60Chars` ‏1,250 ⇒ **1,226** · `feedback.under40Chars` ‏1,021 ⇒ **1,019** · `allMedianChars` ‏30 و`explanationMedianChars` ‏25 بلا حركة · شروح تمارين A2 تحت 60 ‏48 ⇒ **24** (وA1 يبقى 167؛ 24 + 167 + 1,035 = 1,226 ✓) · القرينة 39.28% (491 من 1,250) والموازنة 25.92/23.84/24.40/25.84 والإنتاجية 50.9% — كلها بلا حركة، وهذا مقصود: الدفعة شروحٌ لا خيارات ولا بدائل.

**الحراج المقيس، وهو أهمّ من الدفعة نفسها.** وسيط `explanationMedianChars` يحسب على جماعة محدودة: تمارين `multiple-choice` + أسئلة القراءة + أسئلة الاستماع + `miniTest` (= **1,250** بندًا، تحت الستين منها **1,087**)، بينما `feedback.allUnder60Chars` يعدّ على السكان الأوسع (**1,733** بندًا، تشمل أنواع تمارين لا تدخل الوسيط). النتيجة العملية: دفعة v153 رفعت 24 نصًّا فلم يقع في جماعة الوسيط إلا **2** منها ⇒ المسافة إلى 60 تحرّكت **464 ⇒ 462** فقط. الأداة `scratch/expl_detail.ts` ترتّب السكان الأوسع، فلا تُجمَع بها دفعةٌ بعد اليوم إلا لمؤازرة القياس؛ بدلاً منها `tmp/pick_median.ts 24 a2` (مؤرشف مع سجلّ هذا الجيل) يرتّب الجماعة نفسها — أقصر بنودها شروحُ أسئلة إصغاء بـ**6 إلى 10 أحرف** — ورفعُ 24 منها ينقل المسافة **462 ⇒ 438** فعلًا، فتبقى ≈ **18 دفعة** بدل 20 بالوتيرة القديمة.

**باني الدفعة صار يقرأ من المستودع.** ‏`tmp/a2e6.py` لكل معرّف: يجلب `explanationAr` الحيّ من ملفّاته، ويطبّق `set_expl.py` على نسخة في /tmp ثم يقيس الفرقَ سطرًا بسطر، ويرفض (أ) أيّ نصّ خارج نطاق الطول، (ب) أيّ نصّ **يحذف إشارة إعرابية** كانت في القديم (`CASE(old) ⊆ CASE(new)`)، (ج) أيّ صياغة تستخدم ألفاظ موضع (الأول/الثاني/الأخير/الموضع/المرتبة) عندما تكون تلك الألفاء خياراتٍ حرفية في السؤال. كشف التمرير الأول **10 نصوص فوق 212** و**إيهامًا موضعيًا واحدًا** في `a2-08-e3` («هل هو الأخير؟» بينما `الأخير` خيارٌ نصّي في السؤال)، فلم تُكتب anything قبل التصحيح؛ وبذلك بقي `case:audit` عند `167645dd621b…` بصفر فجوات ولم تتحرك بصمةُ حارسٍ واحدة. كما تبيّن أن مفاتيح بيانات الدروس **غير منقّطة** (`explanationAr:"…"`)، فأي regex يشترط الاقتباس يفشل صامتًا بـ«id not found» للاثنتين وعشرين حالة.

**رجوع الشجرة الثالث في أربعة أدوار.** فُتح الجيل و(1) ‏`/home/user/wegberlin-full.zip` على القرص ببايتات **v151** (63,786,813) تحت sidecar يحمل بصمة v152 ⇒ `sha256sum -c` **فشل**، وهو الكاشِف الوحيد؛ (2) `node_modules` = **0** ⇒ `npm ci --no-audit` (خروج 0)؛ (3) `src/data` رجع إلى حالة v149 (المقيس الطازج: 1,322 تحت الستين وقرينة 48.16%)؛ (4) ‏`tests/*` والـverifier رجعا إلى v151/v150 بينما بقِي `public/sw.js` والوثيقة الاحتياطية على v152 ⇒ حرفُ الكاش **قد يختلف بين الملفات السبعة**، فلا يُؤخذ أحدهما دليلاً؛ (5) ‏`docs/run-logs/2026-09-20-a2-explanations-5/` رجع **فارغًا (0 ملفات)**. الاستعادة: استخراج `src/data/*` و`tests/*` و`scripts/verify-continuation-handoff.mjs` من أرشيف v151 إلى `/home/user/tmp/v151z` ثم نسخ 15 ملف بيانات drifted ⇒ `diff -rq` = **0**، ثم إعادة تطبيق الدفعة #5 (`tmp/a2e5.json` + `tmp/a2e5-fix1.json`) فخرج القياس مطابقًا تمامًا لما سُلّم في v152 (‏1,250 / 1,021 / 39.28% / ‏TOTAL 48).

**درس يُضاف لقواعد البناء.** ‏`reports/*.json` **لا يصلح دليلاً على حالة المستودع**: نجا من الرجوع بينما رجعت `src/data`، فأي رقم ثابت يُقرأ منه يبدو أخضر كاذبًا. الدليل الوحيد قياسٌ طازج من الأداة التي تكتب (`npm run lesson:quality`) مقارنةً بالتثبيت، مع عدِّ ملفات سجلّ الجيل وفحص SHA لل أرشيف.

**Bounded scope مُسلَّم.** لم يُشغَّل متصفح؛ آخر مرجعي أخضر **v143** (40 من 42) ⇒ لا ادّعاء **82/82**. ولا طبقة توابل: العقد موثّق في ADR-080 ولم تُكتب سجلّات تمريضية. وP1-398 ما يزال قرار تعريف عند المالك. البوابة المشتركة `lesson:quality:audit` خروجها **1** بملاحظتين (50.9% مقابل 25%، ووسيط 25 مقابل 60) — تُترك مغلَّفة بلا تطبيع.


## 4x) قياس دفعة v154 — أول دفعة من جماعة الوسيط: الوسيط تحرّك (30 ⇒ 31) والمسافة 462 ⇒ 438 (2026-09-21)

**الدفعة.** أربعة وعشرون بندًا من **جماعة الوسيط** في A2 — شروحُ أسئلة إصغاء وبنودُ اختيارٍ قصيرة كانت بين **6 و11 حرفًا** («مساءً.»، «-er + als.»، «um … zu.»، «wir würden.») — رُفعت إلى **176–211 حرفًا**: ‏a2-04-lq3، a2-07-lq2، a2-22-lq2، a2-24-m1، a2-10-lq1، a2-11-lq1، a2-19-m3، a2-07-lq3، a2-10-m2، a2-22-m3، a2-10-m1، a2-13-m3، a2-16-m2، a2-19-lq2، a2-21-lq1، a2-23-m2، a2-01-lq2، a2-02-lq2، a2-03-m4، a2-07-m2، a2-09-m5، a2-11-m2، a2-14-lq2، a2-14-m1. التطبيق: `scratch/set_expl.py a2 /home/user/tmp/a2e7.json` ⇒ **applied: 24 of 24 · missing: []**.

**المقيس بعد التوليد.** `feedback.allUnder60Chars` ‏1,226 ⇒ **1,202** · `under40Chars` ‏1,019 ⇒ **995** · `allMedianChars` ‏30 ⇒ **31** · `explanationMedianChars` ‏25 بعدُ (لم يتجاوز 60، ولم يقصُر عن السابق) · جماعة الوسيط تحت 60: ‏1,087 ⇒ **1,063** ⇒ **المسافة إلى 60: ‏462 ⇒ 438**، أي **19 دفعة** بواقع 24 بالوتيرة الجديدة. على مستوى التمارين: A2 تحت 60 ‏**24** بلا حركة (هذه الدفعة من `lq`/`m` لا من `-e`)، وA1 ‏167.

**لماذا تحرّك الآن ولم يتحرّك قبل.** الحساب موثّق في `distance-to-close.ts`: الوسيط يرى 1,250 بندًا فقط (`multiple-choice` + قراءة + إصغاء + `miniTest`)؛ دفعة v153 اختيرت من `allUnder60Chars` (سكان 1,733) فأصابت الجماعة بـ**2 من 24**، ودفعة v154 اختيرت بـ`tmp/pick_median.ts` فأصابت بـ**24 من 24**. الفرقُ ليس أسلوبًا بل تعريفُPopulation؛ ومن هنا صار الاختيارُ آليةً مسمَّرة: لا تُجمَع دفعةٌ لمؤشّرٍ من جماعةٍ أخرى.

**ما لم يلمسه أحد (وهو مقصود).** القرينة **39.28%** (491 من 1,250) والموازنة 25.92/23.84/24.40/25.84 والإنتاجية **50.9%** وأرقام الاسترجاع/المراسي/المراجعة (89·4، 1,297·134، 3,277 بصفر) — الشرحُ لا يُغيّر الخيارات ولا البدائل ولا طابور الاسترجاع.

**الحُرّاس والفاحص.** `case:audit` صفر فجوات عند `167645dd621b…`؛ فاحصُ الباني رفض **18 نصًّا فوق 212** و**إيهامَيْن موضعيَيْن** («الجملة الثانية»، «المركز الثاني») قبل الكتابة؛ البصمتان المتحرّكتان: content:audit ⇒ **67196e878068** و learning-architecture ⇒ **afdcd449e412** (بقية البصمات ثابتة). البوابة المشتركة `lesson:quality:audit` ما زالت خروجها **1** بملاحظتين (50.9% مقابل 25%، وسيط 25 مقابل 60) — لا تُطْبَع ولا تُغلَّق بوساطة.

**الدفعة التالية جاهزة.** بـ`tsx tmp/pick_median.ts 24 a2`: أقصرُ بنود الجماعة الآن عند **11 حرفًا** (‏a2-14-m4، a2-21-m1، …) والمتوقَّع **438 ⇒ 414**.

**استعادةُ هذا الدور (الرجوع الخامس).** الشجرة فُتِحت و`src/data` عند v149 (‏1,322 و48.16%) و`node_modules` = 0 و`.next` = 0 وسجلّ v153 **فارغ (0 ملفات)**، والأرشيف على القرص أقدم من sidecar (فشل `sha256sum -c`). استُعيدت البيانات من `main` (‏`git clone --depth 1` ⇒ 1,274) ثم أُعيدت الدفعات #5 و#6 فقيست 1,226/1,019/39.28%/TOTAL 24 قبل أن تبدأ الدفعة #7 — أي أن التسليم السابق أُعيد بناؤه قياسًا لا رواية. وحادثةُ بناء مُثبَّتة: `npm run build` قُتل بـOOM (خروج 137) عند «Running TypeScript» حين شُغّل مسبارٌ آخر معه؛ الحلّ الذي مشى: `tsc --incremental` لتدفئة `.next/cache/.tsbuildinfo` (5.7 ثانية) ثم بناءٌ **وحيدٌ بلا منافس** على الذاكرة.


## 4y) قياس دفعة v155 — الدفعة الثامنة: وسيط الجماعة 25 ⇒ 26 والمسافة 414، والفصل بين فشل الأنواع ونفاد الذاكرة (2026-09-21)

**ما تحرّك ولماذا.** الاختيارُ هذه المرّة من جماعة الوسيط مرةً ثانية: 24 بندًا عند 11–13 حرفًا (‏`m` و`lq` و`rq` في A2) ⇒ الجماعة تحت 60: 1,063 ⇒ **1,039** والسقف 625 ⇒ المسافة **438 ⇒ 414 (= 18 دفعة)**، و`explanationMedianChars` ‏25 ⇒ **26**. هذه ثاني حركة وسيط متتالية؛ قبل تصحيح الطريقة كانت دفعةٌ كاملة تُحرّك المسافة **2** فقط.

**ما لم يتحرّك ولم يُسكَت.** القرينة **39.28%** (491/1,250) — لا علاقة للشرح بطول الخيارات · الخلط 25.92/23.84/24.40/25.84 · الإنتاجية أحادية السلسلة **50.9%** مقابل سقف 25% (P1-398 قرارُ مالك) · تذكّر 89/4 · المراسي 1,297/134 · المراجعة البشرية **0/3,277** · نصوص الرعاية مؤجَّلة · لا طبقة تمريضية مكتوبة (ADR-080) · بوابةُ الطول تُترك خروجُها 1 بملاحظتيها كما هما، بلا تطبيعٍ ولا خفضِ سقف.

**الحُرّاس المطبَّقون على النصّ الجديد.** لا إيحاء موضعيًا (الفاحص رفض «الرسالة الأولى» و«التصريف الثالث» لأنّ `الأول`/`الثالث` تُعدّ إيحاءً)، لا ألفاظًا موضعية، لا CJK، لا تكرارًا بين الشروح، ولا إسقاط لإشارة إعرابية كانت في النصّ القديم؛ وكلّ شرحٍ يستند إلى عبارةٍ منقولة من النصّ/النصّ المسموع («Kein Fieber…», «…weil seine Bestellung noch nicht angekommen ist») فلا تعتمد إجابةٌ على معرفةٍ خارج اللغة.

**حادثةُ هذه الجلسة (تُدرَّس).** (1) **الرجوع السابع** نقلَ الأرشيفَ المسلَّم إلى بايتاتٍ ملفوفة قديمة تحت sidecar جديد — لذا يُعاد `sha256sum -c` عند التسليم لا قبله بدقائق، وفاحصُ البنية صار أداةً في المستودع. (2) فشلُ `npm run build` عند «Running TypeScript» مع `tsc` منفردًا صفرًا: على هذا الصندوق لا تُشغَّل سلسلة prebuild الكاملة مع فحص الأنواع في عملية واحدة؛ قُسِّمت (تدقيقات، ثم `next build --webpack` بسقف 1,200 ميغابايت، ثم فحوص الحجم). (3) `tests/unit/delivery-layout.test.ts` اختفى مع أن سكربت البناء نجا — فالاختباراتُ نفسها تُعاد قراءتُها من القرص لا من الذاكرة.



## 4z) قياس دفعة v155.1 — الدفعة التاسعة: المسافة 414 ⇒ 390، وكشفٌ أن الشجرة المستعادة أقدمُ من الحُرّاس (2026-09-21)

**ما تحرّك ولماذا.** دفعةٌ تاسعة من جماعة الوسيط: 24 بندًا عند **14–15 حرفًا** (17 من `miniTest` و7 من أسئلة
القراءة/الإصغاء في A2) رُفعت إلى **165–204 حرفًا** (المجموع **345 ⇒ 4,487**) ⇒ الجماعة تحت 60:
**1,039 ⇒ 1,015** من 1,250 والسقف 625، فالمسافة **414 ⇒ 390 = 17 دفعة × 24**، ويقيس المنتقي للدفعة القادمة
**390 ⇒ 366**. وسيطُ الجماعة بقي **26** كما يُتوقَّع من ذيل التوزيع، وتحرّك وسيطُ القائمة الأوسع
**31 ⇒ 32**، ونزلت تحت‑40 **971 ⇒ 947** وتحت‑60 العريضة **1,178 ⇒ 1,154** من 1,733 — وكلُّ فرقٍ يساوي 24 بالضبط
حيث يُنتظَر أن يساويه.

**توزيعٌ بالنمط بدل الرقم الواحد** (`suffix9.ts` على الكائنات الحيّة لا على نصّ الملفات): A2 قصيرُها
**216 من 432** = تمارين **24**/168 · `miniTest` **76**/120 · `rq` **64**/72 · `lq` **52**/72؛ وA1 ‏**431 من 432** =
تمارين **167**/168 وسائرُ الأنماط قصيرةٌ كلها (120 و72 و72)؛ وB1 ‏264/432 وB2 ‏243/437. والمجموع
**431 + 216 + 264 + 243 = 1,154** يطابق `feedback.allUnder60Chars` ✓، وعلى السكان المقيَّمين بوسيط الجماعة:
A1 ‏311 · **A2 ‏197** · B1 ‏264 · B2 ‏243 (المجموع 1,015 ✓). **وبهذا يُدرَك رقمٌ كان سيُثبَّت خطأً:** شروحُ تمارين
A2 **بقيت 24** ولم تنقص إلى 9، لأن الدفعة لم تحمل بندَ تمارين واحدًا.

**ما لم يتحرّك ولم يُسكَت.** القرينة **48.16%** · الخلط 25.92/23.84/24.40/25.84 · الإنتاجية أحادية السلسلة
**50.9%** · مفرداتُ الكتابة خارج المدى **0** · تذكّر 89/4 · مراسٍ 1,297/134 · مراجعة بشرية **0/3,277** — كله
مقيسٌ بعد الكتابة.

**البصمتان اللتان تحرّكتا.** `content:audit` ‏`eca2409c0489…` ⇒ **`27d4c28626e1…`** و`learning-architecture`
‏`e765f8e14d80` ⇒ **`9b57e06047aa`**؛ وبقيت `case:audit` عند **`167645dd621b…` بصفر فجوات** وبالعدادات
19 عقود/23 نظرية/57 مضبوطًا/44 تقييمًا، وبلا حركة أيضًا `similarity` ‏`efdb019376f3…` و`language`
‏`9da1a2e19f1e…` و`lexical-strategy` ‏`439956b453f0` و`study-modes` ‏`1645b5caa1b0` و`practical-context`
‏`c935b0f4ef8b` و`exam:format-claims` ‏`d729860a7334`.

**الكشفُ الأهمّ في هذا الدور: العجزُ أوسعُ من الشروح، والحارسُ صادقٌ لا عاطل.** بعد تحديث التقارير
بـ`:write` يفشل `handoff:check` عند «noun target-gap counters drifted»: التدقيق يقيس **4,802 / 1,380 / 3,333**
بينما يثبّت `tests/unit/lexical-target-gap.test.ts` و`academic-content-governance.test.ts` والمُتحقِّق
**4,874 / 1,381 / 3,404**؛ ويُرجع `mcq-option-parallelism` **8 فحوصاتٍ حمراء** (`b1-07:e1` ‏50/52/46/28،
`b1-12:e1`، `b1-20:e1`، `b2-05:e1` ‏50/56/26/17، `b2-11:e1`، `b2-13:e1`، `b2-17:e7`، `b2-19:e7`)؛ والقرينةُ هنا
**48.16%** بينما توثّق v155 ‏**39.28%**. البرهانُ أنه ليس من دفعات الشروح: `explanationAr` **لا يُقرأ** في
`src/core/content-validation/lexical-target-gap.ts` (`grep -c` = **0**)، وبقي pending-human عند 89 و4. فالتفسير
الصحيح أن تراجعَ اللقطة أعاد `src/data` إلى ما **قبل** سلسلة دفعات القرينة/الموازاة المسجَّلة في `DECISIONS.md`
(العدادات 4,833→4,845→4,857→4,864→4,874 والقرينة 44.4→42.8→41.92→41.04%)، وأن ما استُعيد في الأدوار السابقة
هو **حمولاتُ الشروح فقط** (145 بندًا). التعافي مُخطَّطٌ وفي المستودع: **28 ملف JSON** في
`docs/run-logs/*cue-parallelism*/` (منها 12 ملف `*-batch.json`، كلُّ بندٍ فيها قائمةُ `options` مُعادَ توليفها)
تغطي كلَّ درسٍ ورد في الفحوصات الحمراء.

**قرارٌ مُلتزَم: لا وسمٌ سادسَ عشر، ولا كاش، ولا حزمة.** كان ممكنًا رفعُ `sw.js` إلى `v156` وتوليدُ الأرشيف، لكن
النشر من هذه البيانات **يرجع** بالمحتوى المنشور (يُسقِط دفعاتِ القرينة والموازاة و72 مرشحًا معجميًّا)، فالحزم
مؤجَّل حتى تُستعاد `src/data` إلى حالة الحُرّاس ثم تُعاد البوابات. وما **بطل** في هذا الدور: ظنُّ أن
`src/data/{exercises,curriculum,micro-drills}` JSON مفقودةٌ وأن التدقيقات تسقط حتمًا — فـ`vendor:materialize` و
`runtime:materialize` و`offline:manifest` **تنجح** وتولّد **318 مسارًا و5 حُزَم (a1 58 · a2 58 · b1 58 · b2 219 ·
full 318)**؛ وتدقيقا `content:audit` و`learning:architecture:audit` كانا يفشلان لأن **الآثار المخزَّنة قديمة**،
وهذا يُصلَح بـ`:write` وقد صُلح، لا لأن البناء متعذّر.

**إصلاحٌ حُرّاسيٌّ صغير لا تجميل.** ثلاثةُ ملفات اختبار ثبتت على `staging/previous-v150` بينما `public/sw.js`
على `v154` (ضررُ التراجع لا الدفعة): `offline-curriculum-rollback.test.ts` و
`offline-recovery-partial-export.test.ts` و`today-offline-readiness.test.ts` — وُظِّفت الحرفية على `v154` ⇒ من
**6 ملفات حمراء و13 فحوصًا** إلى **3 و10**، والفحوصُ الثلاثةُ نفسها **13/13**. لم يُليَّن حارسٌ ولم تُعدَّل
عداداتٌ لتُطبِق؛ العشرةُ الباقية تبقى حمراء **إلى أن تُستعاد البيانات**.

**ديمومةُ الأداة.** صار مطبِّقُ الدفعات في المستودع: `scripts/apply-explanation-batch.py`
(`--level --payload [--dry-run] [--data-root]`؛ يقفل على `oldLen` تموضعًا ومحتوىً، ويرفض مستوى بلا
`lessons-<level>-module*.ts`، ويعدّ الكتابة الفعلية) مع حمولاتِ الدفعات العشر في
`docs/run-logs/a2-explanations-payloads/`. تحقّقٌ ثلاثي: `--dry-run` على الدفعة 9 المطبَّقة ⇒ **24 ×
`SKIP-already-lifted`** و`would write 0 field(s)` · `--level c9` ⇒ **رفضٌ بخروج 1** · ومسارُ الكتابة على نسخة ⇒
`wrote 1 field(s)` ثم `wrote 0 field(s)` عند الإعادة.

**البوابات في هذا الدور.** `npx tsc --noEmit` خروج **0** · `npm run lint` كان **3 أخطاء** ثم صار **0**: مصدرُها **ملفٌ مؤرشف** في سجلّات v150 (`docs/run-logs/2026-09-20-a2-explanations-4/distance-to-close.ts`) أرجعه التراجعُ بلا تثبيت الأنواع؛ أُلغيت `any` الثلاثة بـ`type Scored = { explanationAr?: string; type?: string }` (لا تعطيلُ للقاعدة)، **وأعاد الملفُ نفسُه القياسَ مستقلًّا**: `scored items 1250 · median 26 · <60 1015 · <40 947` و«lift 390 more (batches of 24 ⇒ 17)» وA1 ‏311 · A2 ‏197 · B1 ‏264 · B2 ‏243 — اتفاقٌ تامّ مع `pick_median.ts` والتقرير ✓. · `npx vitest run tests/unit` ‏**1,017 من 1,027** في
**153 من 156** ملفًا · `case:audit` و`governance:audit` و`exam:format-claims:audit` **خُضْر** ·
`handoff:check` **أحمر** عند باب العدّادات أعلاه · لا متصفح (آخر أخضر v143: 40 من 42) · لا Vercel مُتحقَّق منه ·
**الدفعة العاشرة** جاهزة عند **15 حرفًا** (`a2-21-lq2`، `a2-23-lq2`، `a2-11-m3`، `a2-14-m2`، `a2-15-rq3`،
`a2-17-m3`…) بـ`npx tsx docs/run-logs/2026-09-21-a2-explanations-9/pick_median.ts 24 a2`.
السجلّ: `docs/run-logs/2026-09-21-a2-explanations-9/README.md`.


## 4ee) جيل v156 — استعادةُ دفعات القرينة أخضَرَت الحُرّاس، والوسمُ والبناءُ نجحا، والحزمةُ محجوبةٌ بفقدان البيانات التمرينية (2026-09-22)

**ما أثبته الفاحص.** كان الانزياحُ محتويًا لا توثيقيًّا: التدقيقُ يقيس 4,802/1,380/3,333 والاختباراتُ تثبّت
4,874/1,381/3,404، و`mcq-option-parallelism` يُرجع 8 فحوصاتٍ حُمرًا، والقرينة 48.16% بدل 39.28%. الحلُّ لم يكن
تليينَ الحارس ولا إعادةَ تثبيتٍ «لِتُطابِق»، بل **إحضار البيانات إلى حالة الحارس**.

**الآلية (مُستعمَلةٌ من المستودع، لا من الذاكرة).** `scripts/parallelise-options.py --batch <json> [--write]` يقفل على
أن «نصَّ المفتاح يبقى حرفيًّا عند `correctIndex`»، ويرفض المفتاحَ الأطولَ أو الأقصرَ وحدَه والتوسّعَ فوق 25 والحروف
غير اللاتينية والتكرار، ويَرفُض الكتابةَ كلها ما دامت مشكلةٌ واحدة قائمة. جُرِّبت الحمولاتُ الاثنتا عشرة
`--dry-run` أولًا (صفر مشاكل) ثم كُتبت: **113 بندًا في 7 ملفات**. الحمولاتُ الأقدم (2026-09-19) فُحصت لا فُرضت:
39/41 مطابقةٌ حرفيًّا، و2 (`b1-15-lq3` و`b1-15-m4`) تحمل الشجرةُ فيها التصحيحَ اللاحق ⇒ تُرِكت.

**المقيس بعد الاستعادة.** القرينة **39.28%** ✓ · الأسماء **4,874 / 1,381 / 89 / 3,404** · الأفعال
**1,257 / 134 / 4 / 1,119** · authoredTargets **1,470** · البوابات: `tsc` 0 · `lint` 0 · `vitest` **1,027/1,027**
في **156/156** · `handoff:check` **0** · `case:audit` **0 فجوات** عند `167645dd621b…` وبلا حركةٍ في العدادات
(19/23/57/44) — دليلٌ أن استعادةَ المشتتات لا تلمس إشارات الحالة. **بلا حركة كما هو متوقَّع:** وسيطُ الشرح **26**،
و`allUnder60Chars` **1,154**، و`under40Chars` **947**، والإنتاجية **50.9%**. **ودليلُ أن الشرح ما زال الطابور:**
`lesson:quality` يُخرج `status: fail` بالملاحظتين (26 مقابل 60، و50.9% مقابل 25%) لا بالقرينة.

**الوسمُ والبناء.** `scripts/bump-pack-generation.py` (جديدٌ في المستودع) نقل الحرفيةَ في **15 موضعًا / 7 ملفات**
ورفض أيَّ بقايا قديمة، و`Sync batch: v156` ثُبِّت في **13** وثيقةً لأن المُتحقِّق يشتقّ الجيلَ من اسم الكاش. البناءُ
خرج 0: **321/321** صفحة، بصمة `offline:size` **`7db270c8e04d`** مع **full 5,685,849**، `js:budget` ‏110/1,737,576/251,309،
`media:budget` ‏544/52,943,843/المنهاج 956,936. **و**`learning-architecture` لم تتحرك (`9b57e06047aa`) بينما تحرّكت
`content:audit` إلى **`12a2542b93b3…`** — أي أن بصمةَ الهيكل لا تقرأ نصوص التمارين، بخلاف بصمة المحتوى: فرقٌ يستعمل
للتشخيص لا للتجميل. **ملاحظةُ بيئة:** علّق «Running TypeScript» على 20 ميغابايت متاحة بلا مقايضة؛ `sudo` متاح هنا،
فأُنشئت `/home/user/.swapfile` (2G) فمرّ البناءُ وحده — تُضاف هذه إلى قياسات الافتتاح.

**الحزمُ محجوبٌ بحارسٍ لا بعطل.** `archive:delivery` يرفض بلا `src/data/{exercises,curriculum,micro-drills}`
المؤلَّفة (≥900/≥30/≥10؛ v155 حملت 1,202 ملفًا في `src/data`)، وهذه الشجرة عند **79 ملفًا** (التمارينُ أساليبُ داخل
`lessons-*.ts`، فلا تُستنتَج الـJSON منها)، ولا نسخة كاملة على الصندوق، و`data:recover backup` يرفض تصويرَ شجرةٍ
ناقصة. ⇒ **لا ZIP ولا `sha256sum -c` في هذا الدور**، والتعافي عند المالك:
`node scripts/recover-src-data.mjs restore --from <zip> --apply` ثم `content:audit:write` ثم `archive:delivery`.
الدرس المُستفاد من التسلسل كلّه: **القياسُ أولًا ثم القرار** — ثلاثُ مقولاتٍ نُقِضت بمقياس (أن البناء محجوب، أن
`case:audit` محجوب، أن الأعداد انكسرت من الشروح)، ومقولةٌ واحدة صمدت لأنها قِيست: الحزمُ محجوب بفقدان JSON التمريني.


## 4ef) جيل v156.1 — الدفعة العاشرة: الوسيط تحرّك 26 ⇒ 27 والمسافة 390 ⇒ 366، والتراجع الثامن وُزِن بالقياس (2026-09-22)

**الدفعة.** 24 بندًا (miniTest ‏10 · إصغاء 9 · قراءة 5) رُفعت من 15–17 إلى **186–212** حرفًا
(392 ⇒ 4,835). **قرارٌ منهجيٌّ وثّقته الأداة:** الفاحصُ لم يُنسخ — بل **رأسُ `a2e9.py` نفسه
مُنفَّذٌ بـ`exec`** (`import` للعقد لا كتابته من جديد): المدى 156–212، `oldLen` من الشجرة، كلمةُ
الحالةِ تبقى، رفضُ البالغِ 60+. أربعُ جولاتِ تمشيط (21⇒5⇒2⇒1) إلى `validator: 0 violations` —
والأطولُ منها 212 حرفًا (سقفُ الفاحص) عند `a2-03-rq2`.

**المقيس.** تحت 60 (الجماعة 1,250): **1,015 ⇒ 991** · المسافة **390 ⇒ 366 = 16 دفعة** · وسيطُ
الشرح **27** · `under40Chars` ‏**923** · العريض **1,130/1,733** · `allMedianChars` ‏**33** · A2
بالمستوى **173**. **ثباتٌ مُثبتٌ بالقياس:** القرينة 39.28% · الإنتاجية 50.9% · الاستماع 93 كلمة ·
**العداداتُ المعجمية** 4,874/1,381/89/3,404 و1,257/134/4/1,119 — والرسالةُ العملية: **الشرحُ
يرفع جماعةَ الوسيط بلا أن يلمسَ بوابةَ العدّادات**، فكلُّ دفعاتِ الشرح قادمةٌ «مجانًا» من حيث
الحُرّاس، وبالثمن الوحيد: وقتُ التأليف.

**التراجعُ الثامن (نصفِيّ).** بعد يومين من خضرةٍ كاملة تراجعَ الصندوقُ: `node_modules` صفرًا،
ملفاتُ b1/b2 رجعَت (القرينة 48.16% من جديد)، 5 ملفاتِ حرفيةٍ رجعت جيلَ v155 (4 فحوصاتٍ كاش
أحمرَت)، وسجلّ v156 مُمحى — وصمدت a2 والوثائقُ وحرفيةُ `sw.js`. **التمييزُ كان بالقياس لا
بالذاكرة:** `lesson:quality` فضحَ ملفاتِ b1/b2، وفحوصاتُ الكاش فضحتِ الحرفيات، و`distance-to-close`
أثبتَ أن a2 سليمة. ثم: `npm ci` ⇒ 12 حمولة ⇒ 9 مواضع ⇒ إعادةُ كتابة السجل ⇒ القياسُ الكامل
(156/156 · 1,027/1,027 · `handoff:check` 0). **والبيئةُ فضحت نقطةَ ضعفٍ ثانية:** `/home/user/.swapfile`
ينمحى مع كل صندوقِ لقطة — فأيُّ بناءٍ دون فحص المقايضة أولًا يعيد حادثةَ «Running TypeScript»
(تكررَت مرتين في دورين). **التثبيتُ المقترح في البروتوكول:** أولُ سطرَين من أي جلسة:
`test -e /home/user/.swapfile || { sudo -n fallocate -l 2G /home/user/.swapfile && sudo -n chmod 600
/home/user/.swapfile && sudo -n mkswap /home/user/.swapfile && sudo -n swapon /home/user/.swapfile; }`.

**البناءُ أُعيد** بعد الدفعة (المحتوى تغيَّر): خروج 0، 321/321، بصمة `offline:size`
**`c71d54038559`** مع **full 5,692,835**، `js:budget` ‏110/1,739,688/251,309، المنهاج 958,946 gzip.
و`content:audit` تحرّك إلى **`6b5b28eb3104…`** بينما `learning-architecture` بقيت `9b57e06047aa` —
نفسُ فرقِ البصمتين المؤكَّد في v156: بصمةُ المحتوى تشمل الشرحَ والمشتتات، وبصمةُ الهيكل لا.
**الحزمةُ محجوبةٌ على حالها** (لا `src/data` تمريني)؛ لا ZIP ولا `sha256sum -c`.

## 4eg) اقتباساتُ الشروح: صنفُ خطأ لم يكن مقيسًا (v156.2)

`explanationAr` كان يُقاس **طولًا** (وسيط ≥ 60) و**أسلوبًا** (لا صياغةَ موضعية، لا تكرار)، ولم يكن
يُقاس **صدقًا**: هل الصيغةُ الألمانيةُ التي يقتبسها الشرحُ بين «…» موجودةٌ فعلًا أمام المتعلّم؟

القياسُ الأول أعطى **164** بلاغًا؛ أكثرُها كان عيبًا في الأداة لا في المحتوى (`fill-blank` يُخفي
الجملةَ في `template` + `acceptedAnswers`، واقتباسُ الجملةِ **المحلولة** صحيحٌ تعليميًّا). بعد
تركيبِ الجملة المحلولة ⇒ 53، وبعد تمييزِ جنسِ التمرين ⇒ 49، وبعد إعفاءِ «البناءِ حول خيار» ⇒ **6**
أخطاءٍ حقيقية، ثم **0** بعد التصحيح مع **49** مثالًا تقابليًّا مشروعًا.

**الدرس المنهجي:** حين يُخرج حارسٌ جديد عشراتِ البلاغات، البدايةُ عيّنةٌ تُقرأ يدويًّا لا عتبةٌ
تُرفع. هنا كانت أولُ عيّنةٍ من ثلاثة بنود كافيةً لكشف أن الأداةَ لا تقرأ `template`، وهو ما وفّر
تصحيحَ 115 بلاغًا زائفًا.

**الأثرُ الأخطر كان من صنعنا:** إعادةُ كتابة المشتتات لخفض القرينة (v156) نجحت في هدفها
(48.16% ⇒ 39.28%) لكنها تركت ثلاثةَ شروحٍ تُسمّي مشتتاتٍ **حُذفت**. أيُّ إعادةِ كتابةٍ للخيارات
تُلزِم بعدها `npx tsx scripts/audit-explanation-quotes.ts`؛ وقد أُضيف ذلك إلى إجراء `parallelise`.
