# دفعة B1 الثالثة — شروحات (4): 72 بندًا ⇒ مستوى B1 مكتمل (168/168)

التاريخ: 2026-09-18 · الكاش: `dwnb-full-pack-v130` (staging/previous v129) · البناء 321/321

## ما أُنجز
أُعيد تأليف 72 شرحًا في تمارين B1 بأربع دفعات (24 ثم 12 ثم 12 ثم 24) بالنمط المعتمد: القاعدة، ثم شكل البديل الخاطئ، ثم «فحص ذاتي».
ثمّة 8 شروحات طُبّلت أولًا ثم قُصّت إلى النطاق، وشرح واحد (`b1-20-e3`) كان يحمل لفظ «الأخير» فاستُبدل.

## القياس قبل وبعد (من `reports/lesson-quality-audit.json`)
| المقياس | قبل | بعد |
| --- | --- | --- |
| `feedback.allUnder60Chars` (من 1,733 بندًا) | 1,518 | 1446 |
| `feedback.allMedianChars` | 27 | 27 |
| `feedback.allMissing` | 0 | 0 |
| `multipleChoice.under40Chars` (من 1,250) | 1,087 | 1070 |
| تمارين B1 بشرح ≥60 حرفًا | 96/168 | **168/168** |
| شروحات B1 خارج 156–212 حرفًا | 8 | 0 |
| أطول تلميح مفتاح (`longestOptionIsKeyPct`) | 55.2% | 55.2% (بلا تغيير) |
| خلط المواضع A/B/C/D | 25.92/23.84/24.4/25.84 | 25.92/23.84/24.4/25.84 (بلا تغيير) |

## الحراسات
`scratch/guardscan.ts b1` ⇒ flagged **0** (لا لفظ موضع، لا اسم حالة إعرابية).
0 شرح مكرر بطول ≥60 · 0 داخل نص إنجليزي · `acceptedAnswers` لم تُمَسّ · `promptAr` لم يُمَسّ في هذه الدفعة.

## انزياح مُتحقَّق منه وأُعيد تثبيته
`case:audit` أنزل `discoveredSignals.controlled` من 26 إلى **24** لأن شروحات جديدة لم تعد تسمّي الحالة. أُعيد تثبيت الرقم في `tests/unit/meaning-first-case.test.ts` وحده، والمُدقِّق لم يُرخَّ: `ok: True` و0 فجوات.

## البوابات على البناء الجديد
- `tsc --noEmit` : 0 خطأ
- `npx vitest run` : 150 ملفًا / **985** اختبارًا (السجل `vitest.log`)
- `npm run lint` : 0
- `npm run handoff:check` : 0 انحراف (يقبل `Offline cache: dwnb-full-pack-v130`)
- `npm run review:packet` : 3,277 سجلًا / 17 ورقة / 196 مشعرًا / 1,104 نصًّا غير محسوم / 16 نطاقًا
- البناء: 321/321 صفحة، 318 مسار Offline، حزم a1:58 a2:58 b1:58 b2:219 full:318
- `offline:size` : بصمة `c79a2e90cefb` (a1 2,355,104 · a2 2,422,794 · b1 2,492,369 · b2 3,812,536 · full 5,532,239)
- `js:budget` : 107 مقاطع / 1,688,569 gzip / أكبر ملف 636,998
- `media:budget` : 544 ملفًا / 52,943,843 بايتًا · curriculum 919,675 gzip
- بصمة المحتوى المشتركة: `d6093844630aa646956d6000e914abc3315064ad3741613da0680d652ca02fc6` · بصمة التشابه `efdb019376f3…` (بلا تغيير)

## المتصفح (`e2e-subset.log`, `e2e-fullpack.log`)
6/6 نجحت في 1.3 دقيقة (دورة درس كاملة 14 مرحلة، مختبر الكتابة، بداية المبتدئ — على `chromium` و`mobile-chromium`).
اختبار الحزمة الكاملة `opens unvisited lessons` نجح وحده في 32.9 ثانية على `chromium`.
ملاحظة صدق: محاولة أولى للمجموعة المكوّنة من 8 اختبارات انتهت بمهلة وانحرافين؛ السبب المُتحقَّق منه كان خادم `next-server` يتيمًا على منفذ 3100 يستهلك الذاكرة (1.9 جيجابايت إجمالًا)؛ بعد إنهاءه رجعت الاختبارات ناجحة. لا علاقة للمحتوى بالأمر.

## غير منجز
- البواقي على مستوى التمارين: B2 **76** · A2 **168** · A1 **167**؛ والباقي (1,035) في أسئلة القراءة/الاستماع/`miniTest`.
- تلميح «الأطول=الأصح» ما زال 55.2% مقابل حد ≤40% (P1-397)؛ يحتاج تحرير مشتتات.
- المراجعة البشرية المستقلة 0/3,277؛ تقييم الإنتاج استرشادي؛ تواريخ telc تُتحقَّق عند البناء.
- مسح عام لكل المستويات للكلمة العربية «شيء» داخل نصوص ألمانية ما زال غير مقاس.
