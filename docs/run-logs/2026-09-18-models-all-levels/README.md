# دفعة 2026-09-18 — إدخال 68 نموذج كتابة إلى نطاقاتها (A1 · A2 · B1)

قياسات هذه الدفعة (لا رقم هنا متوقع؛ كلها مخرجات أوامر):

| الأمر | النتيجة |
| --- | --- |
| `npx tsc --noEmit` | 0 أخطاء |
| `npm run lesson:quality` | 1250 بندًا مُصحَّحًا · خلط 25.92/23.84/24.4/25.84 · **مخالفات النطاق 0** (من 68) · إجابة وحيدة 51.94% · وسيط الشرح 23 · تلميح 55.2% |
| `npm run lesson:quality:audit` | `Wrote reports/lesson-quality-audit.json.` (‏`statedRangeTasks` ‏92، `violations` 0) |
| `npx vitest run` (كامل) | **150 ملفًا / 985 اختبارًا** نجاح — بعد إعادة تثبيت رقمين منزلقين في اختبارين |
| `npm run lint` | خروج 0 |
| `npm run build` | خروج 0 · 321/321 صفحة static/SSG |
| `offline:size` (postbuild) | بصمة `3109f1f42266` · a1 2,342,297 · a2 2,409,991 · b1 2,451,290 · b2 3,799,673 · full 5,491,048 |
| `js:budget` | 107 مقاطع / 1,675,785 gzip / أكبر ملف 624,214 |
| `media:budget` | 544 ملفًا / 52,943,843 بايتًا تحت 59,500,000 · المنهج 906,755 gzip |
| `npm run handoff:check` | خروج 0 (0 انحراف) — بعد تثبيت عدّادات الفجوات المعجمية في المُتحقِّق |
| `npm run review:packet` | 3,277 سجلًا · 17 ورقة CSV · **196 صفًا موسومًا** (كان 832) · 1,104 نصًا غير مُتحقَّق · 16 نطاقًا |
| `source:audit --strict` | 18 سجلًا · 18 طازجة · 0 قديمة |
| `case:audit` | 19 عقدًا / 23 نظريًا / 57 مُتحكَّمًا / 44 تقييمًا / 0 فجوات |
| `secret:audit` | 0 نتيجة |
| `npm run lesson:quality` قبل الدفعة | مخالفات النطاق **68** (A1 20 · A2 24 · B1 24) |

## ما غُيِّر
- ملفات البيانات المعدَّلة في هذه الدفعة: **24 ملفًا** في `src/data` (وحدات A1 ‏8 وA2 ‏8 وB1 ‏8) — نصوص `modelDe` فقط، لا إضافة حقول ولا تغيير مفاتيح ولا تعديل في منطق التصحيح. (ستة ملفات B2 في `src/data` عليها تعديلات الدفعة السابقة b2-27.)
- بصمات أُعيد تثبيتها: 6 تقارير `:write`، و`offline fingerprint`، و`content SHA` ‏`f7fd595cdbe9…414457`، وكاش `dwnb-full-pack-v127` (staging/previous v126) في `public/sw.js` و`scripts/verify-continuation-handoff.mjs` و3 اختبارات وحدة و`tests/e2e/critical-flows.spec.ts` والبرومبتين و`PROJECT_STATUS.md`.
- اختبارات أُعيد تثبيت أرقامها لانزلاق حقيقي: `tests/unit/lexical-target-gap.test.ts` (أسماء 4,653/1,320/3,244/1,409 · أطر 1,180/1,042) و`tests/unit/academic-content-governance.test.ts` (1,320).

## الحادثة المسجَّلة (وليس إخفاؤها)
كتابة جُمل b1-17 سقطت داخل نص b1-16 لأن السكربت كان يبحث عن آخر `modelDe` **يسبق** النص المطابق، والدرس b1-16 كان آخر نموذج قبله داخل نفس الملف. كُشفت بالمقارنة بين عدد الكلمات المطبوع في الطباعة المسبقة (123) والعدد المقروء وقت الكتابة (186)، ثم أُصلحت بحذف الجُمل من b1-16 وبإدراجها في b1-17 بمسح **أمامي** من معرّف الدرس. القياس النهائي: b1-16 ‏186 · b1-17 ‏200 · كلاهما داخل نطاقه.

## غير منجز
- اختبار المتصفح الكامل لـ `dwnb-full-pack-v127` لم يُشغَّل (المتصفحات ومكتباتها تُعاد من `restore-e2e.sh` بعد كل تفكيك للبيئة).
- الشرح تحت 60 حرفًا (1,614 بندًا)، والبدائل الـ21، وتلميح 55.2%، وتمديد الاستماع.

## اختبار المتصفح (مقياس لا متوقع)
- مجموعة مستهدفة بأربعة عناوين (A1 مبتدئ · مختبر الكتابة · درس كامل 14 مرحلة · الحزمة الكاملة دون اتصال) على مشروعَي desktop وmobile: **6 نجاح / 2 فشل** في 13.8 دقيقة (`e2e-subset.log`).
- الفشلان هما اختبار الحزمة الكاملة في المشروعين عند السطر 1762-1763 (`packCard` ثم `todayReadiness`)، وسببه ترتيب الأمر لا المحتوى: شُغِّل الاختبار على بناء **سابق** لترقية اسم الكاش إلى `v127`.
- بعد `npm run build` (بصمة بناء جديدة `3109f1f42266`) أُعيد الاختبار نفسه معزولًا على مشروع chromium: **نجح في 36.4 ثانية** (`e2e-pack-isolated.log`). نسخة mobile لم تُعَد تشغيلها.
- الحزمة الكاملة (82 اختبارًا) لهذا الإصدار **لم تُشغَّل حتى نهايتها**؛ لا يُدَّعى خضراء المتصفح كاملة. البيئة احتاجت `restore-e2e.sh` (chromium ‏114.7 MiB + `LD_LIBRARY_PATH`)، ونجح فحص الإقلاع بـ `missing_libs=0`.
