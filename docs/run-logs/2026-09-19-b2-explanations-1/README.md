# دفعة B2 الأولى — 76 شرحًا ⇒ مستوى B2 مكتمل في التمارين (173/173)

التاريخ: 2026-09-19 · الكاش: `dwnb-full-pack-v131` (staging/previous v130) · البناء 321/321 · بصمة Offline `65b2e7277c28`

## ما أُنجز
- 76 شرحًا في تمارين B2 أُعيد تأليفها بثلاث دفعات: 24 ثم 24 ثم 28، بالنمط المعتمد (القاعدة، شكل البديل الخاطئ، «فحص ذاتي»).
- قبل التطبيق مُسحت قائمة الانتظار بطول البند؛ النص الذي طلع خارج 156–212 حُسم قبل الكتابة لا بعده (`b2-12-e1` قُصّ من 214 إلى 192).
- نصّان صُحّحا في المسودة لأنهما كانا يحملان لفظًا مواقعيًا (`b2-17-e5` بصيغة «الأولى»، و`b2-20-e2` بصيغة «الثاني»).
- قياس جانبي أُغلق: البحث عن «شيء» داخل نصوص التمارين أعطى 9 مواضع، كلها عربية صريحة (صياغات سؤال أو ترجمة مشتتات) — لا حشو عربي داخل جملة ألمانية. الرقم: 9 مواضع.

## القياس (من `reports/lesson-quality-audit.json`)
| المقياس | قبل | بعد |
| --- | --- | --- |
| `feedback.allUnder60Chars` (من 1,733 بندًا) | 1,446 | 1370 |
| `feedback.allMedianChars` | 27 | 27 |
| `feedback.allMissing` | 0 | 0 |
| `multipleChoice.under40Chars` (من 1,250) | 1,070 | 1062 |
| تمارين B2 بشرح ≥60 حرفًا | 97/173 | **173/173** |
| تمارين B1 | 168/168 | 168/168 |
| `writing.violations` / المهام بنطاق معلن | 0/92 | 0/92 |
| أطول تلميح مفتاح | 55.2% | 55.2% |

المتبقي على مستوى التمارين: A2 168 · A1 167، والباقي 1,035 في أسئلة القراءة والاستماع و`miniTest`.

## إصلاح بنيوي: بوابة `js:budget` انكسرت وعولجت بالهندسة
- بعد الدفعة الأولى من هذه الجلسة فشل البناء: أقصى مقطع 642,417 بايت gzip مقابل سقف احتياطي 637,500 (الحد الفعلي 750,000 بهامش 15% في ADR-049). السبب: بيانات المنهج كلها في مقطع مشترك واحد ينمو مع كل شرح.
- المعالجة: تقسيم `src/data/lessons-<level>-module*` إلى مقطع لكل مستوى في `next.config.ts` (`cacheGroups` مع `enforce: true`). لم يُمَسّ نصّ الحدّ ولا سكربت الميزانية.
- النتيجة المقاسة: 110 مقطعًا / 1,705,576 gzip إجمالًا / أقصى مقطع **243,237**. حِزم Offline: full ‏5,590,306 (كان 5,532,239) لأن صفحات HTML تشير أربعة مقاطع إضافية؛ `media:budget` بلا تغيير جوهري (544 ملفًا / 52,943,843 · curriculum 925,548 gzip).
- اختبار جديد: `tests/unit/curriculum-chunk-split.test.ts` (3 حالات) يثبّت التقسيم ويمنع تليين `scripts/audit-js-budgets.mjs`.

## الحراسات والانزياح
- `scratch/guardscan.ts b2`: 16 علمًا، كلها نصوص سابقة محميّة (تذكر الموضع صراحةً فلا تُدْوَر) — 0 منها من هذه الدفعة.
- 0 شرح خارج 156–212 في الجديد، 0 اسم حالة إعرابية، 0 تكرار، ولم تُمَسّ `acceptedAnswers`.
- `case:audit`: ok true و0 فجوات، لكن `discoveredSignals.controlled` نزل 24 → **23** لأن `b2-09-e4` كفّ تسمية الحالة. رُفِع التثبيت في `tests/unit/meaning-first-case.test.ts` وحده.
- ما تُرِك عمدًا: `b2-22-e7` بطول 220 حرفًا (فوق النطاق التحريري، تحت أي بوابة).

## البوابات
- `npx tsc --noEmit` : 0 خطأ
- `npx vitest run` : 151 ملفًا / **988** اختبارًا (`vitest.log`)
- `npm run lint` : 0 · `npm run handoff:check` : 0 انحراف
- `npm run build` : 321/321 (`build2.log`, `build4.log`) — البناء الأول `build.log` سجل الفشل بالميزانية، والثاني `build3.log` قُطع بأداتي لا بكودك
- `review:packet` : 3,277 سجلًا / 17 ورقة / 196 مشعرًا / 1,104 نصًّا غير محسوم / 16 نطاقًا
- بصمات: content SHA `9aeac5e5649949f827b40e9128bb8ed3c75f883c3d56080e04163ae7549777af` · `case` ‏bf7ad78b1d49 · `similarity` ‏efdb019376f3 · `language` ‏460f84c70347 · `lexical` ‏439956b453f0

## المتصفح
- المجموعة المحتواة على المحتوى: **6/6** نجحت في 1.2 دقيقة (`e2e-subset.log`) على سطح المكتب والجوال: دورة درس بأربع عشرة مرحلة، مختبر الكتابة، بداية المبتدئ.
- تثبيت الحزمة الكاملة: المحاولة الأولى فشلت بعد انتظار 360 ثانية (`e2e-fullpack.log`)؛ السبب المُتحقَّق منه نوافذ Chromium شاردة من تشغيلي السابق أنهكت الذاكرة (1,984 MB إجمالًا). بعد تنظيفها نجحت **1/1 في 30.0 ثانية** (`e2e-fullpack-retry.log`).
- مجموعة 82 اختبارًا الكاملة لم تُشغَّل لهذا الكاش؛ التشغيل الكامل يتطلب نافذة أطول من مدة الأداة هنا.

## ما لم يُغلَق
- A2 168 ثم A1 167 تمرينًا، ثم 1,035 بندًا في أسئلة المراحل.
- تلميح «الأطول = الأصح» 55.2% مقابل حد ≤40% (تحرير مشتتات، لا تدوير).
- المراجعة البشرية المستقلة 0 من 3,277؛ تقييم الكتابة والمحادثة استرشادي؛ الشهادة يمنحها الامتحان وحده.
