# دفعة البدائل الإنتاجية رقم 2 — 2026-09-19 (v134)

## لماذا هذه الدفعة

كانت البوابة تقول: تمارين إنتاجية بإجابة وحيدة مقبولة 51.9% والسقف 25%.
التصنيف الموجود في `docs/adr/ADR-078-explicit-accepted-variants-policy.md` قسّم البنود
الـ201 هكذا: 147 جملة قصيرة لا عنصر ثانٍ فيها، و33 ترتيبها مرتبط بمجموعة المصدر أو بنص
فرعي، و17 لا تحتمل تقديم جرّة خلفية، و4 يفكك التقديم المقترح رصيد الرموز، و**صفر بندًا
قابلًا للإضافة بثقة آلية**. فالعمل المطلوب كان 21 بندًا بقرار مؤلِّف، لا 104 بنود كما
قدّرتُ أول مرة؛ وهذا التصحيح مسجَّل هنا حتى لا تُقرأ الدفعة على أنها تقصير مؤجَّل.

## ما نُفِّذ

بنود أربعة، خمسة بدائل، كل بديل مذكور في `explanationAr` في السطر نفسه:

- `b1-06-e3` (ترتيب كلمات): أُضيف `Unser letztes Projekt ist ein Beispiel dafür`.
- `b2-14-e3` (كتابة): أُضيف `Der Termin wurde anschließend bestätigt`.
- `b2-15-e4` (كتابة): أُضيف `Er verschenkte neunzig Minuten, nachdem er die
  Aufgabenstellung überflogen hatte.`
- `b2-24-e4` (صحة/خطأ مع تصحيح): أُضيف `Am Montag wurde der Antrag abgelehnt, weil die
  Anlagen fehlten.` و `Weil die Anlagen fehlten, wurde der Antrag am Montag abgelehnt.`

طُبِّعت الأرقام من الواقع: البند `b2-24-e4` صار به 3 إجابات مقبولة، والبقية 2.

## ما رُفض وعلة الرفض

- 159 بندًا من نوع «املأ الفراغ» فيها كلمات أخرى صحيحة نحويًا (`ohne`/`mit`،
  `keinen`/`einen`، `der`/`einer`) لكن الإجابة الأخرى تغيّر المعنى أو تُسقط البنية
  المُعلَّمة؛ وإضافتها لمجرد تحسين النسبة هي قلب رايات في صيغة بيانات (قاعدة 2 في ADR-078).
- `b2-16-e4`: بدائله الوحيدة تقدّم `als ob` وتُبقي الجملة غير طبيعية، فرُفضت.
- المسبار الآلي للترتيب: نسختان من قاعدة الفعل الثاني (على الرموز، ثم على القطع) أعطتا
  164 مرشحًا منها 77 إنذارًا؛ بالفحص اليدوي كانت الإنذارات كلها عيوب المسبار لا عيوب
  البيانات، فلم تُضف بوابة ترتيب جديدة ولم تُغيَّر أي بيانات بسببها.

## القياس بعد التنفيذ

`lesson:quality`: الحالة fail، والبنود الثلاثة: تلميح الأطول 55.2%، والإجابات الوحيدة
50.9% (197 من 387 بعد أن كانت 201)، ووسيط الشرح 24 حرفًا. البوابة تبقى حمراء عمدًا:
السقف لم يُرخَ ولم تُلمَّس العتبات.

إثبات القبول تم عبر المسار الحقيقي `evaluateExercise` → `compareAccepted`:
الإجابة الأصلية مقبولة، والبديل الجديد مقبول، وصيغة السحب-والإفلات بحروف صغيرة مقبولة،
والخطأ الشائع ما زال مرفوضًا — أي اختبار عكسي لكل بند.

## البوابات

`tsc` بـ0 خطأ · `lint` بـ0 · `vitest` كامل · `handoff:check` · البناء 321/321 ·
`content:audit` بصمة محتوى `6865b7f3cbce1dbf22d91df3511c690e834db24ad066f29e76e1dd50fd400983` ·
والبصمات الأخرى لم تتغير (النحو/الدعوى/القصة/اللغة/الاستراتيجية/الأوضاع) · `review:packet`
3,277 سجلًا في 17 ملفًا و268 مستنسخًا مستخرجًا آليًا — ولا تسمّى مراجعة بشرية.

## المتاح afterward

الـ17 بندًا الباقية تحتاج قرارات مؤلِّف أُخرى (بعضها بلا بديل طبيعي)، وحمل القراءة له
دفعة ثانية لم تُنفَّذ، وتحرير المشتتات (P1-397) لم يبدأ. لا شيء من هذا مقفل.

## ملاحظة إجرائية (لا تُكرَّر)

رفع الكاش من v133 إلى v134 لم يمس `tests/unit/offline-curriculum-rollback.test.ts` لأنه
يثبّت اسم `dwnb-full-pack-previous-v132` وحده، فلا يحتوي السلسلة `dwnb-full-pack-v133`
التي بُحث بها. سقط الاختبار أول مرة ثم صُحح التثبيت إلى `previous-v133`؛ قائمة الرفع يجب
أن تبحث عن `dwnb-full-pack-[a-z-]*v13[0-9]` لا عن الاسم الحالي وحده.

## اختبار المتصفح: كشف تثبيتًا قديمًا من الدفعة السابقة

أُجري تشغيل كامل لملف `tests/e2e/critical-flows.spec.ts` (41 اختبارًا) لا مجرد مجموعة
فرعية، فوقع الاختيار على تثبيتين قديمين تركتهما دفعة حمل القراءة (v133) دون أن يُكتشَفا
لأن تلك الدفعة شغلت 6 اختبارات فقط:

- سطر 176: كان يتوقّع «أسماء هدف إضافية موثقة · 8» في `b1-01`، والواقع **21** لأن الدفعة
  السابقة أضافت مراسي اسم للدرس (25 مرسى = 4 أساسية + 21 إضافية). ثُبِّت بعد التحقق من
  `nounsByLesson["b1-01"]` الذي يقرأ منه المكوّن نفسه، لا من تقدير.
- سطر 198: كان يتوقّع حجم فهرس البحث ‏3,512 والواقع **3,621** (نفس الرقم المثبَّت في
  `tests/unit/search.test.ts:9`).

النتيجة بعد التصحيح: `41 passed (7.8m)` على سطح المكتب بخروج 0، والسجل في
`e2e-desktop-full.log`، ثم تشغيل مماثل على مشروع الجوال. لا شيء حُذف ولا أي اختبار وُسِم
تجاهليًا؛ التثبيت رُفع إلى الرقم المقيس.

## لاحق في نفس اليوم (v135)

كشف تشغيل المتصفح الكامل تعليقًا في تثبيت الحزمة على مشروع الجوال؛عالِج في
`docs/run-logs/2026-09-19-pack-install-reconcile/README.md` والوثيقة
`docs/OFFLINE-PACK-INSTALL-FLAKE-2026-09-19.md`.
