# دفعة 2026-09-19 — زحمة الكلمات المجهولة في القراءة + إصلاح مصحّح القياس (الطابع `dwnb-full-pack-v133`)

## لماذا هذه الدفعة

قرّر المسؤول التقني تحويل الأولوية من طول الشرح إلى ما يكسر القراءة فعلًا: نصّ B2 كان يترك للقارئ
كلماتٍ غير معروفة وسيطًا عند 42%، مع أقصى 61.4% في `b2-09`. القياس القديم كان أيضًا معيبًا (انظر أدناه)،
فبدأنا بتصحيح المقياس ثم بعلاجه بالمحتوى.

## ما تغيّر في البيانات

- ‏109 إدخالًا في `reading.glossary` لأربعة دروس: `a1-02` ‏17، `b1-01` ‏23، `b2-06` ‏30، `b2-09` ‏39.
- ‏53 مرساة اسم (جنس + جمع + معنى عربي) في `src/data/lexical-grammar-a1.ts` و`-b1.ts` و`-b2.ts`.
- لم يُمسَّ أي شرح ولم تُغيَّر أي عتبة.

## تصحيح عيب قياس في `scripts/generate-lesson-quality-audit.ts`

`fold()` كان يبدأ بـ`normalize("NFKD")` فتتفكك اللوترة إلى حرف + علامة، ثم تُستبدل العلامة بمسافة:
«Produktivität» صار كلمتين. لذلك كانت سطور استبدال ä/ö/ü/ß ميّتة، وكانت الأرقام مشوّهة في موضعين:
حمل القراءة، وتتبّع الأهداف. أُصلح الترتيب (NFC ثم استبدال صريح ثم تجريد)، واستُبعدت الرموز الرقمية
من عداد «الكلمات» لأن الأرقام ليست مفردات. الفعلان مُثبّتان في التقرير نفسه بحقلين:
`legacyFoldMedianUnknownWordPct` و`definition: cumulative-taught-v2`.

كما أُضيف تعريف v2: الكلمة تُحسب مجهولة فقط إذا لم يعلّمها أي درس في المستوى **أو ما دونه**.
التعريف v1 كان يقارن نص B2 بمواد B2 وحدها، فحُسبت أفعال A1 معروفةً للمتعلّم ككلمات مجهولة.

## القياس من `lesson:quality`

| المقياس | قبل | بعد |
| --- | --- | --- |
| `reading.medianUnknownWordPct` | 42.0 بالتعريف القديم | **29.8** بالتعريف v2 |
| أوساط المستويات | 29.2 / 35.6 / 51.9 / 47.1 (v1) | A1 **28.9** · A2 27.6 · B1 **31.6** · B2 **30.5** |
| عدد النصوص فوق 45% | 4 | **0** |
| أقصى نص | 61.4% (`b2-09`) | **42.4%** (`b1-23`) |
| `a1-02` | 50.0% | **18.0%** |
| `b1-01` | 45.2% | **22.6%** |
| `b2-06` | 46.6% | **24.8%** |
| `b2-09` | 46.0% | **27.3%** |
| `objectives.lexicalReviewPrompts` | 19 | 26 (نتيجة stricter fold، لا تراجع محتوى) |

`allUnder60Chars` لم يتغيّر: **1,322** من 1,733 (هذه الدفعة لم تكتب شروحًا).

## أثر جانبي أُلغِي فورًا

إدخال شرح معجمي لاسم يرفعه إلى طابور `pending-human` في تدقيق المعجم. القياس: ‏4,653 مرشحًا و1,320
مغطاة و**89** منتظرًا قبل الدفعة؛ بعد الشروح المعجمية وحدها قفز المنتظر إلى **142**؛ بعد إضافة
المراسات عاد إلى **89** مع المغطاة 1,376 والمرشحين 4,685 والسياق 3,220.
`lexicalTargetGaps.nounAnchorCount` صار **1,297** و`totalRootObjects` **4,345**.
وبسبب 109 سطرًا معجميًا جديدًا نما فهرس البحث ثنائي اللغة: **3,621** بدل 3,512.
حزمة المراجعة البشرية: ‏3,277 سجلًا، والصفوف الموسومة **268** بدل 196 — السبب هو تشدّد تتبّع الأهداف
(26 بندًا) وليس أي تراجع لغوي؛ أعمدة القرار ما زالت فارغة.

## أوامر ونتائج

- `npm ci` → exit 0 (غاب `node_modules` عند الفتح).
- `npx tsc --noEmit` → 0 · `npm run lint` → 0 · `npx vitest run` → **151 ملفًا / 988 اختبارًا**.
- `content:audit:write` ثم `case` و`governance` و`similarity` و`language` و`lexical:strategy` و`study:modes` → الكل `ok: true` و0 مشكلات. بصمة المحتوى `f62de3e9a8feee1aa1213d75214153db8e5ef63daac508882875576af205540e`.
- ‏9 تثبيتات عُدّلت في الاختبارات بعد نمو المحتوى (عدّادات المراسات لكل مستوى، `totalRootObjects`،
  التثبيتات في `verify-continuation-handoff.mjs`، فهرس البحث، ومعرّف `a1-02-noun-16`). لا عتبة أُرخيت.
- البناء: المحاولة الأولى `build-stalled.log` (تعثّر في «Running TypeScript» عند 14 ميغابايت حرّة)،
  الثانية `build2.log` (نفس التعثّر)، الثالثة `build3.log` (`--max-old-space-size=640` ⇒ نفاد داخل V8 وأعلن `Failed to type check.`)، الرابعة `build4.log` **نجحت** بعد تحرير 159 ميغابايت من خدمات الجوفن
  وفيها `Finished TypeScript in 29.4s` و321/321 صفحة في 7.3 ثانية.
  لا تغيير في البنية: `js:budget` ‏110 / 1,712,758 / أكبر 244,943 · وسائط 544 / 52,943,843 · مناهج 933,160 ·
  بصمة `offline:size` ‏`37bbaf8e8c58` (a1 2,384,247 · a2 2,460,238 · b1 2,521,840 · b2 3,869,918 · full 5,611,048).
- المتصفح: مجموعة الستة **6 نجحت** في 59.3 ثانية، واختبار الحزمة الكاملة **2/2** في 46.2 ثانية.
- `npm run review:packet` → ‏3,277 / 17 / 268 / 1,104 / 16.

## ما لا تدّعيه الدفعة

لا بوابة حمل القراءة بعد: بدون مُلمْعِز (lemmatizer) يخلط المقياس بين تصريف معلَّم ومفردة جديدة،
فتحويله إلى ختم ملزم الآن يعني ختمًا يصرخ. رُفع ذلك إلى قائمة الأعمال مع السبب.
لم يُجرَّ نطاق الـ82 اختبارًا الكامل لهذا الطابع. ولا مراجعة بشرية، ولا دفع إلى `origin/main`.
المتبقي في مسار الشروحات: A2 120 ثم A1 167، و1,035 سلسلة مرحلة، وتلميح الطول 55.2% مقابل 40%.

## ختم جديد أُضيف في هذا الدور (لا تكذب الوثيقة على البصمة بعد اليوم)

`scripts/verify-continuation-handoff.mjs` صار يقرأ `public/offline-size-manifest.json` ويستخرج
`buildFingerprint.slice(0,12)` وحجم الحزمة الكاملة بتنسيق الآلاف، ثم يفرض وجودهما في
`PROFESSIONAL_CONTINUATION_PROMPT_AR.md` و`PROJECT_STATUS.md`. القياس: في الحالة الصحيحة
`handoff:check` exit 0، وبعد إفساد البصمة في الوثيقة عمدًا خرج بـ exit 1 والرسالة
`continuation prompt offline fingerprint is missing "37bbaf8e8c58"`، ثم عاد إلى 0 بعد الإرجاع.
هذه أول مرة يُغلق فيها ثقب «رقم مخطط قديم يمرّ رغم خضرة البوابات» الذي أُعلن في نقاش 2026-09-19.

بند مفتوح صغير ناتج عن هذا الدور: لا يوجد اختبار وحدة يحرس هذا الربط نفسه (تطلب ملفًا جديدًا
وإعادة تثبيت عدّاد الاختبارات في أربع وثائق). أُجِّل بتعمّد مع تسجيله، لا بإخفائه.
