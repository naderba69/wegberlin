# دفعة v150 — شروحات A2، الدفعة الثالثة (24 بندًا) — 2026-09-20

**البند**: P1-399 (شرح تصحيحي سببي لا يقل عن 60 حرفًا لكل بند). البند السابق P1-397 (قرينة «الأطول = الأصح»)
أُغلق قياسًا عند v149 وبقي مفتوحًا على البوابة المشتركة، والإنتاجية أحادية السلسلة (P1-398) قرار مؤلِّف لا
يُتخذ آليًا (ADR-078)، فكان العمل الجاري هو الشروح.

**النطاق**: 24 تمرينًا من A2 كانت أقصر شروحها بين 21 و25 حرفًا، اختيرت من أقصر 120 بندًا في A2
(`scratch/expl_detail.ts a2 0 24` طبع `TOTAL 120`). لم تُمَسّ `acceptedAnswers` ولا نصّ الخيارات ولا المفاتيح
ولا بنية الملفات؛ والتحرير جرى بأداة خارج المستودع ترفض أي انزياح عن الطول المتوقع.

**الصيغة**: مدى محريري 156–212 حرفًا؛ الواقع بعد التأليف 168–210 (فاق واحد خارج المدى). كل نص يذكر القاعدة،
ثم مثالًا ألمانيًا بين «»، ثم سبب فشل كل بديل مطروح في السؤال، ثم «فحص ذاتي: …؟».

**ما قبل وما بعد (من `reports/lesson-quality-audit.json`، `lesson-quality.txt`)**:
- `feedback.allUnder60Chars` ‏1,322 ⇒ **1,298** من 1,733 (الفرق 24 بالضبط؛ لا بنود أُخرى تحرّكت).
- `feedback.allMedianChars` ‏28 ⇒ **29** · `feedback.explanationMedianChars` ‏24 ⇒ **25** · `allMissing` صفر.
- `feedback.under40Chars` ‏1,047 ⇒ **1,035** من 1,250 (‏12 من البنود الـ24 اختيار من متعدد؛ الباقي
  fill-blank ‏7 وmatching ‏3 وword-ordering ‏1 وerror-correction ‏2).
- تمارين A2 بشرح ≥60 حرفًا: ‏48/168 ⇒ **72/168**؛ المتبقي على مستوى التمارين **A2 ‏96 ثم A1 ‏167**، والباقي
  **1,035** في أسئلة المراحل (المجموع 1,298 ✓).
- القرينة لا تتغير: **39.28%** (‏491 من 1,250) كما عند v149، وتحت السقف ≤500/≤40% (`cue-probe.txt`).

**الأداة والتطبيق**: `scratch/set_expl.py a2 <spec>` تحقق طول كل بند قبل الكتابة؛ الخرج **applied: 24 of 24
missing: []**، والانتقالات المطبقة (طول ⇒ طول) في `spec-a2-explanations-3.json` مع النص الكامل، و`builder-a2-explanations-3.py`
هو مولّد الدفعة وفاحصها (الفاحص رفض 13 مخالفة في المسودة الأولى: 12 نصًا خارج المدى ونصًا فيه «الثاني»، فأُعيد
التأليف قبل أي كتابة).

**انزياح حوكمة مقصود**: `discoveredSignals.controlled` في `reports/case-teaching-audit.json` ‏21 ⇒ **20**، لأن
شرحَي `a2-04-e1` و`a2-04-e2` كانا يسمّيان الحالة (‏Dativ) صراحةً. رُفِع التثبيت في
`tests/unit/meaning-first-case.test.ts` وحده (سطر واحد) كما فُعِل عند v131 مع `b2-09-e4`. البوابة `case:audit`
تخرج صفرًا وتطبع 19 عقدًا / 23 نظرية / 57 مضبوطًا / 44 تقييمًا / **0 فجوات** (`case-audit.txt`).

**البوابات (`writes.txt` و`vitest.txt` و`lesson-quality.txt`)**: `bash /home/user/tmp/writes.sh` (13 عنوانًا) ⇒
بصمة محتوى **99f930237c1083e5dbcf1f7dc317092f82110c415a43912b49c39b1959d64** · tsc منفرد صفر · lint صفر ·
vitest **155 ملفًا / 1,019 اختبارًا** (بلا اختبار جديد؛ لا تغيير في عدد الاختبارات عن v149) ·
`handoff:check` صفر (`handoff-check.txt`) · `lesson:quality` صفر بوسيط 25 · **`lesson:quality:audit` خروج 1**
بملاحظتين: «الإنتاجية أحادية السلسلة 50.9% (أقصى 25%)» و«وسيط الشرح 25 حرفًا (أدنى 60)»
(`lesson-quality-audit-strict.txt`) — فلا البوابة خضراء ولا البنود تُنقل إلى implemented.

**البناء وتسلسل البيئة (`environment.txt`)**: المبادلة ما تزال مرفوضة (`/usr/sbin/swapon` ⇒
`Operation not permitted`؛ `free -m` = 1,984 ميغابايت، Swap صفر). المحاولة الأولى بـ900 ميغابايت بعد تدفئة j
جرَت **قبل** أن يولّد Next ملفّات `.next/types`، فكان `.tsbuildinfo` غير مطابق: تعثّر «Running TypeScript» خمس
دقائق والذاكرة إلى 16 ميغابايت متاحة، فأُوقف (`build-stopped-by-agent.txt`، بلا `BUILD_EXIT`)؛ التدفئة بعدها
نجحت في 26 ثانية (‏`tsc-warm-before-next-types.txt` صفر السطور/‏`tsc-warm-after-next-types.txt`، 307,615 بايت)،
ثم `NODE_OPTIONS=--max-old-space-size=900 npm run build` ⇒ **خروج 0** في نحو 100 ثانية: **321 صفحة**
(SSG ‏9.7 ثانية)، بصمة `offline:size` **90a27131fd55** (a1 2,397,900 · a2 2,478,732 · b1 2,541,239 · b2 3,895,209 ·
full 5,649,071)، `js:budget` ‏110/1,725,725/251,309، `media:budget` ‏544 ملفًا و52,943,843 بايتًا والمنهاج
**945,047** gzip (نما لأن الشروح العربية جزء من حزم المصدر).

**الوثائق**: المسح v149 ⇒ v150 على 15 ملفًا مع تخطي `## 39)` في البرمت الرئيسي وسطر ‏`دفعة كسر القرينة في
`b2-19`` في `PROJECT_STATUS.md` وسطر `دفعة v149` في `DECISIONS.md` وفصل `## 4s)` في وثيقة النتائج وسطر 397 في
`P1_AUDIT.md`؛ فصول جديدة: `## 40)` في `PROFESSIONAL_CONTINUATION_PROMPT_AR.md`، `## 4t)` في
`docs/LESSON-QUALITY-FINDINGS-2026-09-17.md`، وADR في نهاية `DECISIONS.md`، وسطر v150 في `PROJECT_STATUS.md`؛
وأُعيد تثبيت §4 و§5 و§6 في `docs/AGENT-HANDOFF-PROMPT-AR.md`، ونُسخت وثيقة الاحتياط إلى
`docs/BACKUP-RESUME-PROMPT-2026-09-20-v150-AR.md` بحذف نسخة v149، ورُفع الكاش إلى `dwnb-full-pack-v150`
في سبعة ملفات، وأُعيد توليد فهرس `docs/run-logs/README.md` (25 مجلدًا بعد هذا المجلد).

**ما لا يُدَّعى**: لم يُشغَّل متصفح لهذه الدفعة (`README.txt` يحمل سطر `PLAYWRIGHT_EXIT=NOT_RUN`؛ آخر قياس v143
‏40 من 42)؛ ولا 82/82؛ ولا إغلاق P1-397 أو P1-399؛ ولا مراجعة بشرية (0 من 3,277)؛ ولا نشر (لا `.git`)؛ ولا
يُدَّعى أن حجم الحزمة أو بصمتها مذكوران هنا — البلوغ لهما في `wegberlin-full.zip.sha256`.
**الحزمة**: شجرة هذا المجلد **16** ملفًا (`README.md` + 15 سجلًا)؛ لا تُكرَّر هنا نصوص الدفعة المدفوعة لأن
`spec-a2-explanations-3.json` يحملها كاملة مع `builder-a2-explanations-3.py` القابل لإعادة التشغيل.
