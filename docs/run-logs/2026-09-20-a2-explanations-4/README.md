# دفعة v151 — شروحات A2، الدفعة الرابعة (24 بندًا) + بينّة قرار P1-398 — 2026-09-20

**البند**: P1-399 (شرح تصحيحي سببي لا يقل عن 60 حرفًا لكل بند)، الدفعة الرابعة على التوالي؛ وفي نفس الدور جُمعت
البينة الرقمية لبند P1-398 (تعدد البدائل المقبولة في التمارين الإنتاجية) لأن المضي فيه ليس عمل تحرير آلي.

**النطاق**: 24 تمرينًا من A2 كانت شروحها بين **25 و28** حرفًا (أضطرّ ما بقي في المستوى؛ الاستقصاء بطبع
`TOTAL 96` قبل الدفعة)، فكتبت نصوصًا **174–212** حرفًا بنفس نمط المستودع: القاعدة، مثال ألماني بين «»، سبب فشل
كل بديل مطروح، ثم «فحص ذاتي: …؟». لم تُمَسّ الخيارات ولا `acceptedAnswers` ولا أي ملف آخر؛ والتحرير بأداة
خارج المستودع ترفض أي انزياح عن الطول المتوقع.

**فاحص ما قبل التطبيق** (`builder-a2-explanations-4.py`): رفض المسودة الأولى بتسع مخالفات — تسعة نصوص خارج
المدى 156–212 (183 إلى 236)؛ ثم بمسودّتين أخريين خفّضت إلى 214 و213 فقُصّت عبارة من كل منهما. صفر مخالفة قبل
الكتابة، والفحص يشمل أيضًا: عبارة موضعية بـ`scripts/rebalance-answer-positions.ts:21` (مطابقة جزئية، فـ«الأولى»
و«الثاني» ممنوعتان)، وتسمية حالة بـ`meaning-first-case.ts:5-7`، وتكرار النصوص، وحروف CJK. الطول المطبَّق تحقق منه
`scratch/set_expl.py` لكل بند ⇒ **applied: 24 of 24 missing: []** (الانتقالات 25⇒206، 26⇒200، 28⇒209 …).

**قبل/بعد** (`reports/lesson-quality-audit.json`، و`lesson-quality.txt`):
- `feedback.allUnder60Chars` ‏1,298 ⇒ **1,274** من 1,733 (الفرق 24 بالضبط؛ لا بند آخر تحرّك).
- `feedback.allMedianChars` ‏29 ⇒ **30** · `feedback.explanationMedianChars` ‏25 ⇒ **25** بلا حركة — لأن 4 فقط من
  البنود الـ24 كانت اختيارًا من متعدد، والمؤشر يخصّ شرح الاختيار.
- `feedback.under40Chars` ‏1,035 ⇒ **1,031** (من `feedback.ofItems` = 1,250). **مفتاح**: المفتاح `feedback.under40Chars`
  لا `multipleChoice.under40Chars`؛ السجلات القديمة تذكر الأخير لأن المؤشر كان تحت `multipleChoice` قبل توسيعه.
- تمارين A2 بشرح ≥60: ‏72/168 ⇒ **96/168**؛ والمتبقي على مستوى التمارين **A2 ‏72 ثم A1 ‏167** والباقي **1,035** في
  أسئلة المراحل — والفصل صحيح: 72 + 167 + 1,035 = **1,274** ✓.
- قرينة «الأطول = الأصح» **39.28%** (491 من 1,250) بلا حركة (`cue-probe.txt`)؛ حارس الدروس الموازية على **15** درسًا
  (16 اختبارًا في ملفه)؛ `case:audit` بصمة ‏167645dd621b و0 فجوات و`discoveredSignals.controlled` ما يزال **20**
  (لم تُسمَّ حالة في أي نصّ جديد)؛ الفجوات المعجمية وأطر الأفعال بلا حركة.

**بينّة P1-398** (`variant-worklist-run.txt` و`accepted-answer-worklist.md`، مقيسة بهذا الدور):
**197** بندًا إنتاجيًا بإجابة وحيدة مقبولة، و**0 قابلة للإضافة بثقة** تحت قاعدة ADR-078 (البديل يُشتَقّ فقط من تقديم
جرّة خلفية مع بقاء الفعل المُصرَّف في موضعه). التوزيع بالنوع: **159 fill-blank · 36 word-ordering · 2 error-correction**.
التبريرات: 147 «جملة قصيرة جدًا»، 32 «نص فرعي أو مجموعة المصدر»، 14 «لا جرّة خلفية قابلة للتقديم»، 4 «لا يحفظ رصيد
الكلمات». الحساب: سقف 25% من 387 يعني ≤96 ⇒ يلزم رفع **101** بندًا؛ ولو فُرض رفع البنود الـ38 كلها (الترتيب والتصحيح)
لبقي **159 من 387 = 41.1%**. الاستنتاج موثَّق لا رأي: السقف لا يُبلَغ بتأليف بدائل لأن الفراغ ذا الكلمة الواحدة الصحيحة
لا يحتمل ثانيًا صحيحًا؛ المطلوب قرار تعريف/تصميم من المالك (الخيارات الثلاثة في `## 4u)` من وثيقة النتائج)، ولا تُرخى
العتبة ولا يُوَسَّع `normalizeGermanText`. **اقتراح حارس مرافق (لم يُنفَّذ)**: اختبار وحدة يمنع بدلًا يختلف عن
المفتاح بالحالة والترقيم فقط، وإلا صار إغلاق البند قابلًا للتزوير بسطر.

**المسافة إلى الإغلاق (مقيسة بإعادة إنتاج نفس مجتمع البوابة، `distance-to-close.ts`)**: البنود المُصحَّحة 1,250 · الوسيط 25 · تحت 60 حرفًا **1,099** · تحت 40 **1,031** · ولكي يبلغ الوسيط 60 يجب ألّا يبقى أكثر من 625 تحت الستين، أي رفع **474 بندًا** = **20 دفعة** بـ24. والتوزيع: A1 ‏311 وA2 ‏281 وB1 ‏264 وB2 ‏243 — فالعمل ليس في تمارين A1/A2 وحدها؛ جُمَل القراءة والاستماع و`miniTest` في B1 وB2 هي الأكثر بقيّة رغم اكتمال تمارينها.

**البوابات**: `bash /home/user/tmp/writes.sh` (13 عنوانًا) ⇒ بصمة محتوى **dac99157d91c2b7fdbe13e0453c3bd755d1c1e44151ea70ca58a5d23afb5bfc4** ·
tsc منفرد صفر · lint صفر · **vitest 155 ملفًا / 1,019 اختبارًا** بلا اختبار جديد (`vitest.txt`) · `handoff:check` صفر
مع «doc sync: 13 standing documents re-synced against dwnb-full-pack-v151» (`handoff-check.txt`) · `lesson:quality` صفر ·
**`lesson:quality:audit` خروج 1** بملاحظتين: «productive exercises with one accepted string: 50.9% (max 25%)» و
«median explanation length 25 chars (min 60)» (`lesson-quality-audit-strict.txt`) — فلا بوابة خضراء ولا انتقال أي بند
إلى implemented.

**البناء والبيئة** (`build.txt`، `build-stopped-by-agent.txt`، `tsc-warm-after-next-types.txt`، `environment.txt`):
المبادلة ما تزال مرفوضة (`/usr/sbin/swapon` ⇒ `Operation not permitted`؛ Swap صفر من 1,984 ميغابايت). المحاولة الأولى
بلغت `Compiled successfully in 27.9s` ثم تعثّر «Running TypeScript» **6:43** دقيقة بذاكرة متاحة **17 ميغابايت**
(RSS ‏819 ميغابايت) فأُوقف برقم العملية؛ التدفئة بعد توليد `.next/types` نجحت في **24.5 ثانية** (خروج 0،
`.tsbuildinfo` = 246,523 بايتًا)؛ ثم `NODE_OPTIONS=--max-old-space-size=900 npm run build` ⇒ **خروج 0 في 71 ثانية**:
**321 صفحة** (SSG ‏9.8 ثانية)، بصمة `offline:size` **8d3241e662dd** مع a1 2,399,789 · a2 2,484,974 · b1 2,543,151 ·
b2 3,897,008 · **full 5,655,234**، `js:budget` ‏110/1,727,633/251,309، `media:budget` ‏544 ملفًا و52,943,843 بايتًا
والمنهاج **946,991** gzip. لم يُمَسَّ `next.config` ولا فُحص الأنواع معطَّلًا. كما صُدِّق في هذا الدور أن استعادة
الشجرة جزئية الحدوث: `npm ci` (‏471 حزمة) أعادت `node_modules` بعد أن فشلت أوّل أمرين بالتقاط `tsx`.

**الوثائق**: المسح v150 ⇒ v151 على **16** ملفًا (وثيقة الاحتياط دخلت المسح ثم سُمِّيت)، مع تخطي مواضع التاريخ:
`## 40)` في البرمت، و`دفعة شروحات A2 الثالثة` في `PROJECT_STATUS.md`، و`## دفعة v150 —` في `DECISIONS.md`، و`## 4t)`
في وثيقة النتائج، وسطر 397 في `P1_AUDIT.md`؛ وفصول جديدة: `## 41)`، و`## 4u)`، وADR v151 في آخر `DECISIONS.md`، وسطر
v151 في `PROJECT_STATUS.md`؛ وأُعيد تثبيت §4 و§5 و§6 في `docs/AGENT-HANDOFF-PROMPT-AR.md` وسطري 398 و399 في
`P1_AUDIT.md`؛ ونُسخت وثيقة الاحتياط إلى `docs/BACKUP-RESUME-PROMPT-2026-09-20-v151-AR.md` بحذف نسخة v150؛ ورُفع
الكاش إلى `dwnb-full-pack-v151` في سبعة ملفات؛ وأُعيد توليد فهرس `docs/run-logs/README.md` بعد إضافة هذا المجلد.

**ما لا يُدَّعى**: لا متصفح لهذا الجيل (`README.txt` يحمل `PLAYWRIGHT_EXIT=NOT_RUN`؛ آخر قياس v143 ‏40 من 42)؛
ولا **82/82**؛ ولا إغلاق P1-397 ولا P1-398 ولا P1-399؛ ولا مراجعة بشرية (0 من 3,277)؛ ولا نشر (لا `.git`)؛ ولا
حجم الحزمة ولا بصمتها مذكوران هنا — البلوغ لهما في `wegberlin-full.zip.sha256`.
