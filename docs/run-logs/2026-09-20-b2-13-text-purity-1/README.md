# دفعة 2026-09-20 (2) — نقاء النصوص المؤلَّفة، تصحيح `b2-13`، ورفع الكاش إلى `v139`

## لماذا هذه الدفعة

نشأت من دفعة مزامنة الوثائق (`../2026-09-20-doc-sync-1/README.md`). أثناء مسح `src/**` بالحروف الشرق آسيوية
للتأكد من نظافة الملفات التي عُدِّلت، ظهر عيب في بيانات الدرس نفسها لم يكن يراه أي حارس سابق.

## ما وُجد وكيف حُدِّد موضعه

- المسح بنطاقات `\u3040-\u30ff` و`\u4e00-\u9fff` و`\uac00-\ud7af` على كل الملفات النصية في المستودع (عدا `node_modules`) أعطى موضعًا واحدًا في
  البيانات: `src/data/lessons-b2-module6.ts:460`، داخل `explanationAr` لنص عربي يبدأ «جملة الهدف تحمي ما لا يُـ» ثم حرفان شرق آسيويان محل «تفاوض».
- الموضع البصري خادع: السطر 460 يقع بعد معرّفات `b2-12-m1…m5` في الملف الخام، لكن السجل المجمَّع
  (`academicLessonList`) يعرّفه على أنه **`b2-13.exercises[4]`**. القراءة من السجل هي المرجع، لا من ترتيب الملف.
- النطاق الحقيقي للحارس القديم: `tests/unit/mcq-option-parallelism.test.ts` كان يمسح **الخيارات** وحدها، فلا يرى
  الشروح والوصايا وبقية النصوص — لذلك مرّ العيب.
- الحجم الكامل للجرد: 96 درسًا، 39,540 نصًا مؤلَّفًا، ونص واحد ملوث (0.0025%).

## ما نُفِّذ

1. التصحيح: القطعة الملوثة (حرفان شرق آسيويان بعد «يُـ») صارت «يُتفاوض عليه»، وإزالة مسافة زائدة بعد الواو في «و معيار الفشل» → «ومعيار الفشل».
   لا مساس بأي `options` ولا بأي `correctIndex` ولا بأي مفتاح إجابة.
2. حارس معمَّم جديد: `tests/unit/lesson-text-purity.test.ts` يمشي على كل عقدة نصية في كل درس ويرفض العبرية
   `[\u0590-\u05ff]` والشرق آسيوية `[\u3000-\u30ff\u3400-\u4dbf\u4e00-\u9fff]` والكيريلية `[\u0400-\u04ff]`،
   ويسجل أيضًا أن عدد الدروس 96 وعدد النصوص فوق 39,000 حتى لا يصمت الحارس بتقلّص المدخلات.
3. رفع جيل الكاش لأن نص الدرس داخل الحزمة: `dwnb-full-pack-v138` → `dwnb-full-pack-v139`، مع `staging` و`previous`
   إلى `v138`، في `public/sw.js` و`scripts/verify-continuation-handoff.mjs` و`tests/e2e/critical-flows.spec.ts` (3 مواضع)
   و`tests/unit/offline-pack-controls.test.ts` و`tests/unit/offline-recovery-partial-export.test.ts` و
   `tests/unit/today-offline-readiness.test.ts` و`tests/unit/offline-curriculum-rollback.test.ts`.
4. أول امتحان حقيقي لحارس ADR-079: نُقل سطر `Sync batch` في الوثائق الثلاث عشرة إلى `v139`، ومرّ `handoff:check`.
5. برومبت الاحتياط أُعيد توليده باسمه الجديد: `docs/BACKUP-RESUME-PROMPT-2026-09-20-v139-AR.md`، وحُدِّث كل مرجع له.

## الأثر على المقاييس

- `lesson:quality`: القرينة **52.08%** (651 من 1,250 عنصرًا) لم تتحرك، لأن التصحيح في شرح لا في خيار.
- الجرد المعجمي: 1,297 مرساة اسم، 4,717 إشارة (1,377 مغطى / 89 في قائمة البشر / 3,251 سياقيًا / 1,466 هدفًا مؤلَّفًا)،
  أطر الأفعال 1,200 (134 / 4 / 1,062) — كلها بلا تغيير، لأن الكلمة المحذوفة عربية.
- `content:audit --check` مرّ بعد إعادة توليد التقارير؛ بصمة المحتوى `4c4fc71b383f…` لم تتحرك لأن التقارير نفسها لم تتغير.
- البوابات: `tsc --noEmit` صفر · `npx vitest run` **154/154 ملفًا** و**1,002/1,002 اختبارًا** في 162.52 ثانية ·
  `npm run --silent lint` صفر · `npm run build` خروج 0 و**321/321** صفحة · `offline:size` بصمة **f85aea1e3118**
  (a1 2,389,039 · a2 2,465,399 · b1 2,528,529 · b2 3,877,433 · **full 5,622,695**) · `js:budget` 110 مقاطع
  و1,717,045 gzip وأكبر 246,712 · `media:budget` 544 ملفًا و52,943,843 بايتًا والمنهاج 937,466 ·
  `handoff:check` خروج 0 مع `- doc sync: 13 standing documents re-synced against dwnb-full-pack-v139 (ADR-079)`.
- فرق الحزمة مقابل `v138` −668 بايتًا في `full` (5,623,361 → 5,622,695) سببه انكماش السطر المصحح وتبدد المسافة الزائدة.

## ما لم يُشغَّل (تأجيل معلن، لا إغلاق)

اختبار المتصفح الكامل لم يُنفَّذ على `v139`: تغيّر نصّ واحد وسطر كاش، ولا مسار واجهة ولا منطق تصحيح تغيّر، وإعادة
تجهيز chromium ومكتباته في هذا الدور تتجاوز زمن الدفعة. آخر تشغيل كامل موثق يبقى **82/82** بتاريخ 2026-09-19 على
`v138` (`../2026-09-19-cue-parallelism-1/e2e-v138-full-run2.log`)، ولا يُنسب شيء منه إلى `v139`. أول تشغيل متصفح
قادم يجب أن يكون في نهاية دفعة المرحلة الثالثة التالية (`b2-12` ثم `b1-05` ثم `b2-07` ثم `b2-08`).

## ملفات هذه الدفعة

`build-v139.log` · `lesson-quality-v139.log` · `vitest-v139.log` · `handoff-v139.log` · `handoff-final.log` · `lint.log`.
