# دفعة v140 — موازة الخيارات الثانية (مسار القرينة 3B)

**التاريخ**: 2026-09-20 · **الجيل قبل**: v139 · **بعده**: v140 · **الفرع**: `main` (بلا دفع؛ الصندوق ليس مستودع git)

## ما أُنتج

49 عنصر اختيار من متعدد أُعيد تأليف تشتيته الألماني: `b2-12` (13 من 13)، `b1-05` (12 من 13)،
`b2-07` (12 من 13)، `b2-08` (12 من 13)، ثم عنصران إضافيان من الاختبار المصغّر فرضهما حارس التوازي
(`b2-07-m2` بتباعد 32 ⇒ 16، و`b2-08-m2` بكلمة عربية ملتصقة بخيار ألماني ⇒ تشتيت ألماني).
لم تُمسّ نصوص المفاتيح ولا الإجابات ولا مواضعها. السجل التأليفي (المفاتيح الحرفية والتشتيتات والأطوال)
في `/home/user/scratch/b3b/` خارج المستودع: `b2-12-authored.json` … `b2-08-authored.json` و`batch-*.json`.

## القياسات بعد الدفعة

| المقياس | القيمة | المصدر |
|---|---|---|
| قرينة «الخيار الأطول = المفتاح» | 602 من 1,250 = **48.16%** (كانت 651 = 52.08%) | `reports/lesson-quality-audit.json` |
| شرح تحت 60 حرفًا | 1,322 من 1,733 (لم يتغيّر) | `feedback.allUnder60Chars` |
| وسيط الشرح | 24 حرفًا (البوابة 60 ⇒ `--strict` أحمر عمدًا) | `feedback.explanationMedianChars` |
| إنتاج بقبول وحيد | 197 من 387 = 50.9% | `productive` |
| فجوات المعجم — الأسماء | 4,802 = 1,380 covered / 89 pending-human / 3,333 context-only | `reports/academic-content-audit.json` |
| فجوات المعجم — أطر الأفعال | 1,233 = 134 covered / 4 unclassified / 1,095 not-target | المصدر نفسه |
| بصمة محتوى Zod | `e9c471b4046a` | `contentSha256` |
| الكاش | `dwnb-full-pack-v140` (staging/previous v139) | `public/sw.js` |
| `offline:size` | 3bd784b8622f · full 5,630,874 | سجل البناء |

## السجلات

- `lesson-quality.log` و`*-audit-write.log` (تسعة): إعادة توليد التقارير بعد تعديل النصوص، كلها exit 0.
- `05-vite-build.log`: `npm run build` الكامل (prebuild بثماني عشرة بوابة + `next build --webpack`) — EXIT=0،
  321/321 صفحة، تجميع 11.6 ثانية، توليد صفحات 10.5 ثانية، `js:budget` 110/1,720,034/249,350،
  `media:budget` 544 ملفًا / 52,943,843 بايتًا / مصادر 939,473 gzip.
- `06-vitest-unit.log`: 154/154 ملفًا و**1,006/1,006** اختبارًا، EXIT=0.
- `07-lint.log`: `npm run lint` exit 0. `08-tsc.log`: `tsc --noEmit` صفر.
- `09-a11y.log`: **خطأ في يدي لا في التطبيق** — لا وجود لسكربت باسم `a11y:audit`؛ فحص الوصول يجري داخل
  حزمة المتصفح عبر `tests/e2e/accessibility-a11y.spec.ts`. أُبقي السجل للدلالة على المحاولة الفاشلة.
- `10-handoff-check.log`: `npm run handoff:check` exit 0 — بما فيه حارس مصادقة الوثائق (ADR-079) وتثبيت
  الأرقام المعجمية وعدد الاختبارات واسم الكاش وبصمة `full`.
- `11-lesson-quality-strict.log`: البوابة الحمراء المقصودة (وسيط الشرح 24 تحت 60، وقبول وحيد 50.9% فوق 25%).
- `12-e2e-desktop.FAILED-attempt.log`: محاولة سقطت كل بنودها لأن `libcups.so.2` كانت ناقصة لـ chromium الكامل؛ ثُبّتت.
- `14/15-e2e-desktop|mobile.port-conflict.log`: محاولة رفضها playwright لأن خادم `next start` يتيمًا من المحاولة الأولى كان يحتل 3100.
- `16-e2e-desktop.log`: المشروع الكامل على v140 — **40 من 41** في 13.3 دقيقة. البند الفاشل انتظار تثبيت الحزمة الكاملة (سقفه 360 ثانية) ولم يظهر تحت حِمل 3.2 وذاكرة شبه نفدت.
- `17-e2e-mobile.log`: أُوقف مشروع mobile عمدًا في البند 4 من 41 لتفريغ الموارد؛ لا نتيجة له وليس فشل اختبار.
- `18-e2e-pack-isolated.log`: البند نفسه، معزولًا على صندوق فارغ — **أخضر في 35.5 ثانية** (`rc=0`). النتيجة المقاسة إذن: لا انحدار في التطبيق، والفشل اختناق مورد؛ لم يُخفَّ السقف ولم يُعدَّل الاختبار.

## ما لم يُدّعَ

- المراجعة البشرية ظلت **0 من 3,277**؛ حزمة المراجعة (17 ورقة و268 بندًا معلَّمًا) أداة، لا تنفيذ مراجعة.
- أعلام التقييم الصوتي/Fonem assessment مطفأة؛ لا قياس مسموع في هذه الدفعة.
- حداثة صيغة telc: بنداه المفتوحان كما هما (`evidenceCollectedAt: 2026-09-15`، `claimsFormatUnchanged: false`).
- القرينة لا تزال فوق سقف 40%: الباقي 102 بندًا ≈ 9 دروس، أولها `b2-11` و`b1-20` و`b1-07`.
