# سجل الدفعة — `b1-07` و`b1-12`: كسر قرينة الأطول (v144) — 2026-09-20

## القصد والنتيجة المقيسة

المرحلة 3C (نـزول قرينة «الخيار الأطول هو الصحيح» إلى ≤40% على كل الدروس). عند بدء الدفعة كانت القرينة
577 من 1,250 = 46.16% (قياس v143). حُوِّل في هذه الدفعة الدرسان التاليان بالانحراف:

- `b1-07`: 11 عنصرًا من 13 بقرينة (وسيط الفارق 14 حرفًا) ⇒ صفر
- `b1-12`: 11 عنصرًا من 13 بقرينة (وسيط الفارق 6 أحرف) ⇒ صفر

القياس بعد التحويل: **555 من 1,250 = 44.4%**. البوابة `lesson:quality:audit` ما تزال exit 1 بثلاث ملاحظات،
فلم يُغلق البند P1-397 ولا يُدّعى الوصول إلى السقف.

## الأدوات، بلا تحرير يدوي لبيانات الدروس

1. `npx tsx scripts/dump-cue-items.ts b1-07 > b1-07-items.jsonl` (ومثله `b1-12`)
2. `python3 scripts/cue-batch.py <items.jsonl> <lesson-authored.json> <lesson> <batch.json>`
3. `python3 scripts/parallelise-options.py --batch <batch.json> --write`

الملفات `*-authored.json` و`*-batch.json` في هذا المجلد هي المدخلات الحرفية، فالدفعة قابلة للتكرار.
مجموع المشتتات المؤلَّفة: **69** (23 مجموعة × 3)، والمفاتيح ومواضع الإجابة لم تُمَسّ.

## ما كشفه الحارس ولم تكن البوابة تراه

`b1-12-m5` ليس عنصر قرينة (المفتاح «Erfolge und Probleme» طوله 20 وكان أطول مشتت 21)، لكنه كان **مجموعة
مختلطة اللغة**: ثلاثة مشتتات عربية مع مفتاح ألماني. ظهر الفشل عند إضافة `b1-12` إلى `PARALLELISED` في
`tests/unit/mcq-option-parallelism.test.ts` لأن اختبار الحارس يجتاز كل عناصر الدرس لا عناصر القرينة فقط.
أُصلح بالأداة نفسها (انظر `b1-12-extra-items.jsonl` و`b1-12-extra-authored.json`): أطوال 28/26/17 حول مفتاح
طوله 20، توسيع 11، ألمانية نقية. لم تُخفَّف القاعدة لتنجح الدفعة.

## البوابات من هذه الشجرة

- `tsc.log` exit 0 · `lint.log` exit 0 (صفر مشاكل) · `vitest-unit.log` **155/155 ملفًا و1,015/1,015 اختبارًا** exit 0
- `build.log` `BUILD_EXIT=0`: 321 صفحة · بصمة `offline:size` **5920fa8f1432** · a1 2,393,854 · a2 2,470,196 ·
  b1 2,536,008 · b2 3,887,581 · `full 5,635,555` · `js:budget` 110 / 1,721,819 / 249,865 ·
  `media:budget` 544 ملفًا / 52,943,843 بايتًا / المنهاج 940,949 gzip
- `handoff.log` exit 0 بعد تثبيت الوثائق على v144 · `lesson-quality-strict.log` exit 1 عن قصد (السقف 40% متجاوز)
- `cue-probe.log`: الإحصاء الكامل — ex 83 من 194 (42.8%) · rd 172 من 288 (59.7%) · ls 140 من 288 (48.6%) ·
  mt 160 من 480 (33.3%)؛ A1 82 (26.3%) · A2 147 (47.1%) · B1 162 (51.9%) · B2 164 (52.2%). المواضع بلا تغيير
  A 324 · B 298 · C 305 · D 323 و3 دروس بلا D.

## عدادات أُعيد تثبيتها

الأسماء 4,823 ⇒ **4,833** مرشحًا · مغطى 1,381 بلا تغيير · سياقًا 3,353 ⇒ **3,363** · مؤلَّف 1,470 بلا تغيير.
أطر الأفعال 1,240 ⇒ **1,242** · سياقًا 1,102 ⇒ **1,104** · مغطى 134 بلا تغيير.
**pending-human: 89 و4 — لم تتحرك**، والمراسي 1,297 و134 كما هي. التدقيق اللغوي 179 TSX / 6991 / 399 / 45 / 233
وصفر مشاكل. بصمة المحتوى `cfbffc605825…` (تغيّرت مرتين داخل الدفعة: `b27320694fde` قبل إصلاح `m5` ثم
`cfbffc605825` بعده، والثانية هي المثبتة). الكاش `dwnb-full-pack-v144` (staging/previous v143).

## عطلان في البيئة سجّلا بدل أن يُخفيا

1. نسخة المستودع عند بدء الجلسة كانت مرجَّعة عن دفعتي v142 وv143 في ملفات الاختبار والمكوّنات (`deferred` ممتلئ،
   `PARALLELISED` سبعة، pins معجمية قديمة، بلا `useDeviceValue`)، بينما بقيت `public/sw.js` والتقارير عند v143.
   استُعيدت الشجرة كاملة من أرشيف v143 المسلَّم (`/home/user/wegberlin-full.zip`، بصمة
   `7a08b332297b…`) ثم verify: `grep` وجد مشتتات `b2-11` في `src/data/lessons-b2-module6.ts` والحارس على 9 دروس.
2. أول محاولة بناء خرجت exit 1 لأن `prebuild` رفض `docs/generated/ACADEMIC_SCHEMA_REPORT.md` و
   `ANSWER_INTEGRITY_REPORT.md` عتيقَين بعد إصلاح `m5`. أُعيد توليد كل `:write` ثم بُني بنجاح — ولم يُتجاوز
   الحارس بأي راية.

البناء يحتاج مقايضة على هذا الصندوق: `/home/user/.swapfile` بحجم 1500 MB مع
`NODE_OPTIONS=--max-old-space-size=1200` (بلا مقايضة يُقتل بإشارة 137)، و`TMPDIR=/home/user/tmp` متّبع.

## المتصفح

لم يُعَد تشغيل المتصفح على هذا البناء؛ آخر قياس مسجّل يبقى ما ثُبّت عند v143 (40 من 42 في مشروع chromium،
والفشلان مرّا منفردَين، وبند الترطيب نجح في 12.5 ثانية)، ومشروع `mobile-chromium` لم يُشغَّل إطلاقًا.
لذلك لا رقم متصفح باسم v144، ولا يُقال إن إصلاحات v142 مستقرة بصريًا إلى الأبد.

## التالي

55 عنصرًا تنـزل بالقرينة إلى ≤40% (555 − 500)، وتغطيها خمسة دروس بالضبط، 11 عنصرًا لكلٍّ:
`b1-16` · `b1-22` · `b2-05` · `b2-13` · `b2-17`. بعدها تأتي المرحلة 4 (الشروحات عند مستوى التمرين: A2 ‏120 ثم
A1 ‏167) وبعدها خريطة المالك.
