# سجلّ الدفعة التاسعة — شروحات A2 (وسمُ الجيل: **v155.1**، محتوى فقط) — 2026-09-21

> كل رقم في هذا الملف **مقاس بعد الكتابة** على هذه الشجرة، لا منسوخ من دفتر. حيث تعارض هذا الملف مع المستودع،
> فالقياس هو المرجع، ويُعاد القياس قبل أي تثبيت.

## 1) ما نُفِّذ

- **24 بندًا** من جماعة الوسيط في A2 (17 من `miniTest` و7 من أسئلة القراءة/الإصغاء) كانت شروحها **14–15 حرفًا**
  (شروحٌ صورية: عنوانٌ مقطوع أو كلمة) رُفعت إلى **165–204 حرفًا**: اقتباسٌ من النصّ الألماني + القاعدة + لماذا يسقط
  كلُّ مشتّت + `فحص ذاتي: …؟`.
- التوليف والفحص بـ`a2e9.py` (في هذا المجلد) — نسخةٌ من `a2e8.py` بالمدى **156–212**، ومنع الألفاظ الموضعية
  (حتى كشظايا داخل كلمات)، ومنع CJK، ومنع التكرار داخل الدفعة، و**رفض البند الذي شرحه ≥ 60**، وقرأتُ `oldLen`
  من المستودع نفسه (لا من يدي). الجولة الأولى: **22** نصًّا خارج المدى؛ الثانية: مخالفةٌ واحدة (`a2-05-m3` عند 152،
  وهو تحت الحدّ الأدنى) مُدِّدت ⇒ `validator: 0 violations · 24 items`.
- التطبيق بـ`python3 /home/user/scratch/set_expl.py a2 /home/user/tmp/a2e9.json` ⇒ **`applied: 24 of 24 missing: []`**
  (سِجلّ كل بند `14->1xx` / `15->1xx`).
- **تحقّقٌ من الشجرة نفسها لا من التقارير**: قُرِئت `src/data/lessons-a2-module*.ts` بعد الكتابة ومُورِنَ كلُّ معرّف
  على حمولة الدفعة ⇒ **24/24** طولُ النصّ الحيّ يطابق الطول المكتوب، والمجموع **345 ⇒ 4,487 حرفًا**.

| المعرّف | القديم | الجديد | | المعرّف | القديم | الجديد |
|---|---|---|---|---|---|---|
| `a2-01-rq1` | 15 | 189 | | `a2-09-lq3` | 14 | 203 |
| `a2-01-m2` | 15 | 199 | | `a2-09-m2` | 14 | 202 |
| `a2-01-m3` | 14 | 193 | | `a2-10-m4` | 15 | 176 |
| `a2-03-lq3` | 14 | 194 | | `a2-10-m5` | 14 | 183 |
| `a2-05-m1` | 14 | 172 | | `a2-12-m1` | 14 | 190 |
| `a2-05-m3` | 15 | 195 | | `a2-12-m4` | 14 | 191 |
| `a2-05-m4` | 14 | 168 | | `a2-13-m4` | 14 | 173 |
| `a2-05-m5` | 15 | 194 | | `a2-14-m3` | 15 | 185 |
| `a2-07-rq1` | 14 | 165 | | `a2-14-m5` | 15 | 176 |
| `a2-07-m1` | 14 | 185 | | `a2-17-rq1` | 15 | 202 |
| `a2-08-lq3` | 14 | 178 | | `a2-17-lq3` | 15 | 204 |
| — | — | — | | `a2-22-m2` | 14 | 175 |
| — | — | — | | `a2-24-m4` | 14 | 195 |

## 2) القياس بعد الكتابة

`lesson:quality` + مسبار جماعة الوسيط (`tmp/pick_median.ts`) + مسبار التوزيع (`tmp/suffix9.ts`):

- **جماعة الوسيط (1,250 بندًا: تمارين + أسئلة قراءة/إصغاء + `miniTest`):** تحت 60 **1,039 ⇒ 1,015** والسقف 625 ⇒
  **المسافة إلى وسيط 60 = 414 ⇒ 390 = 17 دفعة × 24** (توقّع المسبار للدفعة القادمة: 366). وسيطُ الجماعة بقي **26**
  كما هو متوقّع: الدفعة تأخذ من ذيل التوزيع فلا تحرّك الوسيط حتى تُستنفد الطبقة.
- **القائمة الأوسع (`feedback`):** `allUnder60Chars` **1,178 ⇒ 1,154** من 1,733 · `under40Chars` **971 ⇒ 947**
  (من 1,250) · `allMedianChars` **31 ⇒ 32** · `allMissing` **0** · `explanationMedianChars` **26**.
- **التوزيع المقاس بالنمط** (مصدرٌ واحد، كلُّ الأرقام من `suffix9.ts` على الكائنات الحيّة):
  A1 ‏431 قصيرًا من 432 = تمارين **167**/168 · `miniTest` **120**/120 · `rq` **72**/72 · `lq` **72**/72 —
  A2 ‏216 من 432 = تمارين **24**/168 · `miniTest` **76**/120 · `rq` **64**/72 · `lq` **52**/72 —
  B1 ‏264/432 · B2 ‏243/437. والمجموع **431 + 216 + 264 + 243 = 1,154** ✓ يوافق `allUnder60Chars`.
  **تصحيحٌ لرقمٍ كان سيُكتب خطأً:** شروحُ تمارين A2 (`_e`) **بقيت 24** ولم تنقص إلى 9 — لأن هذه الدفعة لم تأخذ
  بندَ تمارين واحدًا (17 × `miniTest` + 7 × أسئلة). وعلى السكان المقيَّمين بوسيط الجماعة: A1 ‏311 · **A2 ‏197** ·
  B1 ‏264 · B2 ‏243 (المجموع 1,015 ✓).
- **لم يتحرّك عمدًا** (الدفعة نصٌّ لا خيارات ولا مفردات): القرينة **48.16%**، مزيجُ المفتاح 25.92/23.84/24.4/25.84،
  مفردات الكتابة خارج المدى **0**، البنود الإنتاجية أحادية السلسلة **50.9%**، وسيطُ الاستماع **93 كلمة (~32 ثانية)**.

## 3) البصمات (بعد تحديث التقارير بـ`:write` في هذا الجيل)

- تحرّكت اثنتان، كما هو متوقّع من دفعة شرحٍ صرفة: `content:audit` ‏`eca2409c0489…` ⇒
  **`27d4c28626e130c1405fbe99017acd936fc10fad6194423560ce0b466d917f54`** · `learning-architecture` ‏`e765f8e14d80` ⇒
  **`9b57e06047aa`**.
- **بلا حركة (قِيست بعد الكتابة لا بالذاكرة):** `case:audit` ‏`167645dd621b…` بـ**0 فجوات** وبنفس العدّادات
  **19 عقود / 23 نظرية / 57 مضبوطًا / 44 تقييمًا** · `similarity` ‏`efdb019376f3…` · `language` ‏`9da1a2e19f1e…` ·
  `lexical-strategy` ‏`439956b453f0` · `study-modes` ‏`1645b5caa1b0` · `practical-context` ‏`c935b0f4ef8b` ·
  `exam:format-claims` ‏`d729860a7334` · `governance` ‏**3,277 سجلًا / 16 عائلة / 18 مصدرًا / 12 خطرًا / 12 ادّعاءً /
  18 سطر نطق** والمراجعة البشرية **0 من 3,277**.

## 4) الدَّيمومة: صارت الأداة في المستودع

- **`scripts/apply-explanation-batch.py`**: مطبِّق الدفعات العام (`--level --payload [--dry-run] [--data-root]`)،
  يقفل على `oldLen` تموضعًا ومحتوىً، يرفض مستوى بلا ملفات `lessons-<level>-module*.ts`، ويعدّ الكتابة الفعلية.
  تحقّقٌ ثلاثيّ: `--dry-run` على دفعةٍ مطبَّقة ⇒ **24 × `SKIP-already-lifted measured=… expected=14`** و
  `would write 0 field(s)` · `--level c9` ⇒ **`batch refused: no lessons-c9-module*.ts`** بخروج 1 · ومسارُ الكتابة
  على نسخةٍ في `tmp/wtest` (`a2-01-e1`، 188 ⇒ 107) ⇒ `wrote 1 field(s)`، والنصّ حاضر في الملف، وإعادةُ التشغيل ⇒
  `wrote 0 field(s)` (لا كتابةَ ثانيةً أبدًا).
- **`docs/run-logs/a2-explanations-payloads/`**: حمولات الدفعات العشر (216 بندًا + إصلاح واحد) — راجع `README.md`
  هناك لأمر إعادة التشغيل بالترتيب.

## 5) بوابات هذا الجيل (مخرج الفاحص)

- `npx tsc --noEmit` ⇒ **خروج 0**.
- `npx vitest run tests/unit` ⇒ **153 ملفًا أخضر / 3 حمراء (156)**، **1,017 اختبارًا ناجحًا / 10 فاشلة (1,027)**.
  والسببان معلَّلان بالأسفل (§6) — لا يُليَّن أيٌّ منهما.
- `npm run case:audit` ⇒ أخضر بالأعداد في §3. `npm run governance:audit` ⇒ أخضر. `exam:format-claims` ⇒ أخضر.
- `npm run lint` ⇒ كان **3 أخطاء** في ملفٍ مؤرشف (`docs/run-logs/2026-09-20-a2-explanations-4/distance-to-close.ts`
  أرجعه التراجعُ بلا تثبيت أنواع — precedent من v152 حيث أُصلِح نفسُ الملف بالطريقة نفسها) ⇒ صُلح بإلغاء `any`
  ثلاثًا لا بتعطيل القاعدة ⇒ **خروج 0**. **وفائدةٌ إضافية**: الملف المؤرشف مسبارٌ مستقلّ، فأعاد القياسَ نفسه
  حرفيًّا (`1250 · median 26 · <60 1015 · <40 947 · lift 390 ⇒ 17 batches`، وA1 311 · A2 197 · B1 264 · B2 243)
  موافقًا لـ`pick_median.ts` وللتقرير ✓.
- **إصلاحٌ صغير أُجري في هذا الجيل:** ثلاثةُ ملفات اختبار كانت تثبّت حرفيةَ الكاش على `staging/previous-v150`
  بينما `public/sw.js` على `v154` (نتيجةُ التراجع لا الدفعة): `tests/unit/offline-curriculum-rollback.test.ts`
  (`previous-v150→v154`) و`tests/unit/offline-recovery-partial-export.test.ts` و`tests/unit/today-offline-readiness.test.ts`
  (`staging-v150→v154`). القياس قبل الإصلاح: 6 ملفات حمراء و13 اختبارًا؛ بعده: 3 و10، والفحوص الثلاثة نفسها
  **13/13 خضراء**.
- **لا كاش ولا حزمة ولا نشر في هذا الجيل** — لا لأن البيانات مفقودة (انظر §6: البناء يعمل)، بل لأن رفعَ وسمٍ إلى
  `v156` مع بياناتٍ أنقصَ من المنشور يعني publishًا راجعًا. حارسُ البنية `tests/unit/delivery-layout.test.ts` أخضر
  (4 فحوص) والأرشيف لم يُعَد توليده.

## 6) كشفٌ مهمّ: الشجرة المستعادة أقدمُ من الاختبارات، والعجزُ أوسع من حمولات الشروح

الأدلة المقاسة هذا الجيل:

- التدقيق يقيس للأسماء **4,802 مرشحًا / 1,380 مغطى / 89 pending-human / 3,333 سياقًا** ولأطر الأفعال
  **1,233 / 134 / 4 / 1,095**، بينما يثبّت `tests/unit/lexical-target-gap.test.ts` و
  `tests/unit/academic-content-governance.test.ts` و`scripts/verify-continuation-handoff.mjs` الأرقام
  **4,874 / 1,381 / 3,404** و**1,257 / 1,119** ⇒ لذلك يفشل `handoff:check` عند «noun target-gap counters drifted»
  وتيفشل اختباران اثنان. و`explanationAr` **لا يُقرأ إطلاقًا** في `src/core/content-validation/lexical-target-gap.ts`
  (`grep -c explanationAr` = **0**)، وبقي pending-human عند 89 و4 بلا حركة ⇒ **الفشل ليس من دفعات الشروح**، بل من
  جيل البيانات.
- **8 فحوص موازاة-المشتّتات** حمراء: `b1-07:e1 (50/52/46/28)` · `b1-12:e1` · `b1-20:e1` · `b2-05:e1 (50/56/26/17)` ·
  `b2-11:e1` · `b2-13:e1` · `b2-17:e7` · `b2-19:e7` — أي دروسُ دفعاتِ خفضِ القرينة المسجَّلة في `DECISIONS.md`
  (العدادات 4,833 → 4,845 → 4,857 → 4,864 → 4,874 والقرينة 44.4% → 42.8% → 41.92% → 41.04%)، بينما تقيس هذه
  الشجرة **48.16%**. ⇒ **ما ضاع بالتراجع ليس 145 شرحًا فقط، بل سلسلةُ دفعات القرينة/الموازاة كلها.**
- **التعافي مُخطَّطٌ وموجود في المستودع:** `docs/run-logs/*cue-parallelism*/*-batch.json` — **28 ملف JSON** (منها
  12 ملف `*-batch.json`) تغطي `b1-07` `b1-12` (+extra) `b1-16` `b1-22` `b1-20` `b2-05` `b2-11` `b2-13` `b2-17`
  (+m2) `b2-19`، وكلُّ بندٍ فيها قائمةُ `options` مُعادَ توليفها. الترتيب الموصى به للجيل القادم: استعادةُ دفعات
  القرينة ⇒ `npm run content:audit:write` + `learning:architecture:audit:write` ⇒ إعادةُ تثبيت عدادات 4,874 في
  الوثائق ⇒ `lesson:quality` ⇒ **بعدها فقط** وسمُ `v156` والكاش و`archive:delivery`.
- ما ليس عجزًا: `vendor:materialize` و`runtime:materialize` و`offline:manifest` **تنجح كلها** هنا وتولّد
  **318 مسارًا و5 حُزَم (a1:58 · a2:58 · b1:58 · b2:219 · full:318)** مطابقةً لما وثّقته v155 — فالظنّ بأن
  `src/data/{exercises,curriculum,micro-drills}` JSON مفقودةٌ ويُتعذَّر البناء **ظنٌّ باطل**: التمارين أساليبٌ داخل
  ملفات `lessons-*.ts` (79 ملفًا) ولا شجرة JSON ثانية.

## 7) ما لم يُغلق / غير متحقَّق

- لا Vercel مُتحقَّق منه؛ لا متصفح على هذا البناء (آخر أخضر v143: 40 من 42، والفاشلان مرّا منفردَين)؛ المراجعة
  البشرية **0 من 3,277**؛ P1-397 لم يُغلق (البوابة fail عند 48.16%)؛ P1-398 قرارُ مالك؛ `lesson:quality:audit`
  يخرج 1 بالقياس (وسيط 26 مقابل حدٍّ 60) وهذا هو الغرض من الطابور، لا عطلٌ يُصلَح بتليين الحدّ.
- طابور الشروح بعد هذه الدفعة: **390 بندًا = 17 دفعة** (و978 من القصير في أسئلة المراحل خارج قياس التمارين).

## 8) الملفات في هذا المجلد وأمرُ إعادة التشغيل

- `a2e9.py` — مولِّد الحمولات + الفاحص (المدى، الألفاظ الموضعية، CJK، التكرار، رفض ≥60، قراءة `oldLen`).
- `detail9.txt` —.dump البنود الـ24 مع خياراتها ومقاطع النصوص المصدرية (المرجع الذي قُوِّسَت منه العبارات الألمانية).
- `measure9.ts` — مسبار السكان الأوسع (1,733 بندًا: العدد، تحت 60، تحت 40، الوسيط، التوزيع حسب المستوى).
- `suffix9.ts` — مسبار التوزيع حسب نمط البند (`_e` / `m` / `rq` / `lq`) لـ A1 وA2.
- `pick_median.ts` — منتقي الدفعة القادمة من جماعة الوسيط، ويطبع السكان/تحت 60/السقف والمسافة قبل وبعد.
- `medlevel9.ts` — نفس التقسيم على سكان الوسيط (1,250) حسب المستوى، مع عدد الدفعات المتبقية.
- تُشغَّل بمسارها من داخل المستودع: `npx tsx docs/run-logs/2026-09-21-a2-explanations-9/suffix9.ts` (و`measure9.ts`
  يشير إلى مطلق `/home/user/der-weg-nach-berlin/src/data/academic-lessons`؛ لا تُستعمل `node --experimental-strip-types`).

```bash
# إعادة تشغيل الدفعة على شجرةٍ نظيفة (تُطابَق oldLen حرفيًّا، وتُتجاهل البنودُ المرفوعة سلفًا):
python3 scripts/apply-explanation-batch.py --level a2 \
  --payload docs/run-logs/a2-explanations-payloads/a2e9.json --dry-run
python3 scripts/apply-explanation-batch.py --level a2 \
  --payload docs/run-logs/a2-explanations-payloads/a2e9.json
npm run lesson:quality && npm run content:audit:write && npm run learning:architecture:audit:write
```
