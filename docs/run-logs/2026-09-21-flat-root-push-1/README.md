# 2026-09-21 · جولة رابعة: أمر الرفع المسطّح الذي طلبهOwner (مبنيّ ومقاس)

الطلب: «أمر مطابق يفكّ `der-weg-nach-berlin/` ويدفع **محتواه** إلى `main`، لا المجلد نفسه».
المُخرَج: `TERMUX_UNNEST_PUSH.sh` (جذر المستودع) + نفس الكود ككتلة جاهزة للنسخ في
`TERMUX_GITHUB_UPLOAD.md` (323 سطرًا). الاختبارات أُجريت على **نصّ النسخة** نفسه، مع سطر واحد
مستبدل للـ remote المحلي (أُظهر الفرق بـ diff للتأكد أنه السطر الوحيد).

## القياس على remote حقيقي محلي (bare) وأرشيف مركّب بنفس شكل ما عندكم

| الحالة | النتيجة |
|---|---|
| أرشيف ملفوف فيه بيانات | خروج 0 · `سيُدفع: 18 ملفًا · src/app=2 · بيانات=6` · `* [new branch] main -> main` · `ls-remote = 79672bf…` |
| جذر المستودع بعد الدفع | `.githooks .github .gitignore PROFESSIONAL_CONTINUATION_PROMPT_AR.md README.md next.config.ts package-lock.json package.json public src vercel.json` · `der-weg-nach-berlin` = **0** · `node_modules` = **0** · dotfiles = 3 · `pre-commit` منفَّذ · commit واحد |
| أرشيف مسطّح أصلًا | خروج 0، بلا إزالة، نفس العدّادات (`86ec860…`) |
| sidecar مغايِر | `❌ الأرشيف لا يطابق sidecar — لا ترفعه` · خروج 1 · قبل فكّ الضغط |
| أرشيف بلا `src/data/exercises` | `── محتوى الأرشيف: تمارين=0 منهاج=0` ثم `❌ … لا ترفعه إلى main` · خروج 1 |

## خطأان وُجدا بالقياس وأُصلحا قبل التسليم

1. **عدّاد بحساب `tar -t`**: كان `verify` يرفض مرآة سليمة لأن `tar` يعدّ مدخلات المجلدات (949 مقابل 943).
2. **فخ الاقتباس المتداخل في `bash -c`**: `echo "… $(git ls-files | grep -c "^[.]")"` كان يسري داخل
   النص الملصوق فيصدر `unexpected EOF while looking for matching '"'` عند التنفيذ — و`bash -n` على
   **الملف** لا يكشفه لأن المتن داخل `bash -c '…'` مجرد نص حتى لحظة التنفيذ. أُزيل العدّاد من
   الملف والكتلة، وأُرفق التحذير في الوثيقة. بعد الإصلاح: فحص `bash -n -c` على المتن = 0 للنسختين.

## الربط والفحص

`package.json`: `data:recover` (+ ما سبق) · `tests/unit/delivery-layout.test.ts` أضيفت له حالة رابطة
لأمر الرفع (إزالة الملفوف، `dotglob`، رفض بلا بيانات، رفض sidecar مغايِر، التأكيد على `src/app`،
لا `| head`) ⇒ **8/8 ناجح** · `npx tsc --noEmit` = 0 · `npx eslint` نظيف على الملفات الجديدة.
يبقى العائق نفسه: `src/data` في مساحة العمل 79 ملفًا، فلا أرشيف كامل جديد حتى يرجع المحتوى
(الأمر أعلاه سيرفض بشجاعة أي أرشيف فارغ — وهذا مقصود).
