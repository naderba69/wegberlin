# دفعة شروح A2 رقم 13 — 24 بندًا من جماعة الوسيط (2026-09-23، جيل v159)

## ما نُفِّذ

| البند | القيمة |
|---|---|
| الدفعة | 13 (الخامسة من جماعة الوسيط: MCQ + أسئلة القراءة/الاستماع + Mini-Test) |
| الموضع في القائمة | أقصر 24 بندًا من أصل **919** بندًا تحت 60 حرفًا في جماعة **1,250** (بعد الدفعة: من 943) |
| الاختيار | `pick_median.ts 24 a2` ⇒ تحت-60 **943** (السقف 625) · المسافة **318 → 294** |
| الأطوال | قديم **20–23** ⇒ جديد **161–210** (النطاق المحرَّد 156–212) |
| التطبيق | `scripts/apply-explanation-batch.py --level a2 --payload docs/run-logs/a2-explanations-payloads/a2e13.json` ⇒ **24/24**، `0 skipped · 0 not found` |
| القياس بعد الدفعة | وسيطُ الجماعة **28 ⇒ 29** · تحت-60 **943 ⇒ 919** · تحت-40 **875 ⇒ 851** · `allUnder60Chars` **1,082 ⇒ 1,058** · `allMedianChars` **34 ⇒ 35** |
| فحص الاقتباسات | `npx tsx scripts/audit-explanation-quotes.ts --level a2` ⇒ 432 شرحًا · 531 اقتباسًا ألمانيًا · **0 misquote** · 23 مثالًا مُختلَقًا (ملاحظاتٌ لا أخطاء) |
| المرشّح للأمام | الدفعة 14 تُختار من جديد بـ`pick_median.ts 24 a2` على شجرة الدفعة 13 (تحت-60 صار 919 والسقف 625) |

## الملفات

- `a2e13.py` — باني الدفعة: 24 نصًّا + حراسات الدفعة 12 حرفيًّا (النطاق 156–212، منع الصياغة الموضعية، منع CJK، منع التكرار، والنصّ القديم وطوله يُقرآن من المستودع، وبقاءُ إشارةٍ إعرابية قائمة في النصّ القديم مطلوب، ورَفْضُ أيّ بندٍ بلغ 60 حرفًا لأنه الدفعة الخطأ).
- `dump13.ts` — مستخرِج البنود مع سياقها (خيارات، جذع، نصّ القراءة/النصّ المسموع) حتى لا يُكتَب شرحٌ لا يستند إلى ما يراه المتعلّم.
- `pick_median.ts` — مُنتقي أقصر البنود من الجماعة التي تحسبها البوابة نفسها (لا من قائمة `allUnder60Chars` الأوسع).
- الحمولة النهائية: `docs/run-logs/a2-explanations-payloads/a2e13.json` (مصدرُ الاستعادة).

## أربع ملاحظات منهجية

1. **الشرح يُقتبس من المادة المعروضة لا من الذاكرة.** كلُّ «…» في النصوص الجديدة موجودٌ حرفيًّا في خيارات البند أو جذعه أو نصّ درسِه (النصّ المسموع/نصّ القراءة)، وفاحصُ الاقتباسات يقيس ذلك بنسبة تشابه ولا يُجيز الصمت: **0 misquote**.
2. **المؤشّر لا يُجمَّل.** التطبيق لا يلمس إلا `explanationAr`؛ لذلك لم تتحرّك القرينة (`39.28%`) ولا مزيجُ المفتاح (`25.92/23.84/24.4/25.84`) ولا أمانةُ البدائل (`338/387`) ولا `case:audit` (`167645dd621b`) بصفر فجوات.
3. **البوابة تبقى حمراء بشرعية.** `npm run lesson:quality:audit` يخرج **1** بملاحظتين منشورتين: الإنتاجية أحادية السلسلة **87.3%** مقابل سقف 25% (قرار تعريف عند المالك، P1-398)، ووسيط الشرح **29** مقابل 60 (P1-399). لم يُرفع سقف، ولم تُخفَّف عبارة تحقُّق، ولم يُنقل بندٌ إلى `partial`/`implemented`.
4. **الطريق المعلن مقيس لا مُقدَّر.** المسافة صارت **294**؛ وبمعدّل 24 بندًا في الدفعة تبقى **13 دفعة** حتى يبلغ وسيطُ الجماعة 60 بحدّه الأعلى 625 بندًا تحت الستين.

## البناء والتسليم

- `npm run check` خروج **0**: lint 0 · tsc 0 · **1,050 اختبارًا في 160 ملفًا** · secret/source audits · `handoff:check` 0 · بناء **321/321 صفحة** · `offline:size` **`0d1b57494337`** (a1 2,424,100 · a2 2,546,714 · b1 2,567,442 · b2 3,921,106 · full 5,715,376) · `js:budget` 110/1,751,723/250,914 · `media:budget` 544/52,943,843 والمنهاج 970,991 gzip.
- الكاش `dwnb-full-pack-v159` (staging/previous v158) في سبعة ملفات، و13 وثيقة على `Sync batch: v159`، و`public/offline-size-manifest.json` أُعيد توليده في هذا البناء.
- **المتصفح:** `npm run test:e2e` على البناء المسلَّم ⇒ سطح المكتب **43/43** في 7.5 دقيقة والهاتف **43/43** في 6.9 دقيقة، خروج **0** (سجل `/tmp/e2e_both_v159.log`). لأول مرة في جيلٍ مسلَّم: تثبيتُ الحزمة الكاملة على الهاتف أتمّ داخل سقفه الزمني، فلم تكن هناك ضحيةُ موارد. ولم يُلمَس `tests/e2e/critical-flows.spec.ts` في هذه الدفعة: اسمُ كاش الحزمة يُقرأ من `public/sw.js` فيتبع الترقية تلقائيًا.
