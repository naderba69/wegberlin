import{existsSync}from"node:fs";import{evaluateFeatureProposal,featureProposalRegistry}from"../src/config/feature-proposal-registry";const ids=new Set<string>(),issues:string[]=[];for(const proposal of featureProposalRegistry){if(ids.has(proposal.id))issues.push(`${proposal.id}: duplicate id`);ids.add(proposal.id);const result=evaluateFeatureProposal(proposal);if(!result.accepted)issues.push(`${proposal.id}: ${result.reasons.join("; ")}`);for(const evidence of proposal.automatedEvidence)if(!existsSync(evidence))issues.push(`${proposal.id}: missing ${evidence}`)}if(issues.length)throw new Error(`Feature proposal gate failed:\n${issues.join("\n")}`);console.log(`Feature proposal gate passed: ${featureProposalRegistry.length} decision-changing proposals / 0 unowned or no-impact rows.`);
