import{readdir,readFile,writeFile}from"node:fs/promises";import{join,relative}from"node:path";import{gzipSync}from"node:zlib";const root=process.cwd(),dir=join(root,".next/static/chunks");async function walk(d){const out=[];for(const e of await readdir(d,{withFileTypes:true})){const p=join(d,e.name);if(e.isDirectory())out.push(...await walk(p));else if(e.name.endsWith(".js"))out.push(p)}return out}const files=await walk(dir),rows=[];for(const f of files){const b=await readFile(f);rows.push({path:relative(root,f),rawBytes:b.length,gzipBytes:gzipSync(b,{level:9}).length})}rows.sort((a,b)=>b.gzipBytes-a.gzipBytes);const total=rows.reduce((n,x)=>n+x.gzipBytes,0),max=rows[0]?.gzipBytes??0,budgets={totalChunkGzipBytes:2500000,maxSingleChunkGzipBytes:750000},safetyMarginPercent=15,safetyFactor=1-safetyMarginPercent/100,safetyCeilings={totalChunkGzipBytes:Math.floor(budgets.totalChunkGzipBytes*safetyFactor),maxSingleChunkGzipBytes:Math.floor(budgets.maxSingleChunkGzipBytes*safetyFactor)};const issues=[];if(total>safetyCeilings.totalChunkGzipBytes)issues.push(`total ${total} > safety ceiling ${safetyCeilings.totalChunkGzipBytes}`);if(max>safetyCeilings.maxSingleChunkGzipBytes)issues.push(`chunk ${max} > safety ceiling ${safetyCeilings.maxSingleChunkGzipBytes}`);const report={format:"dwnb-js-budget-report",version:"js-budget-v1",generatedAt:"2026-09-10",safetyMarginPercent,budgets,safetyCeilings,actual:{chunkCount:rows.length,totalChunkGzipBytes:total,maxSingleChunkGzipBytes:max},issues,largest:rows.slice(0,20),boundary:"Built Next static chunk gzip envelope with a mandatory 15% reserve below hard limits; not network waterfall or device performance."};await writeFile("reports/js-budget-report.json",JSON.stringify(report,null,2)+"\n");if(issues.length)throw new Error(`JavaScript budget failed: ${issues.join(", ")}`);console.log(`JavaScript budget passed: ${rows.length} chunks / ${total} gzip bytes / max ${max}`)
