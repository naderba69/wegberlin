import { readFileSync,writeFileSync } from "node:fs";
import { buildContentGovernanceAudit } from "../src/core/content-validation/content-governance";
const REPORT="reports/content-governance-audit.json";const DOC="docs/generated/CONTENT_GOVERNANCE_REPORT.md";const audit=buildContentGovernanceAudit();
const markdown=`# Content accountability, lifecycle, claims, and pronunciation audit\n\nVersion: \`${audit.version}\`  \nPolicy: \`${audit.policyVersion}\`  \nCurriculum: \`${audit.curriculum.curriculumVersion}\` (app \`${audit.curriculum.appVersion}\`)  \nContent SHA-256: \`${audit.contentSha256}\`\n\n## Result\n\n${audit.ok?"PASS":"FAIL"}\n\n| Contract | Count |\n|---|---:|\n| Content records with owner + reviewer + lifecycle | ${audit.counts.contentRecords} |\n| Content families Draft → Validated → Published | ${audit.counts.contentFamilies} |\n| Published families | ${audit.counts.publishedFamilies} |\n| External source records with owner + reviewer | ${audit.counts.sources} |\n| Learner-risk types with owner + reviewer | ${audit.counts.ownedRiskTypes} |\n| General consular/legal context claims | ${audit.counts.highRiskContextClaims} |\n| Arabic-learner pronunciation inventory | ${audit.counts.pronunciationInventoryItems} |\n| Inventory rows with existing interactive synthetic pair | ${audit.counts.pronunciationInteractivePairs} |\n| Inventory rows mapped to articulation diagram | ${audit.counts.pronunciationDiagramCoverage} |\n| Issues | ${audit.issues.length} |\n\n## Lifecycle by family\n\n${Object.entries(audit.familyCounts).map(([scope,count])=>`- \`${scope}\`: ${count} published records; automated validation passed, independent review pending.`).join("\n")}\n\n## Claim boundaries\n\n- Publication means the validated route is available; it does not mean independent German, Arabic, CEFR, or rights approval.\n- All ${audit.counts.highRiskContextClaims} consular/legal rows are language practice with \`officialClaimStatus=not-claimed\` and specialist review pending.\n- All ${audit.counts.pronunciationInventoryItems} pronunciation rows set \`notUniversal=true\`; current diagrams and ${audit.counts.pronunciationInteractivePairs} synthetic pairs are teaching support, not a learner pronunciation score. Independent phonetics review remains pending.\n- Named IDs assign accountable product roles; they do not fabricate that a pending human review occurred.\n`;
const serialized=`${JSON.stringify(audit,null,2)}\n`;
if(process.argv.includes("--write")){writeFileSync(REPORT,serialized);writeFileSync(DOC,markdown)}else{if(readFileSync(REPORT,"utf8")!==serialized||readFileSync(DOC,"utf8")!==markdown)throw new Error("Content governance audit artifacts are stale; run npm run governance:audit:write.")}
if(!audit.ok)throw new Error(`Content governance audit failed:\n- ${audit.issues.join("\n- ")}`);console.log(`Content governance verified: ${audit.counts.contentRecords} records / ${audit.counts.contentFamilies} families / ${audit.counts.sources} sources / ${audit.counts.ownedRiskTypes} risks / ${audit.counts.highRiskContextClaims} claims / ${audit.counts.pronunciationInventoryItems} pronunciation rows.`);
