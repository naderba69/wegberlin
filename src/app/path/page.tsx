import type { Metadata } from "next";
import { PathView } from "@/components/path-view";
export const metadata: Metadata = { title: "مسار التعلّم" };
export default function PathPage() { return <PathView />; }
