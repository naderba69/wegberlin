"use client";import{useState}from"react";import{Check,Scale}from"lucide-react";import{contextAppropriatenessItems,CONTEXT_APPROPRIATENESS_POLICY_VERSION}from"@/data/context-appropriateness-quiz";export function ContextAppropriatenessQuiz(){const[index,setIndex]=useState(0),[answer,setAnswer]=useState<number|null>(null);const item=contextAppropriatenessItems[index];const checked=answer!==null;return <section className="context-appropriateness-quiz" data-context-appropriateness-policy={CONTEXT_APPROPRIATENESS_POLICY_VERSION}><header><Scale size={20}/><div><small lang="de" dir="ltr">Richtig geformt oder passend im Kontext?</small><h2>الشكل الصحيح لا يكفي دائمًا</h2><p>اختر الصيغة الأنسب للموقف، ثم اقرأ سبب الملاءمة. لا تُمنح mastery من هذا المختبر.</p></div><b>{index+1}/{contextAppropriatenessItems.length}</b></header><article><span>{item.level}</span><strong lang="de" dir="ltr">{item.contextDe}</strong><small>{item.contextAr}</small><div>{item.options.map((option,optionIndex)=><button type="button" key={option} disabled={checked} aria-pressed={answer===optionIndex} onClick={()=>setAnswer(optionIndex)} className={checked?(optionIndex===item.correctIndex?"correct":answer===optionIndex?"wrong":""):""}><b lang="de" dir="ltr">{option}</b>{checked&&optionIndex===item.correctIndex&&<Check size={15}/>}</button>)}</div>{checked&&<footer><strong>{answer===item.correctIndex?"مناسب للسياق":"الشكل مفهوم، لكن السياق يطلب بديلًا"}</strong><p>{item.explanationAr}</p><button type="button" onClick={()=>{setIndex((index+1)%contextAppropriatenessItems.length);setAnswer(null)}}>السؤال التالي</button></footer>}</article></section>}
