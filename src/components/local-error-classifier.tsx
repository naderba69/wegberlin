"use client";import{useMemo,useState}from"react";import{BrainCircuit}from"lucide-react";import{trainLocalErrorClassifier}from"@/core/errors/local-classifier";import{useLearning}from"./learning-provider";export function LocalErrorClassifier(){const{state}=useLearning();const model=useMemo(()=>trainLocalErrorClassifier(state.errors),[state.errors]);const[text,setText]=useState("");const[result,setResult]=useState<ReturnType<typeof model.classify>>(null);return <section className="local-error-classifier" data-error-classifier-policy={model.policyVersion}><header><BrainCircuit size={18}/><div><small lang="de" dir="ltr">Kleines lokales Fehlermodell</small><strong>اقتراح فئة من أخطائك أنت</strong></div><b>{model.trainingSampleCount} عينات</b></header>{model.ready?<><label><span>اكتب الجملة التي تريد فرزها محليًا</span><input lang="de" dir="ltr" value={text} onChange={(event)=>{setText(event.target.value);setResult(null)}}/></label><button type="button" disabled={!text.trim()} onClick={()=>setResult(model.classify(text))}>صنّف محليًا</button>{result&&<p role="status">الفئة المقترحة: <b data-bidi-scope="technical" dir="ltr">{result.type}</b> · ثقة نمطية {result.confidence}. لا نعيد تصنيف سجلك تلقائيًا.</p>}</>:<p>نحتاج أربعة أخطاء محلية في فئتين على الأقل قبل التدريب. لا تُرفع بيانات أو أوزان.</p>}<footer>نموذج Naive Bayes صغير يتدرب مؤقتًا داخل المتصفح؛ الاقتراح ليس تشخيصًا ولا مصححًا كاملًا ولا يغير mastery.</footer></section>}
