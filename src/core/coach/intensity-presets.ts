import type{LearnerProfile,LearningState,PlanningIntensityPreset,PlanningIntensitySettings}from"@/types/learning";
export const PLANNING_INTENSITY_POLICY="learner-selected-intensity-presets-v1"as const;export const PLANNING_INTENSITY_BOUNDARY="learner-selected-session-budget-no-automatic-increase-mastery-or-penalty"as const;const budgets=[10,20,30,45,60,90]as const;
export const DEFAULT_PLANNING_INTENSITY:PlanningIntensitySettings={policyVersion:PLANNING_INTENSITY_POLICY,preset:"balanced",evidenceBoundary:PLANNING_INTENSITY_BOUNDARY};
export function planningIntensityBaseMinutes(state:LearningState):LearnerProfile["dailyMinutes"]{const contract=[...state.learningContracts].sort((a,b)=>b.revision-a.revision||Date.parse(b.createdAt)-Date.parse(a.createdAt))[0];return(contract?.dailyMinutes??state.profile?.dailyMinutes??45)as LearnerProfile["dailyMinutes"]}
export function minutesForIntensity(base:LearnerProfile["dailyMinutes"],preset:PlanningIntensityPreset):LearnerProfile["dailyMinutes"]{if(preset==="balanced")return base;if(preset==="light")return Math.min(base,20)as LearnerProfile["dailyMinutes"];const index=budgets.indexOf(base);return budgets[Math.min(budgets.length-1,index+1)]}
export function planningPresetMinutes(state:LearningState){const base=planningIntensityBaseMinutes(state);return{base,preset:state.planningIntensity.preset,minutes:minutesForIntensity(base,state.planningIntensity.preset)}}
export function selectPlanningIntensity(state:LearningState,preset:PlanningIntensityPreset,now=new Date()):LearningState{return{...state,planningIntensity:{policyVersion:PLANNING_INTENSITY_POLICY,preset,selectedAt:now.toISOString(),evidenceBoundary:PLANNING_INTENSITY_BOUNDARY}}}
