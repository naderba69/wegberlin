import type{TrainingInteractionEvent}from"@/types/learning";export const TRAINING_INTERACTION_POLICY="training-interaction-log-v1"as const;export function appendTrainingInteraction(events:TrainingInteractionEvent[],input:Omit<TrainingInteractionEvent,"id"|"policyVersion"|"sequence"|"evidenceBoundary"|"createdAt">,now=new Date(),id=`training-event-${crypto.randomUUID()}`){const related=events.filter(x=>x.surface===input.surface&&x.contentId===input.contentId);const last=related.at(-1);if(last&&last.event===input.event&&now.getTime()-Date.parse(last.createdAt)<2000)return events;return[...events,{id,policyVersion:TRAINING_INTERACTION_POLICY,...input,sequence:related.length+1,evidenceBoundary:"process-metadata-only-no-answer-text-correctness-mastery-or-keystrokes",createdAt:now.toISOString()}as TrainingInteractionEvent]}
