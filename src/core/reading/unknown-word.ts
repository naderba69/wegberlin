import type{ReadingLibraryItem}from"@/types/library";import{wordFamilies}from"@/data/lexical-strategy-registry";
export const UNKNOWN_WORD_STRATEGY_VERSION="unknown-word-and-compound-strategy-v1" as const;
const parts=["wohnung","suche","arbeit","platz","reise","planung","stadt","entwicklung","umwelt","schutz","teilnahme","bedingung","information","quelle","verkehr","mittel","nachbar","schaft","gesundheit","system","projekt","bericht","zeit","raum","weg","ziel","gruppe","sprache","kurs","termin"];
export function splitKnownCompound(word:string){const clean=word.toLocaleLowerCase("de-DE").replace(/[^a-zäöüß]/g,"");for(let i=4;i<clean.length-3;i++){const left=clean.slice(0,i),right=clean.slice(i);if(parts.includes(left)&&parts.includes(right))return[left,right]}return[]}
function bare(form:string){return form.replace(/^(?:der|die|das)\s+/i,"").trim()}
export function unknownWordChallenge(item:ReadingLibraryItem,index=0){const text=item.textDe.toLocaleLowerCase("de-DE");const candidates=wordFamilies.flatMap(f=>f.members).map(m=>({...m,word:bare(m.formDe)})).filter(m=>m.word.length>=5&&text.includes(m.word.toLocaleLowerCase("de-DE")));if(!candidates.length)return null;const target=candidates[index%candidates.length];const split=splitKnownCompound(target.word);return{policyVersion:UNKNOWN_WORD_STRATEGY_VERSION,surfaceDe:target.word,meaningAr:target.meaningAr,compoundParts:split,stepsDe:["Wortart vermuten","Kontext prüfen",split.length?"Kompositum teilen":"Nicht künstlich teilen","Bedeutung erst dann öffnen"],boundary:"hypothesis-before-authored-gloss-no-inferred-meaning-or-mastery" as const}}
