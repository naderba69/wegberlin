export const STYLE_ALTERNATIVE_POLICY_VERSION="same-text-style-alternatives-v1"as const;const frames={colloquial:["Ich finde, …","Für mich ist klar: …"],neutral:["Meiner Ansicht nach …","Aus meiner Sicht …"],formal:["Es ist hervorzuheben, dass …","Unter diesen Bedingungen ist zu berücksichtigen, dass …"]}as const;export function buildStyleAlternativePractice(text:string){const excerpt=(text.trim().split(/(?<=[.!?])\s+/u)[0]??text.trim()).slice(0,240);return{policyVersion:STYLE_ALTERNATIVE_POLICY_VERSION,sourceExcerpt:excerpt,registers:Object.entries(frames).map(([register,options])=>({register:register as keyof typeof frames,options:[...options]})),evidenceBoundary:"rewrite-same-learner-meaning-with-multiple-frames-no-single-correct-answer-score-or-mastery"as const}}
