export type DiffSegment={kind:"equal"|"added"|"removed";text:string};const tokens=(s:string)=>s.trim().split(/\s+/).filter(Boolean);export function buildWritingVersionDiff(before:string,after:string):DiffSegment[]{const a=tokens(before),b=tokens(after),dp=Array.from({length:a.length+1},()=>Array(b.length+1).fill(0));for(let i=a.length-1;i>=0;i--)for(let j=b.length-1;j>=0;j--)dp[i][j]=a[i]===b[j]?dp[i+1][j+1]+1:Math.max(dp[i+1][j],dp[i][j+1]);const out:DiffSegment[]=[];const push=(kind:DiffSegment["kind"],text:string)=>{const last=out.at(-1);if(last?.kind===kind)last.text+=` ${text}`;else out.push({kind,text})};let i=0,j=0;while(i<a.length||j<b.length){if(i<a.length&&j<b.length&&a[i]===b[j]){push("equal",a[i]);i++;j++}else if(j<b.length&&(i===a.length||dp[i][j+1]>=dp[i+1][j])){push("added",b[j++])}else push("removed",a[i++])}return out}export function diffStats(segments:DiffSegment[]){return{added:segments.filter(x=>x.kind==="added").reduce((n,x)=>n+tokens(x.text).length,0),removed:segments.filter(x=>x.kind==="removed").reduce((n,x)=>n+tokens(x.text).length,0)}}
