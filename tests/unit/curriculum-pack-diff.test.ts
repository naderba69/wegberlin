import{describe,expect,it}from"vitest";import{compareCurriculumPacks,CURRICULUM_PACK_DIFF_POLICY}from"@/core/offline/curriculum-pack-diff";const c={curriculumVersion:"v2",buildFingerprint:"b",packId:"full",routeCount:306,includesAudio:false};describe("P2 pre-update curriculum pack diff",()=>{it("labels first install",()=>expect(compareCurriculumPacks(null,c)).toMatchObject({status:"first-install",routeDelta:306}));it("separates curriculum from build update",()=>expect(compareCurriculumPacks({...c,curriculumVersion:"v1",buildFingerprint:"a"},c)).toMatchObject({policyVersion:CURRICULUM_PACK_DIFF_POLICY,status:"curriculum-update",versionChanged:true}));it("labels same-version build",()=>expect(compareCurriculumPacks({...c,buildFingerprint:"a"},c)).toMatchObject({status:"build-update",versionChanged:false,buildChanged:true}));it("labels scope and route delta",()=>expect(compareCurriculumPacks({...c,packId:"a1",routeCount:58},c)).toMatchObject({status:"scope-change",scopeChanged:true,routeDelta:248}));it("keeps metadata-only boundary visible",()=>{const x=compareCurriculumPacks(c,c);expect(x.status).toBe("same-build");expect(x.evidenceBoundary).toContain("no-semantic-content-diff")})});
