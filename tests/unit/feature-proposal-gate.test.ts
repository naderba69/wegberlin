import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{evaluateFeatureProposal,featureProposalRegistry,type FeatureProposal}from"@/config/feature-proposal-registry";const base:FeatureProposal={id:"x",title:"X",decisionImpact:"mission-selection",decisionBefore:"A",decisionAfter:"B",automatedEvidence:["test"],addedPersistentFields:0,addedNetworkOrigins:0,maintenanceOwner:"owner"};describe("P2 executable coach-decision feature proposal gate",()=>{it("accepts owned tested changes that alter a named decision",()=>{expect(evaluateFeatureProposal(base)).toMatchObject({accepted:true,policyVersion:"coach-decision-feature-proposal-gate-v1"});expect(featureProposalRegistry.every((row)=>evaluateFeatureProposal(row).accepted)).toBe(true)});it("rejects no-impact complexity, missing evidence, unowned work, and new origins pending review",()=>{const result=evaluateFeatureProposal({...base,decisionImpact:"none",automatedEvidence:[],maintenanceOwner:"",addedNetworkOrigins:1});expect(result.accepted).toBe(false);expect(result.reasons).toHaveLength(4)});it("is mandatory before every production build",()=>{const pkg=JSON.parse(readFileSync("package.json","utf8"));expect(pkg.scripts.prebuild).toContain("feature:gate")})});
