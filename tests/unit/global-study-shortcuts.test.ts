import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{globalStudyShortcut}from"@/core/accessibility/global-shortcuts";const key=(code:string,target:EventTarget|null=document.body,altKey=true)=>globalStudyShortcut({code,target,altKey,repeat:false}as KeyboardEvent);describe("P2 global next-task and replay-audio shortcuts",()=>{it("maps Alt+N and Alt+R only",()=>{expect(key("KeyN")).toBe("next-task");expect(key("KeyR")).toBe("replay-audio");expect(key("KeyR",document.body,false)).toBeNull();expect(globalStudyShortcut({code:"KeyN",target:document.body,altKey:true,repeat:true}as unknown as KeyboardEvent)).toBeNull()});it("never steals keys from inputs, textareas, selects, or editable content",()=>{for(const tag of["input","textarea","select"]){expect(key("KeyN",document.createElement(tag))).toBeNull()}const editable=document.createElement("div");editable.contentEditable="true";document.body.append(editable);expect(key("KeyR",editable)).toBeNull()});it("routes next through the coach and replays a visible audio only outside exam focus",()=>{const shell=readFileSync("src/components/app-shell.tsx","utf8");expect(shell).toContain("router.push(getCoachTarget(state).href)");expect(shell).toContain("audio.currentTime=0");expect(shell).toContain("if(activeSession)return");expect(shell).toContain("Alt+N")})});
