import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{defaultState}from"@/core/portability/db";import{learningStateSchema}from"@/core/portability/schema";import{mergeLearningStates}from"@/core/portability/merge";describe("P2 local interest-composed level-controlled library",()=>{it("persists up to twelve learner-selected categories without changing level or mastery",()=>{const state={...defaultState,libraryInterestPreferences:{...defaultState.libraryInterestPreferences,categoriesAr:["العمل","السكن"]}};expect(learningStateSchema.parse(state).libraryInterestPreferences.categoriesAr).toEqual(["العمل","السكن"]);expect(state.mastery).toEqual(defaultState.mastery)});it("uses the newer local preference on merge",()=>{const older={...defaultState,updatedAt:"2026-09-10T10:00:00Z"},newer={...defaultState,libraryInterestPreferences:{...defaultState.libraryInterestPreferences,categoriesAr:["الدراسة"]},updatedAt:"2026-09-10T11:00:00Z"};expect(mergeLearningStates(older,newer).libraryInterestPreferences.categoriesAr).toEqual(["الدراسة"])});it("combines type, CEFR level, and interest filters without removing content",()=>{const ui=readFileSync("src/components/library-view.tsx","utf8");expect(ui).toContain("ركّب المكتبة حسب اهتماماتي");expect(ui).toContain('level==="all"||x.level===level');expect(ui).toContain("selectedInterests.includes(x.categoryAr)");expect(ui).toContain("لا تُحذف مادة من المكتبة")})});
