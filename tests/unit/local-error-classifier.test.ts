import{describe,expect,it}from"vitest";import{trainLocalErrorClassifier}from"@/core/errors/local-classifier";import type{ErrorRecord}from"@/types/learning";const e=(id:string,type:ErrorRecord["type"],wrong:string):ErrorRecord=>({id,type,wrong,correct:"x",explanationAr:"x",occurrences:2,lastSeenAt:"2026-09-10"});describe("P2 learner-trained in-browser error classifier",()=>{const errors=[e("1","article","Ich sehe der Mann"),e("2","article","Das ist ein Frau"),e("3","word-order","weil ich bin müde"),e("4","word-order","dass er kommt heute")];it("requires learner error samples from at least two classes",()=>{expect(trainLocalErrorClassifier(errors.slice(0,3)).ready).toBe(false);expect(trainLocalErrorClassifier(errors).ready).toBe(true)});it("classifies locally with bounded provenance but never mutates or diagnoses",()=>{const model=trainLocalErrorClassifier(errors),result=model.classify("weil ich bin krank");expect(result?.type).toBe("word-order");expect(result?.evidenceBoundary).toContain("no-correctness-diagnosis");expect(model.boundary).toContain("no-network")});it("keeps weights ephemeral rather than adding them to LearningState",()=>{const model=trainLocalErrorClassifier(errors);expect(model).not.toHaveProperty("persist");expect(JSON.stringify(model)).not.toContain("mastery")})});
