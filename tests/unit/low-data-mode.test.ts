import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{defaultState}from"@/core/portability/db";import{learningStateSchema}from"@/core/portability/schema";import{mergeLearningStates}from"@/core/portability/merge";describe("P2 persisted low-data mode",()=>{it("is strict, local, portable, and latest-settings-wins",()=>{const low={...defaultState,dataUsagePreferences:{...defaultState.dataUsagePreferences,lowDataMode:true},updatedAt:"2026-09-10T11:00:00Z"};expect(learningStateSchema.parse(low).dataUsagePreferences.lowDataMode).toBe(true);expect(mergeLearningStates({...defaultState,updatedAt:"2026-09-10T10:00:00Z"},low).dataUsagePreferences.lowDataMode).toBe(true)});it("blocks optional Offline audio and WebGPU model downloads while no audio uses autoplay",()=>{const offline=readFileSync("src/components/offline-pack-control.tsx","utf8"),webgpu=readFileSync("src/components/webgpu-model-control.tsx","utf8");expect(offline).toContain("effectiveIncludeAudio=includeAudio&&!lowDataMode");expect(offline).toContain("busy||lowDataMode");expect(webgpu).toContain("|| lowDataMode");expect(webgpu).toContain("تنزيل 130–150 MB محظور");const tsx=readFileSync("src/components/resilient-audio-player.tsx","utf8")+readFileSync("src/components/library-audio-player.tsx","utf8");expect(tsx).not.toMatch(/autoPlay(?:=|\s)/)})});
