import{describe,expect,it}from"vitest";import{auditMeaningTranslations}from"@/core/content-validation/meaning-translation";describe("P2 meaning-not-word-order Arabic translation audit",()=>{it("checks every lesson reading/listening, phrase, theory example, and library summary",()=>{const report=auditMeaningTranslations();expect(report.rowCount).toBeGreaterThan(1000);expect(report.signalCount).toBeGreaterThanOrEqual(3);expect(report.issues).toEqual([])});it("keeps independent Arabic regional clarity review explicitly separate",()=>{expect(auditMeaningTranslations().boundary).toContain("not-independent-arabic-quality-review")})});
