import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
describe("P2 JavaScript, curriculum, audio, and Offline pack budgets",()=>{it("enforces a fifteen-percent reserve below every hard build limit",()=>{const media=readFileSync("scripts/audit-media-pack-budgets.mjs","utf8"),js=readFileSync("scripts/audit-js-budgets.mjs","utf8"),pkg=JSON.parse(readFileSync("package.json","utf8"));expect(media).toContain("MARGIN=0.15");for(const token of["curriculumSourceGzipBytes","totalAudioBytes","a1:16_000_000","b2:38_000_000","full:70_000_000","Audio manifest byte total does not match physical MP3/Opus files"])expect(media).toContain(token);expect(js).toContain("safetyMarginPercent=15");expect(js).toContain("safetyCeilings");expect(pkg.scripts.postbuild).toContain("media:budget")});});
