import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("P2 post-listening note or communicative response",()=>{it("places one German-first local response control after every rendered listening question set",()=>{const source=readFileSync("src/components/library-view.tsx","utf8");expect(source).toContain('item.kind==="listening"&&<section className="listening-communicative-response"');expect(source.indexOf("listening-communicative-response")).toBeGreaterThan(source.indexOf("library-questions"));expect(source).toContain("Was würden Sie jetzt antworten oder notieren?");expect(source).toContain('<ContentNoteControl kind="library" contentId={item.id}/>');expect(source).toContain("لا يُقيّم الرد ولا يغيّر الإتقان")})});
