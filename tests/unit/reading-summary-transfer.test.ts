import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("P2 direct production transfer after every library reading",()=>{it("renders a German-first two-sentence summary bound to the current reading after its questions",()=>{const source=readFileSync("src/components/library-view.tsx","utf8");expect(source).toContain('item.kind==="reading"&&<section className="reading-summary-transfer"');expect(source.indexOf("reading-summary-transfer")).toBeGreaterThan(source.indexOf("library-questions"));expect(source).toContain("Fassen Sie den Text in zwei Sätzen zusammen.");expect(source).toContain("تلخيص كتابي مباشر مرتبط بهذا النص")})});
