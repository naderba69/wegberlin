import{describe,expect,it,vi}from"vitest";import{createRecordingMediaRecorder,RECORDING_MIME_CANDIDATES,selectRecordingMimeType}from"@/core/audio/recording-format";
describe("P2 explicit Opus-first recording format negotiation",()=>{it("prefers WebM Opus and falls back through WebM, MP3, and MP4 in declared order",()=>{expect(RECORDING_MIME_CANDIDATES).toEqual(["audio/webm;codecs=opus","audio/ogg;codecs=opus","audio/webm","audio/mpeg","audio/mp4"]);const supported=vi.fn((mime:string)=>mime==="audio/mpeg"||mime==="audio/mp4");expect(selectRecordingMimeType({isTypeSupported:supported}as never)).toBe("audio/mpeg")});it("constructs with the negotiated MIME and reports whether Opus was unavailable",()=>{class FakeRecorder{static isTypeSupported=(mime:string)=>mime==="audio/webm";mimeType="audio/webm";constructor(public stream:MediaStream,public options?:MediaRecorderOptions){}}const result=createRecordingMediaRecorder({}as MediaStream,FakeRecorder as never);expect(result).toMatchObject({mimeType:"audio/webm",fallbackUsed:true,policyVersion:"opus-first-recording-format-v1"});expect((result.recorder as unknown as FakeRecorder).options).toEqual({mimeType:"audio/webm"})});it("uses browser default only when no declared MIME is supported",()=>{class FakeRecorder{static isTypeSupported=()=>false;constructor(public stream:MediaStream,public options?:MediaRecorderOptions){}}const result=createRecordingMediaRecorder({}as MediaStream,FakeRecorder as never);expect(result.mimeType).toBe("");expect((result.recorder as unknown as FakeRecorder).options).toBeUndefined()})});
