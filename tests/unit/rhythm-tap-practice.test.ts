import{describe,expect,it}from"vitest";import{createRhythmTapAttempt}from"@/core/pronunciation/rhythm-tap";import{defaultState}from"@/core/portability/db";import{learningStateSchema}from"@/core/portability/schema";describe("P2 local rhythm tapping attempt",()=>{it("stores exactly four taps as three bounded intervals without a pronunciation score",()=>{const attempt=createRhythmTapAttempt("B1","prosody-b1-1",[100,500,900,1300],new Date("2026-09-10"),"r1");expect(attempt).toMatchObject({tapCount:4,intervalsMs:[400,400,400],input:"pointer-or-keyboard-taps"});expect(attempt).not.toHaveProperty("score");expect(attempt.evidenceBoundary).toContain("no-pronunciation")});it("rejects incomplete or non-monotonic tapping",()=>{expect(()=>createRhythmTapAttempt("A1","x",[1,2,3])).toThrow("أربع");expect(()=>createRhythmTapAttempt("A1","x",[1,2,2,4])).toThrow("متتابعة")});it("is strict, portable learning process evidence without mastery",()=>{const attempt=createRhythmTapAttempt("A2","x",[0,300,600,900],new Date(),"r");const state={...defaultState,prosodyRhythmAttempts:[attempt]};expect(learningStateSchema.safeParse(state).success).toBe(true);expect(state.mastery).toEqual(defaultState.mastery)})});
