import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{applySpeechPreferences,DEFAULT_SPEECH_PREFERENCES}from"@/core/audio/speech-preferences";import{defaultState}from"@/core/portability/db";import{learningStateSchema}from"@/core/portability/schema";describe("P2 local synthetic voice and pitch preferences",()=>{it("applies only a matching German voice and bounded pitch",()=>{const utterance={pitch:1,voice:null}as unknown as SpeechSynthesisUtterance;const de={voiceURI:"de-1",lang:"de-DE"}as SpeechSynthesisVoice,en={voiceURI:"en-1",lang:"en-US"}as SpeechSynthesisVoice;applySpeechPreferences(utterance,{...DEFAULT_SPEECH_PREFERENCES,voiceURI:"de-1",pitch:1.2},[en,de]);expect(utterance.pitch).toBe(1.2);expect(utterance.voice).toBe(de)});it("is strict and persisted without tracking or exam-grade claims",()=>{const state={...defaultState,speechPreferences:{...DEFAULT_SPEECH_PREFERENCES,voiceURI:"de-1",pitch:0.8 as const}};expect(learningStateSchema.parse(state).speechPreferences.pitch).toBe(.8);expect(state.speechPreferences).not.toHaveProperty("examGrade");expect(state.speechPreferences).not.toHaveProperty("score");expect(state.speechPreferences.evidenceBoundary).toContain("no-tracking")});it("applies preferences to every browser TTS surface",()=>{for(const file of["lesson-runner.tsx","library-audio-player.tsx","resilient-audio-player.tsx","prosody-progression-panel.tsx","targeted-listening-simulation.tsx"]){const source=readFileSync(`src/components/${file}`,"utf8");expect(source).toContain("applySpeechPreferences")}})});
